# E-901: Let $m(n)$ be minimal such that there is an $n$-uniform hypergraph with $m(n)$ edges which is $3$-chromatic. Estimate $m(n)$.
# erdos_number: 901
# erdos_status: open
# priority_tier: P2
# tags: combinatorics, hypergraphs

def erdos_e901_let_m_n_be_minimal_such_that_there_is_an() -> int
  requires true
  ensures result == 0  # open: E-901
  decreases 0
=
  return 0


def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
