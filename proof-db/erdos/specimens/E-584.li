# E-584: Let $G$ be a graph with $n$ vertices and $\delta n^{2}$ edges. Are there subgraphs $H_1,H_2\subseteq G$ such that $H_1$ has $\gg \delta^3n^2$ edges and every two edges in $H_1$ are contained in a cycle of length at most $6$, and furthermore if two edges share a vertex they are on a cycle of length $4$, and $H_2$ has $\gg \delta^2n^2$ edges and every two edges in $H_2$ are contained in a cycle of length at most $8$.
# erdos_number: 584
# erdos_status: open
# priority_tier: P2
# tags: cycles, graph_theory

def erdos_e584_let_g_be_a_graph_with_n_vertices_and_del() -> int
  requires true
  ensures result == 0  # open: E-584
  decreases 0
=
  return 0


def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
