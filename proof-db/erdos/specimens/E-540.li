# E-540: Is it true that if $A\subseteq \mathbb{Z}/N\mathbb{Z}$ has size $\gg N^{1/2}$ then there exists some non-empty $S\subseteq A$ such that $\sum_{n\in S}n\equiv 0\pmod{N}$?
# erdos_number: 540
# erdos_status: proved
# priority_tier: P2
# tags: number_theory

def erdos_e540_is_it_true_that_if_a_subseteq_mathbb_z_n() -> int
  requires true
  ensures result == 0  # open: E-540
  decreases 0
=
  return 0

extern proc erdos_e540_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
