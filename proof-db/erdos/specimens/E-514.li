# E-514: Let $f(z)$ be an entire transcendental function. Does there exist a path $L$ so that, for every $n$,\[\lvert f(z)/z^n\rvert \to \infty\]as $z\to \infty$ along $L$? Can the length of this path be estimated in terms of $M(r)=\max_{\lvert z\rvert=r}\lvert f(z)\rvert$? Does there exist a path along which $\lvert f(z)\rvert$ tends to $\infty$ faster than a fixed function of $M(r)$ (such that $M(r)^\epsilon$)?
# erdos_number: 514
# erdos_status: open
# priority_tier: P2
# tags: analysis

def erdos_e514_let_f_z_be_an_entire_transcendental_func() -> int
  requires true
  ensures result == 0  # open: E-514
  decreases 0
=
  return 0

extern proc erdos_e514_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
