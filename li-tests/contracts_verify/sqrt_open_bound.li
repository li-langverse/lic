# Float refinement VC intentionally left open for 2f (not provable for arbitrary x >= 0).
proc sqrt_open(x: float) -> float
  requires x >= 0.0
  ensures abs(result * result - x) < 1e-12
  decreases 0
=
  return x
