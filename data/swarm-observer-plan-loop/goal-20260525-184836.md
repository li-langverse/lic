---
workflow_repo: lic
cwd: .
research_goal_id: swarm_coverage
---

# Swarm observer orchestration — `orch-r0-ingest-snapshot`

**Branch:** `cursor/swarm-observer-plan-loop` (commit + push before stopping)  
**Agent:** `swarm_observer`

## Work item
- **id:** orch-r0-ingest-snapshot
- **content:** Run swarm-gap-ingest.py; reconcile registry with snapshot + audits; confirm apply-actions dry-run

## Inputs (read first)
- `data/swarm-gap-registry/registry.yaml`
- `benchmarks/data/latest/swarm-gap-actions.json`
- `data/goal-directed-agents/snapshot.json`

## Mandatory deliverables
1. Confirm `swarm-gap-ingest.py` + `swarm-gap-apply-actions.py` ran (or re-run and reconcile).
2. Write **`docs/ecosystem/orchestrator-notes/2026-05-25-orch-r0-ingest-snapshot.md`** with:
   - Gap counts by `gap_kind` (competitor_feature, ui_ux, plan_debt, missing_package)
   - Backlog patches applied / suggested systemd loops
   - **Self-heal actions** for control plane (prompts, observer) — no product code in lic
3. Update registry YAML for gaps you close or refine (status, evidence).
4. Run gates: `./scripts/swarm-observer-plan-gates.sh`
5. **Commit + push:** `git push -u origin cursor/swarm-observer-plan-loop`

## Gap taxonomy
| gap_kind | discoverer | actor |
|----------|------------|-------|
| competitor_feature | gap_explorer | swarm_observer |
| ui_ux | gui_ux_tester | swarm_observer |
| plan_debt | plan_verifier, implementation_gaps | swarm_observer |
| missing_package | gap_explorer | swarm_observer → issue_planner |

## Rules
- **No product code** in lic — orchestration, registry, backlogs, systemd suggestions only.
- Prefer registry + apply-actions over duplicate GitHub issues when a plan loop exists.
