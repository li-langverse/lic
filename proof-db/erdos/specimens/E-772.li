# E-772: Let $k\geq 1$ and $H_k(n)$ be the maximal $r$ such that if $A\subset\mathbb{N}$ has $\lvert A\rvert=n$ and $\| 1_A\ast 1_A\|_\infty \leq k$ then $A$ contains a Sidon set of size at least $r$. Is it true that $H_k(n)/n^{1/2}\to \infty$? Or even $H_k(n) > n^{1/2+c}$ for some constant $c>0$?
# erdos_number: 772
# erdos_status: proved
# priority_tier: P2
# tags: additive_combinatorics, number_theory, sidon_sets

def erdos_e772_let_k_geq_1_and_h_k_n_be_the_maximal_r_s() -> int
  requires true
  ensures result == 0  # open: E-772
  decreases 0
=
  return 0

extern proc erdos_e772_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
