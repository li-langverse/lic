# Swarm gap orchestration — UX dimension

**Goal:** `swarm_coverage`  
**Worker:** `43c75a50`  
**Date:** 2026-06-07  
**north_star_fit:** Swarm gap orchestration — registry, backlog apply, handoffs — domains: ecosystem, ai  
**Publish target:** `research-findings/whitepapers/2026-06/swarm_coverage/ux/`

---

## Abstract

This pass audits swarm gap orchestration through an **operator UX** lens: can a human (or meta-agent) understand swarm health, gap apply status, and UX-related backlog without manual log spelunking? The ecosystem grades **D (65.8)** with `unattended_safe: false`. Primary UX failures are missing control-plane disk mirrors, zero sampled runs in the quality scorecard, and an invisible gap-apply pipeline blocked by PyYAML absence and a recurring ingest syntax error (now fixed).

---

## UX audit matrix

| Surface | Expected UX | Observed | Score |
|---------|-------------|----------|-------|
| Dashboard health | `state.json` + `latest-report.json` feed `/api/runtime` | Missing pre-pass; bootstrapped this run | 3/10 |
| Run history | Recent agent runs visible | `runs_sampled=0` (path `/workspace/li-cursor-agents/data/runs` empty) | 2/10 |
| Gap apply status | `swarm-gap-actions.json` + registry reconcile visible | Stale (2026-05-31); no UI panel | 4/10 |
| Briefing alignment | Top agents reflected in executions | Drift: `ci_maintainer`, `security_auditor`, `pr_merger` recommended; lane runs `swarm_observer` | 5/10 |
| Studio UX debt | `ui_ux` gaps linked to `gui_ux_tester` | UX-16/17 open; UX-21/24 patched in plan loop | 6/10 |
| Error messages | Actionable ingest/apply failures | SyntaxError line 229 (fixed); PyYAML message generic | 5/10 |

**Composite UX score:** 4.2/10 — **degraded operator experience**.

---

## Gap taxonomy (UX-relevant)

| `gap_kind` | Open count | UX impact | Primary route |
|------------|------------|-----------|---------------|
| `plan_debt` (studio-ui) | 4 UX todos | Product studio polish blocked | `gui_ux_tester` / `ui_ux_quality` |
| `plan_debt` (orch-r4) | 1 | Meta-orchestration UX signals incomplete | `swarm_observer` (this goal) |
| `competitor_feature` | 30 | Benchmark honesty UX for scientific users | `gap_explorer` + research verticals |
| `missing_package` | 3 | Std module UX gaps | `issue_planner` |

Evidence: `/workspace/lic/data/swarm-gap-registry/registry.yaml`, `/workspace/benchmarks/data/latest/swarm-gap-actions.json`.

---

## Recommendations (control-plane UX)

1. **Dashboard panel** — render `swarm-gap-actions.json` apply patches + open gap count by kind.
2. **Observer persist** — write `state.json` + `latest-report.json` every supervisor tick.
3. **Worker image** — bake `python3-yaml`; set `LI_CURSOR_AGENTS_ROOT=/app`.
4. **Goal scaffold** — `config/goal-scaffolds/swarm_coverage.md` dimension checklist (shipped).
5. **Handoff** — `gui_ux_tester` for studio-ux-16/17; `pr_merger` → lip#52 for immediate briefing P0.

---

## Evidence paths

- `/workspace/benchmarks/data/latest/ecosystem-quality-report.json`
- `/workspace/benchmarks/data/latest/agent-briefing.json`
- `/workspace/benchmarks/data/latest/swarm-gap-actions.json`
- `/workspace/lic/data/swarm-gap-registry/registry.yaml`
- `/workspace/lic/docs/ecosystem/orchestrator-notes/2026-06-07-orch-r4-ui-ux-signals-43c75a50.md`
- `/app/data/runs/swarm_observer-1780874648585.md`
