# Release notes: httpd M2 TLS/H2 scale (m2-tls-h2)

**Branch:** `cursor/httpd-plan-continue`

## Summary

M2 agent-scale gateway config and runtime oracles: TLS 1.3 terminate profile, HTTP/2 ALPN gate, WebSocket route requires, cold-queue 429 backpressure, upstream circuit breaker, and HTTPS-only webhook egress allowlist.

## Agent continuation

1. Read `scripts/httpd_m2.py`, `li-tests/config_desugar/good/m2_agent_scale.toml`, `runtime/li_rt_httpd.c` M2 parsers, `runtime/li_rt_net.c` queue/circuit paths.
2. Run `HTTPD_GATES_SKIP_LIC_BUILD=1 ./scripts/httpd-plan-gates.sh` and `./scripts/check-httpd-m2-config.sh`.
3. Next: wire TLS terminate + ALPN `h2` on the live listener (OpenSSL/mbedTLS boundary); HPACK/WS handshake state machines; tier5 `h2_static` scenario when bench host has `h2load`.
4. Blocked: full `lic build` on this host (LLVM 22 emit warnings fail ninja) — oracle compile gate needs green `build/compiler/lic/lic`.

## Changed

- `scripts/httpd_m2.py` — validate + flatten M2 TOML (`[server.http2]`, `[route.queue]`, `[circuit_breaker]`, `[webhooks]`, `require=websocket`)
- `scripts/check-httpd-m2-config.sh`, `scripts/httpd-plan-gates.sh` — M2 smoke + `m2_tls_h2_oracle` compile hook
- `li-tests/config_desugar/good/m2_agent_scale.toml`, `reject/m2_*.toml`
- `li-tests/httpd/m2_tls_h2_oracle.li`
- `runtime/li_rt_httpd.c`, `runtime/li_rt.h` — M2 TOML parse + oracle getters/selftest
- `runtime/li_rt_net.c` — queue 429 (`Retry-After`), circuit breaker on upstream peers
- `scripts/httpd_config.py` — `require=websocket` in route DSL
- `docs/superpowers/plans/2026-05-16-li-httpd-plan.md` — `m2-tls-h2` → `completed`

## Not changed

- On-wire TLS 1.3 record layer / ALPN negotiation (config + oracle only)
- HTTP/2 framing (HPACK, multiplex) and WebSocket upgrade handshake on the socket path
- Live GitHub Pages bench refresh (`SKIP_BENCH=1`; no new tier5 CSV this slice)

## Breaking

N/A — new optional M2 tables; existing M1/M1.5 configs unchanged.

## Security

Webhook egress allowlist rejects private/loopback `https` hosts at `lic validate-config` time (SSRF guard).

## Performance

N/A — policy hooks only; no published RPS delta.

## Downstream

Flatten adds `m2_*` keys to `httpd.runtime.conf` for C loader; proxy path emits 429 when queue saturated or all peers circuit-open/down.

## Test plan

```bash
export PYTHONPATH=scripts
chmod +x scripts/check-httpd-m2-config.sh
./scripts/check-httpd-m2-config.sh
HTTPD_GATES_SKIP_LIC_BUILD=1 ./scripts/httpd-plan-gates.sh
# When lic builds:
./scripts/httpd-plan-gates.sh
```
