# E-870: Let $k\geq 3$ and $A$ be an additive basis of order $k$. Does there exist a constant $c=c(k)>0$ such that if $r(n)\geq c\log n$ for all large $n$ then $A$ must contain a minimal basis of order $k$? (Here $r(n)$ counts the number of representations of $n$ as the sum of at most $k$ elements from $A$.)
# erdos_number: 870
# erdos_status: open
# priority_tier: P2
# tags: additive_basis, number_theory

def erdos_e870_let_k_geq_3_and_a_be_an_additive_basis_o() -> int
  requires true
  ensures result == 0  # open: E-870
  decreases 0
=
  return 0


def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
