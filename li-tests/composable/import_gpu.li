import gpu

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  if li_gpu_version() < 1:
    return 1
  if gpu_select_backend_stub(gpu_backend_rocm()) != gpu_backend_rocm():
    return 2
  return 0
