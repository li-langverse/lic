# M-AX-PEANO-SUCC-INJ: Peano — successor is injective (∀ a b, succ a = succ b → a = b).

def proof_db_peano_succ_injective(a: int, b: int)-> int
  requires a >= 0 and b >= 0
  ensures ((a + 1) == (b + 1)) implies (a == b)
  decreases 0
=
  return 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
