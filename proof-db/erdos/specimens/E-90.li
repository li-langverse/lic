# E-90: Does every set of n distinct points in R^2 contain at most n^{1+O(1/log log n)} pairs at unit distance (unit distance problem)?
# erdos_number: 90
# erdos_status: proved
# priority_tier: P0
# tags: discrete_geometry, combinatorics

def erdos_e90_does_every_set_of_n_distinct_points_in_r() -> int
  requires true
  ensures result == 0  # open: E-90
  decreases 0
=
  return 0

extern proc erdos_e90_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
