# Catalog: E-1140
# Statement: Do there exist infinitely many $n$ such that $n-2x^2$ is prime for all $x$ with $2x^2<n$?
# formalization_status: li_open
# discharge: pending — open formalization target (Phase 4)
# erdos_status (literature): open

def formal_target_e_1140() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
