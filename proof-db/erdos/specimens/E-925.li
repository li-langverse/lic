# E-925: Is there a constant $\delta>0$ such that, for all large $n$, if $G$ is a graph on $n$ vertices which is not Ramsey for $K_3$ (i.e. there exists a 2-colouring of the edges of $G$ with no monochromatic triangle) then $G$ contains an independent set of size $\gg n^{1/3+\delta}$?
# erdos_number: 925
# erdos_status: open
# priority_tier: P2
# tags: graph_theory, ramsey_theory

def erdos_e925_is_there_a_constant_delta_0_such_that_fo() -> int
  requires true
  ensures result == 0  # open: E-925
  decreases 0
=
  return 0


def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
