# E-787: Let $g(n)$ be maximal such that given any set $A\subset \mathbb{R}$ with $\lvert A\rvert=n$ there exists some $B\subseteq A$ of size $\lvert B\rvert\geq g(n)$ such that $b_1+b_2\not\in A$ for all $b_1\neq b_2\in B$. Estimate $g(n)$.
# erdos_number: 787
# erdos_status: open
# priority_tier: P2
# tags: additive_combinatorics

def erdos_e787_let_g_n_be_maximal_such_that_given_any_s() -> int
  requires true
  ensures result == 0  # open: E-787
  decreases 0
=
  return 0


def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
