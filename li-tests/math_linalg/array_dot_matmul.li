# Phase 2i: `a @ b` is 1d float dot product (shape-checked); lowering stub returns 0.0 until 7e.
def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  var x: array[4, float]
  var y: array[4, float]
  var s: float = x @ y
  return 0
