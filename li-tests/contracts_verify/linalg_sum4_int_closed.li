# P-linalg: fixed-size int sum — closed AutoVC.
def sum4_int(a: array[4, int]) -> int
  requires true
  ensures result == a[0] + a[1] + a[2] + a[3]
  decreases 0
=
  return a[0] + a[1] + a[2] + a[3]

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  var a: array[4, int]
  a[0] = 1
  a[1] = 2
  a[2] = 3
  a[3] = 4
  var s: int = sum4_int(a)
  return 0
