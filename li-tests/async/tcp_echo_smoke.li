import std.runtime.seam

def main() raises Net, IO -> int
  requires true
  ensures result >= 0
  decreases 0
=
  var port: int = 19876
  var n: int = tcp_echo_epoll_once_i(port)
  if n > 0:
    return 0
  return 1
