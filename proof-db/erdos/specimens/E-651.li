# E-651: Let $f_k(n)$ denote the smallest integer such that any $f_k(n)$ points in general position in $\mathbb{R}^k$ contain $n$ which determine a convex polyhedron. Is it true that\[f_k(n) > (1+c_k)^n\]for some constant $c_k>0$?
# erdos_number: 651
# erdos_status: open
# priority_tier: P2
# tags: convex, geometry

def erdos_e651_let_f_k_n_denote_the_smallest_integer_su() -> int
  requires true
  ensures result == 0  # open: E-651
  decreases 0
=
  return 0


def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
