# E-1097: Let $A$ be a set of $n$ integers. How many distinct $d$ can occur as the common difference of a three-term arithmetic progression in $A$? In particular, are there always $O(n^{3/2})$ many such $d$?
# erdos_number: 1097
# erdos_status: open
# priority_tier: P2
# tags: additive_combinatorics, number_theory

def erdos_e1097_let_a_be_a_set_of_n_integers_how_many_di() -> int
  requires true
  ensures result == 0  # open: E-1097
  decreases 0
=
  return 0


def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
