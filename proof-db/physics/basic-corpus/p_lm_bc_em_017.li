# P-LM-BC-EM-017: Ohm law: V = I R for an ohmic conductor at fixed temperature.

def ohm_law(i: float, r: float) -> float
  requires r > 0.0
  ensures result == i * r
  decreases 0
=
  return i * r

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
