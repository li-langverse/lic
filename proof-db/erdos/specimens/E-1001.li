# E-1001: Let $S(N,A,c)$ be the measure of the set of those $\alpha\in (0,1)$ such that\[\left\lvert \alpha-\frac{x}{y}\right\rvert< \frac{A}{y^2}\]for some $N\leq y\leq cN$ and $(x,y)=1$. Does\[\lim_{N\to \infty}S(N,A,c)=f(A,c)\]exist? What is its explicit form?
# erdos_number: 1001
# erdos_status: open
# priority_tier: P2
# tags: diophantine_approximation, number_theory

def erdos_e1001_let_s_n_a_c_be_the_measure_of_the_set_of() -> int
  requires true
  ensures result == 0  # open: E-1001
  decreases 0
=
  return 0

extern proc erdos_e1001_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
