# E-548: Let $n\geq k+1$. Every graph on $n$ vertices with at least $\frac{k-1}{2}n+1$ edges contains every tree on $k+1$ vertices.
# erdos_number: 548
# erdos_status: open
# priority_tier: P2
# tags: graph_theory

def erdos_e548_let_n_geq_k_1_every_graph_on_n_vertices() -> int
  requires true
  ensures result == 0  # open: E-548
  decreases 0
=
  return 0

extern proc erdos_e548_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
