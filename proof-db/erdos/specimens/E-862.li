# E-862: Let $A_1(N)$ be the number of maximal Sidon subsets of $\{1,\ldots,N\}$. Is it true that\[A_1(N) < 2^{o(N^{1/2})}?\]Is it true that\[A_1(N) > 2^{N^c}\]for some constant $c>0$?
# erdos_number: 862
# erdos_status: open
# priority_tier: P2
# tags: number_theory, sidon_sets

def erdos_e862_let_a_1_n_be_the_number_of_maximal_sidon() -> int
  requires true
  ensures result == 0  # open: E-862
  decreases 0
=
  return 0

extern proc erdos_e862_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
