# Swarm gap orchestration — UX dimension (worker 7a024cc2)

**Goal id:** `swarm_coverage`  
**Publish target:** `research-findings/whitepapers/2026-06/swarm_coverage/ux/`  
**north_star_fit:** ecosystem, ai — operator-visible swarm health and gap routing

## Abstract

Li's async swarm control plane is **degraded but recoverable**. Operator UX fails when gap ingest is blocked, control-plane state is absent, and briefing priorities omit UX agents despite open `orch-r4-ui-ux-signals`. Programmatic self-heal restored gap ingest/apply this cycle; unattended operation remains unsafe until worker image bakes PyYAML and supervisor persists observer state each tick.

## UX findings

### Operator visibility gaps

- `data/control-plane/state.json` and `latest-report.json` were missing at `/app` — dashboard cannot show retry counts or interventions.
- Ecosystem scorecard defaulted `runs_sampled=0` until `LI_CURSOR_AGENTS_ROOT=/app` — swarm execution dimension falsely green with one running observer.
- 8 preflight scripts skipped (`--skip-slow`) — briefing UX hides plan/issue hygiene signals.

### Studio UI / plan_debt UX

- `studio-ui-ux` runner stopped (snapshot 2026-05-30); 7+ `studio-ux-*` registry rows show completed_ids vs plan_pending drift.
- Gap apply cannot patch `2026-05-24-studio-ui-ux-plan-loop.md` — `lic-studio-ui` worktree not mounted.

### Briefing vs scorecard drift

| Source | Top agents |
|--------|------------|
| Briefing heap | `pr_merger`, `ci_maintainer`, `security_auditor` |
| Scorecard | +`gap_explorer`, `plan_verifier` |
| UX gap handoff | `gui_ux_tester` (via `ui_ux_quality`) — **not in briefing top-3** |

## Self-heal actions (programmatic)

- Repaired ingest syntax; installed `python3-yaml` ephemerally; re-ran ingest + apply.
- Refreshed scorecard: **74.6 / C**, `unattended_safe: false`.
- Bootstrapped control-plane JSON artifacts for dashboard consumption.

## Recommendations

1. Bake `python3-yaml` + `LI_CURSOR_AGENTS_ROOT=/app` in org-research worker image.
2. Observer tick: persist `state.json` + `latest-report.json` (see `src/observer/state.ts`).
3. Dashboard: swarm health + gap-apply panel linking `swarm-gap-actions.json`.
4. Dispatch `gui_ux_tester` on `ui_ux_quality` cadence; mount `lic-studio-ui` for backlog patches.
5. Refresh goal-directed snapshot; restart 6 stopped runners.

## Evidence index

- `/workspace/benchmarks/data/latest/ecosystem-quality-report.json`
- `/workspace/benchmarks/data/latest/swarm-gap-actions.json`
- `/workspace/lic/data/swarm-gap-registry/registry.yaml`
- `/app/data/runs/swarm_observer-1780846419423.md`
- `/workspace/lic/docs/ecosystem/orchestrator-notes/2026-06-07-orch-r4-ui-ux-signals-7a024cc2.md`
