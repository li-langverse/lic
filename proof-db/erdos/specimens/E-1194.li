# E-1194: Let $A\subset\mathbb{N}$ be such that every integer $n\geq 1$ can be written uniquely as $a_n-b_n$ for some $a_n,b_n\in A$. How fast must $a_n/n$ increase?
# erdos_number: 1194
# erdos_status: open
# priority_tier: P2
# tags: additive_basis, additive_combinatorics, sidon_sets

def erdos_e1194_let_a_subset_mathbb_n_be_such_that_every() -> int
  requires true
  ensures result == 0  # open: E-1194
  decreases 0
=
  return 0


def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
