# md_integrator_verlet

WP2 harness stub: shares the `md_lennard_jones` C oracle (`md_core.c`) until an `md_integrator_verlet`-specific kernel lands. Catalog path: `benchmarks/tier2_physics/md_integrator_verlet`.
