# Catalog: E-946
# Statement: Are there infinitely many $n$ such that $\tau(n)=\tau(n+1)$, where $\tau$ is the divisor function?
# formalization_status: li_open
# discharge: pending — open formalization target (Phase 4)
# erdos_status (literature): proved

def formal_target_e_946() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
