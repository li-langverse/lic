# Swarm gap orchestration — performance dimension

**Goal:** `swarm_coverage`  
**Dimension:** `performance`  
**Worker:** `b027373c`  
**Date:** 2026-06-08  
**north_star_fit:** ecosystem, ai — blazingly-fast pillar after proof (PH-7e, PH-5b, PH-8p)  
**Publish target:** `research-findings/whitepapers/2026-06/swarm_coverage/performance/`

---

## Abstract

This pass audits swarm gap orchestration through a **performance lens**: whether benchmark regressions, numerics plan debt, and HPC competitor gaps are routed to the async research lane, and whether gap-ingest tooling keeps performance backlogs current. The swarm is **degraded** (grade D); performance work is **queued in registry** but **not refreshing** because ingest is blocked and the goal-directed snapshot is stale (2026-05-30).

---

## 1. Live benchmark posture

**Source:** `/workspace/benchmarks/data/latest/ecosystem-audit.json` (benchmarks block @ 2026-06-01)

| Class | Count | IDs |
|-------|-------|-----|
| Red (tier-1) | **0** | — |
| Yellow | **2** | `num_eig_symmetric`, `num_root_newton` |
| Near-threshold (>1.18× vs cpp) | **5** | `num_opt_bfgs`, `num_integ_semi_implicit`, `num_integ_euler`, `num_integ_rk4`, `num_cg` |
| Green | **145** | — |

**Implication:** Live tier-1 posture is **green**; yellow and near-threshold rows warrant `numerics_researcher` / `bench_improver` attention under proof-before-perf discipline. Registry still lists historical tier-1 red gaps (`gap-benchmark-red-*`) — reconcile closed when audit confirms sustained green.

---

## 2. Performance plan_debt in registry

| Area | Open todos | Swarm goal (control plane) |
|------|------------|----------------------------|
| Sim numerics | `sim-p1-num-dot-axpy`, MD neighbor cell, QM DFT-SCF | `md_sim_algorithms`, `chem_sim_algorithms` |
| HTTPD | `gap-phase2-perf-wrk-soak`, `gap-phase2-streaming-wrk` | `server_platform` |
| Master plan | Phase 7e SIMD matmul, Phase 8p parallel compile/CI | `provability_holes` + `issue_planner` |

Apply pipeline patched sim rows on 2026-05-31 (`swarm-gap-actions.json`); httpd perf todos exist in snapshot but are **not** in latest actions JSON — ingest refresh required.

---

## 3. Competitor_feature pressure (HPC / codegen)

**Open in registry:** 30 rows; performance-critical subset:

- PH-7e: `gap-competitor-pure-li-ph7e-catalog`, Kokkos, RAJA, OpenMP lowering rubric
- PH-5b: PETSc/hypre implicit PDE, GMRES, integrator reds, SUNDIALS-class stiff ODE
- Infra: `gap-infra-verticals-toml-missing-benchmarks-main` blocks vertical bench honesty ingest

**Routing:** `numerics_researcher` for sim/HPC verticals; `bench_improver` / `autoresearch` for catalog microbench improvements. Do **not** spawn new `sim` systemd loops — use research goals per `docs/ecosystem/swarm-architecture.md`.

---

## 4. Gap-ingest integrity (performance observability)

**Finding:** `swarm-gap-ingest.py` cannot run — `PyYAML required`. Last successful apply @ 2026-05-31.

**Risk:** Performance backlogs (`sim-algorithm-backlog.md`, competitor stub patches) do not reflect current snapshot or briefing. For unattended operation:

- Install `python3-yaml` in observer/briefing container
- Merge lic#1504 (ingest Path fallback + CI)
- Ship `benchmarks/competitive/verticals.toml` on main

**Grader gap:** `ecosystem-quality-grade.py` reports `runs_sampled: 0` because `runs_dir` defaults to `/workspace/li-cursor-agents/data/runs` (absent in container). Fix: fallback to `/app/data/runs` for swarm_execution dimension scoring.

---

## 5. Recommendations

1. **Unblock ingest** — PyYAML in CI image; merge lic#1504 when green.
2. **Dispatch `numerics_researcher`** on yellow + near-threshold rows; cite PH-5b/PH-7e.
3. **Route httpd perf soak** via `server_platform` goal — retired httpd systemd loop.
4. **Close registry red rows** when audit shows sustained green (avoid stale P8 dispatch).
5. **Fix grader runs_dir** — restore swarm_execution scoring in observer container.
6. **Do not** disable provability gates or Lean policy for bench velocity.

---

## Evidence index

| Artifact | Path |
|----------|------|
| Ecosystem audit | `/workspace/benchmarks/data/latest/ecosystem-audit.json` |
| Quality scorecard | `/workspace/benchmarks/data/latest/ecosystem-quality-report.json` |
| Gap registry | `/workspace/lic/data/swarm-gap-registry/registry.yaml` |
| Gap actions (stale) | `/workspace/benchmarks/data/latest/swarm-gap-actions.json` |
| Goal-directed snapshot | `/workspace/lic/data/goal-directed-agents/snapshot.json` |
| Orchestrator note | `/workspace/lic/docs/ecosystem/orchestrator-notes/2026-06-08-orch-performance-b027373c.md` |
| Observer digest | `/app/data/runs/swarm_observer-1780957694205.md` |
