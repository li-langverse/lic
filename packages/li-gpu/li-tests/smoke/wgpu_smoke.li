import gpu

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  if li_std_gpu_version() < 1:
    return 1
  var smoke: GpuWgpuSmoke = gpu_wgpu_smoke_run()
  if gpu_wgpu_smoke_passed(smoke) != 1:
    return 2
  if smoke.surface_ok != 0:
    return 3
  return 0
