# E-681: For all large n, does there exist k such that n+k is composite and p(n+k) > k^2, where p(m) is the least prime factor of m?
# erdos_number: 681
# erdos_status: open
# priority_tier: P2
# tags: number_theory, primes

def erdos_e681_for_all_large_n_does_there_exist_k_such() -> int
  requires true
  ensures result == 0  # open: E-681
  decreases 0
=
  return 0


def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
