# Swarm gap orchestration — api-coverage lens

**Goal:** `swarm_coverage`  
**Dimension:** `api-coverage`  
**Worker:** `798a4302`  
**Run:** `swarm_observer-1780802707060`  
**Generated:** 2026-06-07T03:50Z  
**north_star_fit:** ecosystem, ai — programmatic gap registry + control-plane APIs before unattended swarm

---

## Abstract

This pass audits **API and artifact coverage** for swarm gap orchestration: which machine-readable surfaces exist for meta-agents to read briefing, scorecards, gap registries, and control-plane health without human file hunting. Grade **D** (69.6) with `unattended_safe: false` reflects weak ecosystem posture and partial API coverage—not missing SDK auth.

---

## Coverage matrix

| API / artifact | Consumer | Covered? | Gap |
|----------------|----------|----------|-----|
| `agent-briefing.json` | All swarm agents | Yes | 8 preflights skipped; 2 failed |
| `ecosystem-quality-report.json` | `swarm_observer`, grader | Yes | Needs `LI_CURSOR_AGENTS_ROOT` for runs sample |
| `swarm-gap-actions.json` + `registry.yaml` | `swarm_observer`, `gap_explorer` | Yes | Apply requires PyYAML in worker image |
| `data/control-plane/latest-report.json` | Dashboard, observer | Partial | Bootstrapped this run; not persisted each tick |
| `data/control-plane/state.json` | Observer retries | Partial | Bootstrapped; `retry_counts` empty |
| MCP `get_briefing_snapshot` | Research lane | **No** | Wrong root (`fixtures/e2e-benchmarks`) |
| MCP `read_swarm_gap_registry` | Meta-agents | **No** | Recommended; not implemented |
| MCP `read_ecosystem_quality_report` | Meta-agents | **No** | Recommended; not implemented |
| CVE catalog (`cve-catalog.json`) | `security_auditor` | Partial | 19/25 MITRE Top25 missing |
| Org CI audit API | `ci_maintainer` | Blocked | GitHub rate limit HTTP 403 |
| Agent-kit audit | `agent_kit_maintainer` | Blocked | `/workspace/roadmap/agent-kit` missing |

---

## Benchmarks (api-coverage metrics)

| Metric | Value | Source |
|--------|-------|--------|
| Briefing preflight pass rate | 6/16 executed (2 fail, 8 skip) | `agent-briefing.preflight_runs` |
| Scorecard dimensions ≥75 | 1/5 (`swarm_execution` only; n=1 run) | `ecosystem-quality-report.json` |
| Gap apply pipeline | 62 open / 92 total registry rows | `swarm-gap-actions.json` |
| Recommended vs briefing heap | Aligned on P0 (`pr_merger`, `ci_maintainer`) | briefing vs scorecard `recommended_agents` |
| MCP tools for gap orchestration | 2/5 needed surfaces | `li-ecosystem-context` catalog |

---

## Remediation applied (this run)

1. Fixed `lic/scripts/swarm-gap-ingest.py` — `BENCHMARKS_COMPETITIVE` Path fallback (SyntaxError + KeyError).
2. Installed `python3-yaml`; re-ran ingest + apply (62 open gaps, backlog patches idempotent).
3. Refreshed `ecosystem-quality-report.json` with `LI_CURSOR_AGENTS_ROOT=/app`.
4. Bootstrapped control-plane JSON under `li-cursor-agents/data/control-plane/`.
5. Symlinked `/workspace/li-cursor-agents` → `/app` for scorecard runs sampling.

---

## Recommendations

1. **Worker env:** bake `LI_CURSOR_AGENTS_ROOT=/app`, `LIC_ROOT=/workspace/lic`, `python3-yaml` into org-research image.
2. **MCP:** add `read_swarm_gap_registry` and `read_ecosystem_quality_report`; fix briefing snapshot root.
3. **Observer:** persist `state.json` + `latest-report.json` each supervisor tick (`build-report.ts`).
4. **Scorecard:** fail `unattended_safe` when `runs_sampled < 10` (avoid false green on thin samples).
5. **Handoffs:** `gap_explorer` (62 open), `ci_maintainer` (14 missing CI), `security_auditor` (CWE catalog).

---

## Validity

| Field | Value |
|-------|-------|
| `validity_grade` | B- (deterministic artifacts; MCP gaps documented) |
| `status` | active |
| `publish_repo` | research-findings (staging only — repo not mounted) |
| `evidence_paths` | See orchestrator note `2026-06-07-orch-r7-api-coverage-798a4302.md` |
