# E-963: Let $f(n)$ be the maximal $k$ such that in any set $A\subset \mathbb{R}$ of size $n$ there is a subset $B\subseteq A$ of size $\lvert B\rvert\geq k$ which is dissociated that is, the sums $\sum_{b\in S}b$ are distinct for all $S\subseteq B$. Estimate $f(n)$ - in particular, is it true that\[f(n)\geq \lfloor \log_2 n\rfloor?\]
# erdos_number: 963
# erdos_status: open
# priority_tier: P2
# tags: number_theory

def erdos_e963_let_f_n_be_the_maximal_k_such_that_in_an() -> int
  requires true
  ensures result == 0  # open: E-963
  decreases 0
=
  return 0


def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
