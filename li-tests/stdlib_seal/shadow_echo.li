# EXPLOIT: user proc shadows builtin echo.
# MUST NOT compile — expected: stdlib_symbol_shadow: echo

def echo(x: int) -> int
  requires true
  ensures result == x
  decreases 0
=
  return x

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
