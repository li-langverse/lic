# E-209: Let $A$ be a finite collection of $d\geq 4$ non-parallel lines in $\mathbb{R}^2$ such that there are no points where at least four lines from $A$ meet. Must there exist a 'Gallai triangle' (or 'ordinary triangle'): three lines from $A$ which intersect in three points, and each of these intersection points only intersects two lines from $A$?
# erdos_number: 209
# erdos_status: open
# priority_tier: P2
# tags: geometry

def erdos_e209_let_a_be_a_finite_collection_of_d_geq_4() -> int
  requires true
  ensures result == 0  # open: E-209
  decreases 0
=
  return 0

extern proc erdos_e209_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
