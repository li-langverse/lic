# W8c probe — science gate + Li gpu_path_available (lic check; vendor path via runtime RT).
import chem

def main() -> int raises IO
  requires true
  ensures result == 0
  decreases 0
=
  if chem_dft_gpu_science_gate_ok() != 1:
    return 1
  if chem_dft_gpu_lkir_progress() != 1:
    return 2
  if chem_dft_gpu_path_available() != 1:
    return 3
  return 0
