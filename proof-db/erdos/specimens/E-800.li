# E-800: If $G$ is a graph on $n$ vertices which has no two adjacent vertices of degree $\geq 3$ then\[R(G)\ll n,\]where the implied constant is absolute.
# erdos_number: 800
# erdos_status: proved
# priority_tier: P2
# tags: graph_theory, ramsey_theory

def erdos_e800_if_g_is_a_graph_on_n_vertices_which_has() -> int
  requires true
  ensures result == 0  # open: E-800
  decreases 0
=
  return 0


def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
