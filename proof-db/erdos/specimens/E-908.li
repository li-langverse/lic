# E-908: Let $f:\mathbb{R}\to \mathbb{R}$ be such that $f(x+h)-f(x)$ is measurable for every $h>0$. Is it true that\[f=g+h+r\]where $g$ is continuous, $h$ is additive (so $h(x+y)=h(x)+h(y)$), and $r(x+h)-r(x)=0$ for every $h$ and almost all (depending on $h$) $x$?
# erdos_number: 908
# erdos_status: proved
# priority_tier: P2
# tags: analysis

def erdos_e908_let_f_mathbb_r_to_mathbb_r_be_such_that() -> int
  requires true
  ensures result == 0  # open: E-908
  decreases 0
=
  return 0

extern proc erdos_e908_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
