# E-1014: Let $R(k,l)$ be the Ramsey number, so the minimal $n$ such that every graph on at least $n$ vertices contains either a $K_k$ or an independent set on $l$ vertices. Prove, for fixed $k\geq 3$, that\[\lim_{l\to \infty}\frac{R(k,l+1)}{R(k,l)}=1.\]
# erdos_number: 1014
# erdos_status: proved
# priority_tier: P2
# tags: graph_theory, ramsey_theory

def erdos_e1014_let_r_k_l_be_the_ramsey_number_so_the_mi() -> int
  requires true
  ensures result == 0  # open: E-1014
  decreases 0
=
  return 0


def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
