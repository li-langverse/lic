# md_integrator_leapfrog

WP2 harness stub: shares the `md_lennard_jones` C oracle (`md_core.c`) until an `md_integrator_leapfrog`-specific kernel lands. Catalog path: `benchmarks/tier2_physics/md_integrator_leapfrog`.
