# md_oracle_external

WP2 harness stub: shares the `md_lennard_jones` C oracle (`md_core.c`) until an `md_oracle_external`-specific kernel lands. Catalog path: `benchmarks/tier2_physics/md_oracle_external`.
