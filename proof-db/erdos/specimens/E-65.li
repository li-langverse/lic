# E-65: Let $G$ be a graph with $n$ vertices and $kn$ edges, and $a_1<a_2<\cdots $ be the lengths of cycles in $G$. Is it true that\[\sum\frac{1}{a_i}\gg \log k?\]Is the sum $\sum\frac{1}{a_i}$ minimised when $G$ is a complete bipartite graph?
# erdos_number: 65
# erdos_status: open
# priority_tier: P2
# tags: cycles, graph_theory

def erdos_e65_let_g_be_a_graph_with_n_vertices_and_kn() -> int
  requires true
  ensures result == 0  # open: E-65
  decreases 0
=
  return 0


def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
