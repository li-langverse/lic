# E-1078: Let $G$ be an $r$-partite graph with $n$ vertices in each part. If $G$ has minimum degree $\geq (r-\frac{3}{2}-o(1))n$ then $G$ must contain a $K_r$.
# erdos_number: 1078
# erdos_status: proved
# priority_tier: P2
# tags: graph_theory

def erdos_e1078_let_g_be_an_r_partite_graph_with_n_verti() -> int
  requires true
  ensures result == 0  # open: E-1078
  decreases 0
=
  return 0

extern proc erdos_e1078_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
