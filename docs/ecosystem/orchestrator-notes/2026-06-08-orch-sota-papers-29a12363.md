# Orchestrator note — `orch-sota-papers` (worker `29a12363`)

**Date:** 2026-06-08  
**Agent:** `swarm_observer`  
**Research goal:** `swarm_coverage` (north_star_fit: ecosystem, ai)  
**Dimension:** `sota-papers`  
**Work item:** Reconcile SOTA paper citations in gap registry → research vertical handoffs

---

## Executive summary

| Field | Value |
|-------|-------|
| Swarm posture | **Degraded** — grade **D** (66.3); `unattended_safe: false` |
| Gap ingest | **Blocked** — `PyYAML required`; actions stale @ 2026-05-31 |
| SOTA anchors | **3/30** competitor gaps cite arxiv/OSTI; remainder need `gap_explorer` enrichment |
| Unattended? | **No** — ingest broken, control-plane state missing, 6 runners stopped |

---

## SOTA paper → research goal routing

| Registry id | Citation | PH | Handoff goal | Agent |
|-------------|----------|-----|--------------|-------|
| `gap-hpc-fftw-roofline-catalog-row` | arxiv:2506.08653 (GPU FFT roofline) | — | `numerics_sota` | `numerics_researcher` + `bench_improver` |
| `gap-hpc-raja-execution-policies` | arxiv:2402.08950 (RAJA policies) | PH-7e | `scientific_distributed_computing` | `numerics_researcher` |
| `gap-hpc-openmp-llvm-lowering-rubric` | OSTI 2224192 (OpenMP lowering) | PH-7e | `scientific_distributed_computing` | `numerics_researcher` |

**Uncited competitor gaps (27):** Kokkos, PETSc, hypre, tier-1 bench reds (historical), vertical stubs — route via existing rows in `config/research-goals.yaml`; do **not** create new agent registry ids.

**Sim plan_debt with SOTA overlap:**

| Todo | Gap id | Research vertical |
|------|--------|-------------------|
| `sim-p2-qm-dft-scf` | `gap-plan-pending-sim-sim-p2-qm-dft-scf` | `chem_sim_algorithms` |
| `chem-r2-dft-scf-gap` | `gap-plan-pending-sim-chem-research-chem-r2-dft-scf-gap` | `chem_sim_algorithms` |
| `md-r3-oracle-plan` | `gap-plan-pending-sim-md-research-md-r3-oracle-plan` | `md_sim_algorithms` |

Backlogs patched 2026-05-31 (`sim-algorithm-backlog.md`, `sim-md-research-backlog.md`, `sim-chem-research-backlog.md`).

---

## Ingest blocker (unchanged from security/performance passes)

1. **Dependency** — `PyYAML required`; not installed in observer container.
2. **Infra** — `gap-infra-verticals-toml-missing-benchmarks-main` still open.
3. **Actions JSON** — generated 2026-05-31; no refresh possible this cycle.

**Action:**

```bash
# After python3-yaml available in image:
cd lic
python3 scripts/swarm-gap-ingest.py
python3 scripts/swarm-gap-apply-actions.py
```

---

## Swarm routing (no new systemd loops)

| Next agent | Reason |
|------------|--------|
| `gap_explorer` | Enrich 27 uncited competitor gaps with SOTA references (`ecosystem_gaps` goal) |
| `numerics_researcher` | 3 paper-anchored HPC gaps + sim plan_debt |
| `plan_verifier` | Refresh snapshot plan_pending → registry (plan_audit skipped) |
| `ci_maintainer` | 12 repos missing CI |
| `issue_planner` | `gap-line-profiler-001` → `ecosystem-package-backlog.md` |

---

## Open orch todos (swarm-observer runner)

| Todo | Status | Next step |
|------|--------|-----------|
| `orch-r3-missing-package-sweep` | open | Close `gap-line-profiler-001` handoff after issue_planner |
| `orch-r4-ui-ux-signals` | open | Link `studio-ux-16/17` plan_debt to `ui_ux_quality` goal |

---

## Related artifacts

- Observer digest: `/app/data/runs/swarm_observer-1780959496653.md`
- Whitepaper: `/workspace/lic/docs/research/swarm_coverage/sota-papers/2026-06-08-whitepaper-29a12363.md`
- Scorecard: `/workspace/benchmarks/data/latest/ecosystem-quality-report.json`
