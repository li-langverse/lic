# E-296: Let $N\geq 1$ and let $k(N)$ be maximal such that there are $k$ disjoint $A_1,\ldots,A_k\subseteq \{1,\ldots,N\}$ with $\sum_{n\in A_i}\frac{1}{n}=1$ for all $i$. Estimate $k(N)$. Is it true that $k(N)=o(\log N)$?
# erdos_number: 296
# erdos_status: proved
# priority_tier: P2
# tags: number_theory, unit_fractions

def erdos_e296_let_n_geq_1_and_let_k_n_be_maximal_such() -> int
  requires true
  ensures result == 0  # open: E-296
  decreases 0
=
  return 0

extern proc erdos_e296_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
