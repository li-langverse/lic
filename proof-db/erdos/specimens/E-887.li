# E-887: Is there an absolute constant $K$ such that, for every $C>0$, if $n$ is sufficiently large then $n$ has at most $K$ divisors in $(n^{1/2},n^{1/2}+C n^{1/4})$.
# erdos_number: 887
# erdos_status: open
# priority_tier: P2
# tags: divisors, number_theory

def erdos_e887_is_there_an_absolute_constant_k_such_tha() -> int
  requires true
  ensures result == 0  # open: E-887
  decreases 0
=
  return 0

extern proc erdos_e887_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
