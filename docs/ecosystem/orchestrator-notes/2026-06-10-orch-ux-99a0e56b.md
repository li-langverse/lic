# Orchestrator note — UX gap orchestration (`99a0e56b`)

**Date:** 2026-06-10  
**Agent:** `swarm_observer`  
**Research goal:** `swarm_coverage` (north_star_fit: ecosystem, ai)  
**Dimension:** `ux`  
**Worker:** `99a0e56b`

---

## Executive summary

| Field | Value |
|-------|-------|
| Swarm posture | **Degraded** — grade **D** (67.3); `unattended_safe: false` |
| Gap prep | **Stale** — ingest/apply blocked (PyYAML missing); last success @ 03:54Z |
| Open gaps | **62** (no `ui_ux` kind; studio UX = `plan_debt`) |
| UX audit posture | **Docs-only** preflight; GUI targets not in routine briefing |
| Unattended? | **No** — missing CP state, zero run sampling, studio worktree unmounted |

---

## UX reconciliation (easy pillar — operator surfaces)

Swarm orchestration UX = how operators **see** health, gaps, and handoffs. This pass found the control plane is harder to operate than the product GUIs it audits.

### Operator blind spots

1. **No `data/control-plane/state.json` or `latest-report.json`** — dashboard cannot show retry budgets, interventions, or `swarm_degraded`.
2. **`runs_sampled: 0`** — grader points at `/workspace/li-cursor-agents/data/runs` (missing); live runs are under `/app/data/runs`.
3. **Preflight UX audits** — `ux-audit.json` / `ui-audit.json` only exercise `lic-docs` (static site). Five GUI targets in [gui-ux-quality-handoff.md](../gui-ux-quality-handoff.md) are invisible to briefing.

### Studio / GUI gap routing

| Item | Status | Handoff |
|------|--------|---------|
| `orch-r4-ui-ux-signals` | deferred (no backlog mapping) | Complete via `ui_ux_quality` → `gui_ux_tester` |
| `studio-ux-16`, `studio-ux-17` | skip — `lic-studio-ui` plan not mounted | `studio_ui_ux_builder` when `STUDIO_UI_UX_PLAN_PATH` set |
| `gap-ux-audit-agents-dashboard` | skip in last full sweep | `gui_ui_tester` + dev server ([#38](https://github.com/li-langverse/li-cursor-agents/issues/38)) |
| `gap-ux-audit-world-studio-demo` | Partial (HTML mock) | `studio_ui_ux_builder` |

Do **not** spawn new `studio-ui-ux` systemd loops — use research lane + `studio_ui_ux_builder` agent.

---

## Gap orchestration (Mode B)

```bash
# Intended each cycle (blocked this container):
cd /workspace/lic
python3 scripts/swarm-gap-ingest.py
python3 scripts/swarm-gap-apply-actions.py
# → benchmarks/data/latest/swarm-gap-actions.json
```

**Blocked:** `swarm-gap-ingest: PyYAML required` — bake `python3-yaml` into worker image (regression from performance pass).

| `gap_kind` | Open | UX-relevant action |
|------------|------|-------------------|
| `plan_debt` | 31 | `orch-r4`, studio-ux-16/17, master-plan Doc-c |
| `competitor_feature` | 30 | sim-md stubs — not operator UX |
| `missing_package` | 1 | `li-line-profiler` — dev UX tooling |

---

## Control-plane fixes (orchestration only)

1. Persist CP `state.json` + `latest-report.json` each supervisor tick.
2. Fix `ecosystem-quality-grade.py` `runs_dir` to live data mount.
3. Mount `lic-studio-ui` for gap apply backlog patches.
4. Map `orch-r4-ui-ux-signals` → `config/research-goals.yaml` `ui_ux_quality` handoff (existing agent id).

---

## Evidence paths

- `/workspace/benchmarks/data/latest/ecosystem-quality-report.json`
- `/workspace/benchmarks/data/latest/swarm-gap-actions.json`
- `/workspace/benchmarks/data/latest/ux-audit.json`
- `/workspace/benchmarks/data/latest/ui-audit.json`
- `/workspace/lic/docs/ecosystem/gui-ux-quality-handoff.md`
- `/app/data/runs/swarm_observer-1781070604615.md`

---

## Handoffs (swarm goals — no new agent ids)

| To | Reason |
|----|--------|
| `gui_ux_tester` (`ui_ux_quality`) | Five-target GUI sweep + registry UX rows |
| `studio_ui_ux_builder` | studio-ux-16/17 when worktree mounted |
| `gap_explorer` | Reconcile 62 open gaps after ingest unblocked |
| `issue_planner` | CP persistence + image PyYAML issues |
| `ci_maintainer` | 11 repos missing CI (briefing P0) |
