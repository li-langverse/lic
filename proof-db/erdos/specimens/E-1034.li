# E-1034: Let $G$ be a graph on $n$ vertices with $>n^2/4$ many edges. Must there be a triangle $T$ in $G$ and vertices $y_1,\ldots,y_t$, where $t>(\frac{1}{2}-o(1))n$, such that every vertex is joined to at least two vertices of $T$?
# erdos_number: 1034
# erdos_status: open
# priority_tier: P2
# tags: graph_theory

def erdos_e1034_let_g_be_a_graph_on_n_vertices_with_n_2() -> int
  requires true
  ensures result == 0  # open: E-1034
  decreases 0
=
  return 0

extern proc erdos_e1034_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
