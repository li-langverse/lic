# generated by li-httpd setup-censor
# source: 003_api_keys.sql

[generated.json_paths]
paths = [
  "$.api_keys[*].token",
  "$.api_keys.token",
  "$.token",
  "$.users[*].password_hash",
  "$.users.password_hash",
  "$.password_hash",
  "$.choices[*].message.tool_calls[*].raw_stderr",
]

[generated.headers]
deny_names = [
  "x-internal-api-key",
]
