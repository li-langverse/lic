# E-973: Does there exist a constant $C>1$ such that, for every $n\geq 2$, there exists a sequence $z_i\in \mathbb{C}$ with $z_1=1$ and $\lvert z_i\rvert \geq 1$ for all $1\leq i\leq n$ with\[\max_{2\leq k\leq n+1}\left\lvert \sum_{1\leq i\leq n}z_i^k\right\rvert < C^{-n}?\]
# erdos_number: 973
# erdos_status: open
# priority_tier: P2
# tags: analysis

def erdos_e973_does_there_exist_a_constant_c_1_such_tha() -> int
  requires true
  ensures result == 0  # open: E-973
  decreases 0
=
  return 0

extern proc erdos_e973_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
