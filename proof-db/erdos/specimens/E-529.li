# E-529: Let $d_k(n)$ be the expected distance from the origin after taking $n$ random steps from the origin in $\mathbb{Z}^k$ (conditional on no self intersections) - that is, a self-avoiding walk . Is it true that\[\lim_{n\to \infty}\frac{d_2(n)}{n^{1/2}}= \infty?\]Is it true that\[d_k(n)\ll n^{1/2}\]for $k\geq 3$?
# erdos_number: 529
# erdos_status: open
# priority_tier: P2
# tags: geometry, probability

def erdos_e529_let_d_k_n_be_the_expected_distance_from() -> int
  requires true
  ensures result == 0  # open: E-529
  decreases 0
=
  return 0

extern proc erdos_e529_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
