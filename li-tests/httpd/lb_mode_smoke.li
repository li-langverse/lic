# M1 LB enum surface: round_robin / least_conn / ip_hash / cookie (C runtime, Li wrapper).
import http

def main() raises IO, Alloc -> int
  requires true
  ensures result == 0
  decreases 0
=
  if lb_mode_from_name("round_robin") != 0:
    return 1
  if lb_mode_from_name("least_conn") != 1:
    return 2
  if lb_mode_from_name("ip_hash") != 2:
    return 3
  if lb_mode_from_name("cookie") != 3:
    return 4
  if lb_mode_from_name("unknown") != 0:
    return 5
  return 0
