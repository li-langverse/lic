# E-640: Let $k\geq 3$. Does there exist some $f(k)$ such that if a graph $G$ has chromatic number $\geq f(k)$ then $G$ must contain some odd cycle whose vertices span a graph of chromatic number $\geq k$?
# erdos_number: 640
# erdos_status: open
# priority_tier: P2
# tags: chromatic_number, graph_theory

def erdos_e640_let_k_geq_3_does_there_exist_some_f_k_su() -> int
  requires true
  ensures result == 0  # open: E-640
  decreases 0
=
  return 0


def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
