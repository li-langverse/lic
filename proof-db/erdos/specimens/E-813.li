# E-813: Let $h(n)$ be minimal such that every graph on $n$ vertices where every set of $7$ vertices contains a triangle (a copy of $K_3$) must contain a clique on at least $h(n)$ vertices. Estimate $h(n)$ - in particular, do there exist constants $c_1,c_2>0$ such that\[n^{1/3+c_1}\ll h(n) \ll n^{1/2-c_2}?\]
# erdos_number: 813
# erdos_status: open
# priority_tier: P2
# tags: graph_theory

def erdos_e813_let_h_n_be_minimal_such_that_every_graph() -> int
  requires true
  ensures result == 0  # open: E-813
  decreases 0
=
  return 0

extern proc erdos_e813_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
