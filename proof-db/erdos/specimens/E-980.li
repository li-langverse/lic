# E-980: Let $k\geq 2$ and $n_k(p)$ denote the least $k$th power nonresidue of $p$. Is it true that\[\sum_{p<x} n_k(p)\sim c_k \frac{x}{\log x}\]for some constant $c_k>0$?
# erdos_number: 980
# erdos_status: proved
# priority_tier: P2
# tags: number_theory

def erdos_e980_let_k_geq_2_and_n_k_p_denote_the_least_k() -> int
  requires true
  ensures result == 0  # open: E-980
  decreases 0
=
  return 0


def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
