# E-860: Let $h(n)$ be such that, for any $m\geq 1$, in the interval $(m,m+h(n))$ there exist distinct integers $a_i$ for $1\leq i\leq \pi(n)$ such that $p_i\mid a_i$, where $p_i$ denotes the $i$th prime. Estimate $h(n)$.
# erdos_number: 860
# erdos_status: open
# priority_tier: P2
# tags: number_theory, primes

def erdos_e860_let_h_n_be_such_that_for_any_m_geq_1_in() -> int
  requires true
  ensures result == 0  # open: E-860
  decreases 0
=
  return 0

extern proc erdos_e860_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
