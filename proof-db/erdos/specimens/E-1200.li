# E-1200: There exists a constant $C$ such that for all large $x$ there is a collection of primes $p_1<\ldots<p_k<x$ with $\sum\frac{1}{p_i}<C$ together with a system of congruences $a_i\pmod{p_i}$ such that every integer $n<x$ satisfies at least one of these congruences.
# erdos_number: 1200
# erdos_status: open
# priority_tier: P2
# tags: number_theory, primes

def erdos_e1200_there_exists_a_constant_c_such_that_for() -> int
  requires true
  ensures result == 0  # open: E-1200
  decreases 0
=
  return 0


def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
