# E-971: Let $p(a,d)$ be the least prime congruent to $a\pmod{d}$. Does there exist a constant $c>0$ such that, for all large $d$,\[p(a,d) > (1+c)\phi(d)\log d\]for $\gg \phi(d)$ many values of $a$?
# erdos_number: 971
# erdos_status: open
# priority_tier: P2
# tags: number_theory

def erdos_e971_let_p_a_d_be_the_least_prime_congruent_t() -> int
  requires true
  ensures result == 0  # open: E-971
  decreases 0
=
  return 0


def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
