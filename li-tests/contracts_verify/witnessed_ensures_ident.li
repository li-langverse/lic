def callee(x: int) -> int
  requires x >= 0
  ensures result == x
  decreases 0
=
  return x

def caller() -> int
  requires true
  ensures result >= 0
  decreases 0
=
  var n: int = 5
  return callee(n)
