# E-254: Let $A\subseteq \mathbb{N}$ be such that\[\lvert A\cap [1,2x]\rvert -\lvert A\cap [1,x]\rvert \to \infty\textrm{ as }x\to \infty\]and\[\sum_{n\in A} \{ \theta n\}=\infty\]for every $\theta\in (0,1)$, where $\{x\}$ is the distance of $x$ from the nearest integer. Then every sufficiently large integer is the sum of distinct elements of $A$.
# erdos_number: 254
# erdos_status: open
# priority_tier: P2
# tags: number_theory

def erdos_e254_let_a_subseteq_mathbb_n_be_such_that_lve() -> int
  requires true
  ensures result == 0  # open: E-254
  decreases 0
=
  return 0

extern proc erdos_e254_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
