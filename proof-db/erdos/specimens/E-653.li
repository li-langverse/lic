# E-653: Let $x_1,\ldots,x_n\in \mathbb{R}^2$ and let $R(x_i)=\#\{ \lvert x_j-x_i\rvert : j\neq i\}$, where the points are ordered such that\[R(x_1)\leq \cdots \leq R(x_n).\]Let $g(n)$ be the maximum number of distinct values the $R(x_i)$ can take. Is it true that $g(n) \geq (1-o(1))n$?
# erdos_number: 653
# erdos_status: open
# priority_tier: P2
# tags: distances, geometry

def erdos_e653_let_x_1_ldots_x_n_in_mathbb_r_2_and_let() -> int
  requires true
  ensures result == 0  # open: E-653
  decreases 0
=
  return 0


def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
