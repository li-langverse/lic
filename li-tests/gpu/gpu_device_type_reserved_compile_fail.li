# WP-GPU-05 Partial: @gpu must use `def` (host/device alias laws land with device[T] types).

@gpu
proc illegal_gpu_host_surface() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
