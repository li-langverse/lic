# E-917: Let $k\geq 4$ and $f_k(n)$ be the largest number of edges in a graph on $n$ vertices which has chromatic number $k$ and is critical (i.e. deleting any edge reduces the chromatic number). Is it true that\[f_k(n) \gg_k n^2?\]Is it true that\[f_6(n)\sim n^2/4?\]More generally, is it true that, for $k\geq 6$,\[f_k(n) \sim \frac{1}{2}\left(1-\frac{1}{\lfloor k/3\rfloor}\right)n^2?\]
# erdos_number: 917
# erdos_status: open
# priority_tier: P2
# tags: chromatic_number, graph_theory

def erdos_e917_let_k_geq_4_and_f_k_n_be_the_largest_num() -> int
  requires true
  ensures result == 0  # open: E-917
  decreases 0
=
  return 0

extern proc erdos_e917_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
