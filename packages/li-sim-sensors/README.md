# li-sim-sensors

PH-SIM **SIM-5** sensor raycast bus stub for `sim_automotive` and `sim_robotics` World Studio profiles.

Import: `sim.sensors` — stable raycast sensor IDs, bounded hit distances, session-backed bus emit on `SimSessionStub`.

Proof: `lic check packages/li-sim-sensors/li-tests/smoke/sensor_bus_raycast_contract.li`
