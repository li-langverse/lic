# Catalog: E-511
# Statement: Let $f(z)\in \mathbb{C}[z]$ be a monic polynomial of degree $n$. Is it true that, for every $c>1$, the set\[\{ z\in \mathbb{C} : \lvert f(z)\rvert< 1\}\]has at most $O_c(1)$ many connected components of diameter $>c$ (where the implied cons
# formalization_status: li_open
# discharge: pending — open formalization target (Phase 4)
# erdos_status (literature): open

def formal_target_e_511() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
