# E-491: Let $f:\mathbb{N}\to \mathbb{R}$ be an additive function (i.e. $f(ab)=f(a)+f(b)$ whenever $(a,b)=1$). If there is a constant $c$ such that $\lvert f(n+1)-f(n)\rvert <c$ for all $n$ then must there exist some $c'$ such that\[f(n)=c'\log n+O(1)?\]
# erdos_number: 491
# erdos_status: proved
# priority_tier: P2
# tags: number_theory

def erdos_e491_let_f_mathbb_n_to_mathbb_r_be_an_additiv() -> int
  requires true
  ensures result == 0  # open: E-491
  decreases 0
=
  return 0


def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
