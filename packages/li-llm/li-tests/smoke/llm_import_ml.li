import llm

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  if llm_ml_import_ready() != 1:
    return 1
  if llm_matmul_indirect_bridge(1.0, 1.0) < 1:
    return 2
  return 0
