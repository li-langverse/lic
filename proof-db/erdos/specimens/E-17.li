# E-17: Are there infinitely many primes $p$ such that every even number $n\leq p-3$ can be written as a difference of primes $n=q_1-q_2$ where $q_1,q_2\leq p$?
# erdos_number: 17
# erdos_status: open
# priority_tier: P2
# tags: number_theory, primes

def erdos_e17_are_there_infinitely_many_primes_p_such() -> int
  requires true
  ensures result == 0  # open: E-17
  decreases 0
=
  return 0

extern proc erdos_e17_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
