# E-20: Let f(n,k) be minimal such that every family of n-uniform sets of size >= f(n,k) contains a k-sunflower. Is f(n,k) < c_k^n for some constant c_k>0?
# erdos_number: 20
# erdos_status: open
# priority_tier: P0
# tags: combinatorics, sunflower
# context: Hypergraph sunflower / uniform family extremal problem on Erdős–Rado type bounds f(n,k).
# context: Listed among Bloom's top open Erdős problems with a $1000 prize for k=3..

def erdos_e20_let_f_n_k_be_minimal_such_that_every_fam() -> int
  requires true
  ensures result == 0  # open: E-20
  decreases 0
=
  return 0

extern proc erdos_e20_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
