# E-369: Let $\epsilon>0$ and $k\geq 2$. Is it true that, for all sufficiently large $n$, there is a sequence of $k$ consecutive integers in $\{1,\ldots,n\}$ all of which are $n^\epsilon$-smooth?
# erdos_number: 369
# erdos_status: proved
# priority_tier: P2
# tags: number_theory

def erdos_e369_let_epsilon_0_and_k_geq_2_is_it_true_tha() -> int
  requires true
  ensures result == 0  # open: E-369
  decreases 0
=
  return 0


def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
