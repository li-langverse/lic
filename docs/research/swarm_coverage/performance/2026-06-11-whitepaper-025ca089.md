# Swarm gap orchestration — performance dimension

**Goal:** `swarm_coverage`  
**Dimension:** `performance`  
**Worker:** `025ca089`  
**Date:** 2026-06-11  
**north_star_fit:** ecosystem, ai — proof → easy → fast

---

## Abstract

This pass audits swarm health through the **performance** lens for gap orchestration. The Li numerics tier-1 suite shows **no red rows** (improved since registry seeding), but **five integrator/optimizer microbenches** sit in a **near-threshold band** (1.18–1.20× cpp) and two remain **yellow**. Gap ingest is blocked by missing PyYAML, leaving **62 open registry rows** including **stale tier-1 red competitor_feature entries** that contradict the live audit. Control-plane observer state is absent, making programmatic self-heal unobservable. Conditional unattended operation is scorecard-approved, but orchestration fixes are required before performance gaps close without human steering.

---

## 1. Benchmark posture

Source: `benchmarks/data/latest/agent-briefing.json` → `ecosystem_audit.benchmarks` (generated 2026-06-11T03:37Z).

| Class | Count | Examples |
|-------|-------|----------|
| Green | 153 | — |
| Red | 0 | (registry still lists 8+ stale reds) |
| Yellow | 2 | `num_eig_symmetric`, `num_root_newton` |
| Near-threshold | 5 | `num_opt_bfgs` (1.20×), integrators, `num_cg` |
| Unknown | 28 | FFT, stdlib, tier5 http, ANN, registry benches |

**Pillar order:** proof certificates must precede SIMD/parallel lowering (PH-7e). Near-threshold routing goes to `bench_improver` for evidence-backed kernel work, not unproved shortcuts. See [provability gaps](../../verification/provability-gaps.md).

---

## 2. Gap registry vs live audit

| Signal | Registry | Live audit | Action |
|--------|----------|------------|--------|
| Tier-1 reds | 8+ `gap-benchmark-red-*` open | `red: []` | Auto-close on ingest |
| Near-threshold | Not ingested as gaps | 5 rows | Add briefing → `bench_improver` dispatch |
| PH-7e SIMD | `gap-plan-debt-*-phase-7e-*` open | partial 1d `@` | `plan_verifier` + `numerics_researcher` |
| httpd perf | 2 pending httpd todos | snapshot stale | `server_platform` goal |

Last successful apply: `2026-06-11T00:05:46Z` — 19 backlog patches; ingest re-run **blocked** (PyYAML).

---

## 3. Swarm orchestration health

| Dimension | Score | Key signal |
|-----------|-------|------------|
| `swarm_execution` | 100 | 0 errors, 1 running |
| `gap_pressure` | 60 | 62 open gaps |
| `briefing_health` | 69 | 2 preflight failures, heap drift |
| `goal_directed_health` | 70 | 6/9 runners stopped |
| `ecosystem_posture` | 71 | 33 repos missing CI |

**Control plane gap:** `/app/data/control-plane/state.json` and `latest-report.json` missing — observer cannot surface `retry_counts`, `stopped_agents`, or `swarm_degraded`.

---

## 4. Recommendations

1. **Image:** install PyYAML so `swarm-gap-ingest.py` can refresh registry and close stale reds.
2. **Briefing:** union scorecard agents; dispatch `bench_improver` when `near_threshold` is non-empty.
3. **Ingest logic:** close `gap-benchmark-red-*` when audit `red: []` for ≥2 cycles.
4. **Research routing:** near-threshold numerics → `numerics_sota` / `bench_improver`; httpd perf → `server_platform`; PH-7e → `numerics_researcher` after proof gate.
5. **No new systemd loops** — async swarm research lane only (`docs/ecosystem/swarm-architecture.md`).

---

## 5. Evidence paths

- `benchmarks/data/latest/ecosystem-quality-report.json`
- `benchmarks/data/latest/agent-briefing.json`
- `benchmarks/data/latest/swarm-gap-actions.json`
- `lic/data/swarm-gap-registry/registry.yaml`
- `/app/data/runs/swarm_observer-1781211102593.md`
- `lic/docs/ecosystem/orchestrator-notes/2026-06-11-orch-performance-025ca089.md`

**Publish target:** `research-findings/whitepapers/2026-05/swarm_coverage/performance/` (staged locally; remote not mounted).
