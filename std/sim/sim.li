# std/sim — Li Engine step API facade (PH-SIM).
# Prefer: import sim  |  workspace packages/sim until std re-exports land.

def sim_std_tag() -> int
  requires true
  ensures result == 1
  decreases 0
=
  return 1
