# E-693: Let $k\geq 2$ and $n$ be sufficiently large depending on $k$. Let $A=\{a_1<a_2<\cdots \}$ be the set of those integers in $[n,n^k]$ which have a divisor in $(n,2n)$. Estimate\[\max_{i} a_{i+1}-a_i.\]Is this $\leq (\log n)^{O(1)}$?
# erdos_number: 693
# erdos_status: open
# priority_tier: P2
# tags: divisors, number_theory

def erdos_e693_let_k_geq_2_and_n_be_sufficiently_large() -> int
  requires true
  ensures result == 0  # open: E-693
  decreases 0
=
  return 0

extern proc erdos_e693_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
