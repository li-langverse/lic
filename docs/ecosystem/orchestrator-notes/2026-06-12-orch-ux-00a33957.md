# Orchestrator note — UX gap orchestration (`00a33957`)

**Date:** 2026-06-12  
**Agent:** `swarm_observer`  
**Research goal:** `swarm_coverage` (north_star_fit: ecosystem, ai)  
**Dimension:** `ux`  
**Worker:** `00a33957`  
**Run:** `1781229099422`

---

## Executive summary

| Field | Value |
|-------|-------|
| Swarm posture | **Degraded (conditional)** — grade **C** (76.1); `unattended_safe: true` |
| Gap prep | **Live ingest blocked** (PyYAML); last apply @ `00:05:46Z` (~26h stale) |
| Open gaps | **62** (1 missing_package, 31 plan_debt, 30 competitor_feature) |
| UX audit | **Stale** — `ux-audit.json` @ 2026-05-30; **1 docs target**; 5 GUI targets in handoff not swept |
| Control plane | **Missing** `state.json` / `latest-report.json` |
| Unattended? | **Conditional** — agents run; operator honesty for UX/gap signals is poor |

---

## UX reconciliation

### Operator surfaces

| Signal | Value | Source |
|--------|-------|--------|
| Control-plane reports | **Missing** | `/app/data/control-plane/` |
| UX preflight targets | **1** (`lic-docs`) | `benchmarks/data/latest/ux-audit.json` |
| GUI targets in handoff | **5** | `lic/docs/ecosystem/gui-ux-quality-handoff.md` |
| `ui_ux` registry rows | **0** (studio = `plan_debt`) | `data/swarm-gap-registry/registry.yaml` |
| Briefing heap agents | **2** | `agent-briefing.json` |
| Scorecard agents | **4** | `ecosystem-quality-report.json` |

### Swarm observer plan debt

- **`orch-r4-ui-ux-signals`** — open; apply deferred. **Action:** link to `ui_ux_quality` research goal → dispatch `gui_ux_tester`; ingest five `gap-ux-*` rows on next successful ingest (see `gui-ux-quality-handoff.md`).
- **`orch-r3-missing-package-sweep`** — open; route `gap-line-profiler-001` via `issue_planner`.

### Studio UI plan debt (runner `studio-ui-ux`, stopped)

- `studio-ux-16-palette-search-latency` — apply skip (missing backlog: `lic-studio-ui/.../studio-ui-ux-plan-loop.md`)
- `studio-ux-17-gpu-fail-recovery` — apply skip (same)

**Routing:** `studio_ui_ux_builder` when `STUDIO_UI_UX_PLAN_PATH` is set — not a new lic systemd loop.

---

## Gap orchestration (Mode B)

```bash
# This run (BLOCKED):
python3 scripts/swarm-gap-ingest.py      # FAIL: PyYAML required
python3 scripts/swarm-gap-apply-actions.py

# Last successful cycle @ 00:05:46Z:
# benchmarks/data/latest/swarm-gap-actions.json (19 backlog patches)
```

| `gap_kind` | Open | Patched last cycle | Handoff |
|------------|------|--------------------|---------|
| `missing_package` | 1 | pkg-line-profiler | `issue_planner` |
| `plan_debt` | 31 | sim/security backlogs | `plan_verifier`, swarm goals |
| `competitor_feature` | 30 | 9 vertical stubs → sim-md-research | `numerics_researcher`, research goals |

**Do not** recommend `install-goal-plan-loop-systemd.sh` — retired loops migrate to agents control plane per `docs/ecosystem/swarm-architecture.md`.

---

## Control-plane fixes (orchestration only)

1. Install PyYAML in worker image — unblocks ingest/apply and `orch-r4` registry rows.
2. Persist observer state on disk each tick — dashboard UX for retry budgets.
3. Union scorecard agents into briefing heap — closes goal-orientation drift.
4. Expand UX preflight — proactive five-target GUI sweep per `ux-targets.json`.
5. Mount studio plan path — enable gap-apply for `studio-ux-16/17`.

---

## Evidence paths

- `/workspace/benchmarks/data/latest/ecosystem-quality-report.json`
- `/workspace/benchmarks/data/latest/agent-briefing.json`
- `/workspace/benchmarks/data/latest/swarm-gap-actions.json`
- `/workspace/lic/data/swarm-gap-registry/registry.yaml`
- `/workspace/benchmarks/data/latest/ux-audit.json`
- `/workspace/lic/data/goal-directed-agents/snapshot.json`
- `/app/data/runs/swarm_observer-1781229099422.md`

**Next dispatch:** `gui_ux_tester` → `gap_explorer` → `plan_verifier` → `ci_maintainer` → `security_auditor`
