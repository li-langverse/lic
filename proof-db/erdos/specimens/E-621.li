# E-621: Let $G$ be a graph on $n$ vertices, $\alpha_1(G)$ be the maximum number of edges that contain at most one edge from every triangle, and $\tau_1(G)$ be the minimum number of edges that contain at least one edge from every triangle. Is it true that\[\alpha_1(G)+\tau_1(G) \leq \frac{n^2}{4}?\]
# erdos_number: 621
# erdos_status: proved
# priority_tier: P2
# tags: graph_theory

def erdos_e621_let_g_be_a_graph_on_n_vertices_alpha_1_g() -> int
  requires true
  ensures result == 0  # open: E-621
  decreases 0
=
  return 0


def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
