# E-1047: Let $f\in \mathbb{C}[x]$ be a monic polynomial with $m$ distinct roots, and let $c>0$ be a constant small enough such that $\{ z: \lvert f(z)\rvert\leq c\}$ has $m$ distinct connected components. Must all these components be convex?
# erdos_number: 1047
# erdos_status: open
# priority_tier: P2
# tags: analysis

def erdos_e1047_let_f_in_mathbb_c_x_be_a_monic_polynomia() -> int
  requires true
  ensures result == 0  # open: E-1047
  decreases 0
=
  return 0

extern proc erdos_e1047_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
