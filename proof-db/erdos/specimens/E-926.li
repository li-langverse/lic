# E-926: Let $k\geq 4$. Is it true that\[\mathrm{ex}(n;H_k) \ll_k n^{3/2},\]where $H_k$ is the graph on vertices $x,y_1,\ldots,y_k,z_1,\ldots,z_{\binom{k}{2}}$, where $x$ is adjacent to all $y_i$ and each pair of $y_i,y_j$ is adjacent to a unique $z_i$.
# erdos_number: 926
# erdos_status: proved
# priority_tier: P2
# tags: graph_theory

def erdos_e926_let_k_geq_4_is_it_true_that_mathrm_ex_n() -> int
  requires true
  ensures result == 0  # open: E-926
  decreases 0
=
  return 0


def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
