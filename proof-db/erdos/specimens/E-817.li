# E-817: Let $k\geq 3$ and define $g_k(n)$ to be the minimal $N$ such that $\{1,\ldots,N\}$ contains some $A$ of size $\lvert A\rvert=n$ such that\[\langle A\rangle = \left\{\sum_{a\in A}\epsilon_aa: \epsilon_a\in \{0,1\}\right\}\]contains no non-trivial $k$-term arithmetic progression. Estimate $g_k(n)$. In particular, is it true that\[g_3(n) \gg 3^n?\]
# erdos_number: 817
# erdos_status: open
# priority_tier: P2
# tags: additive_combinatorics

def erdos_e817_let_k_geq_3_and_define_g_k_n_to_be_the_m() -> int
  requires true
  ensures result == 0  # open: E-817
  decreases 0
=
  return 0

extern proc erdos_e817_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
