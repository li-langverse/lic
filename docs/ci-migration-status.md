# CI migration (GitLab primary)

Primary CI: `.gitlab-ci.yml` on GitLab (`gitlab.lilangverse.xyz/li-langverse/lic`).

GitHub Actions: **Pages only** (`pages.yml`). All other workflows removed.

See workspace `MIGRATION.md` for full ecosystem status, schedule variables, and required CI/CD secrets (`GITLAB_TOKEN`, `GHCR_TOKEN`).
