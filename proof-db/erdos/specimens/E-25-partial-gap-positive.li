# partial: consecutive primes strictly increase — gap is positive
# discharge: ok
# o((log n)^2) infinitely often bound remains open.

def prime_gap_at_least_one(p_next: int, p_curr: int) -> int
  requires p_next > p_curr
  ensures result >= 1
  decreases 0
=
  return 1

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  var g: int = prime_gap_at_least_one(11, 7)
  return 0
