# PH-ML Wave 4c — tracker honesty + corpus expansion

**Branch:** `feat/ph-ml-gpu-swarm`  
**Date:** 2026-05-29

## Summary

Wave 4c closes honest **blocked** rows for hardware-dependent WPs, documents lavapipe CI path, expands GPU compile_fail corpus, documents RL process-worker path with in-process serial batch **done**, and deepens `@gpu` MIR coverage without risky codegen changes.

## Stub → Real

| WP | Stub / prior | Real / exit | Verify |
|----|----------------|-------------|--------|
| WP-HW-07 | SPIR-V header only | **blocked** + [lavapipe-vulkan-smoke.md](../ci/lavapipe-vulkan-smoke.md) CI recipe | `lic check packages/lig/li-tests/smoke/kernel_launch_status.li` |
| WP-HW-08 | CPU 2×2 ref | **blocked** (device dispatch); CPU ref remains | `kernel_matmul_parity.li` + bench JSON |
| WP-HW-09 | `LIG_EMIT_CUDA=1` emit stub | **blocked** — `nvidia-smi` present, `CUDA_HOME` unset on lab host | `scripts/bench-lig-gpu-suite.sh` `cuda_hardware` field |
| WP-GPU-05 | one compile_fail | + `gpu_decorator_type_alias_compile_fail.li` + Lean corpus note | `li-tests/run_all.sh gpu` |
| WP-RL-04 | fork IPC stub | in-process `async_env_pool_tick_serial_batch` **done**; process path documented | `lic check packages/li-ml-rl/li-tests/smoke/async_env_pool_serial.li` |
| WP-GPU-04 | loose `ensures` on `@gpu` def | provable `ensures result == x + 1.0` + MIR `gpu_device` doc in `mir.hpp` | `lic build li-tests/gpu/gpu_decorator_mir.li` |

## Hardware evidence (WP-HW-09)

```
$ nvidia-smi -L
GPU 0: NVIDIA GeForce RTX 3060
$ echo CUDA_HOME=${CUDA_HOME:-unset}
CUDA_HOME=unset
```

PTX / device kernel timing remains **blocked** until `CUDA_HOME` + nvcc path wired in CI.

## Tracker deltas

See [PH-ML-GPU-execution-tracker.md](../game-dev/PH-ML-GPU-execution-tracker.md) Wave 4c rows.
