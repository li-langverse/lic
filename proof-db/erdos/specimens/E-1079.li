# E-1079: Let $r\geq 4$. If $G$ is a graph on $n$ vertices with at least $\mathrm{ex}(n;K_r)$ edges then must $G$ contain a vertex with degree $d\gg_r n$ whose neighbourhood contains at least $\mathrm{ex}(d;K_{r-1})$ edges?
# erdos_number: 1079
# erdos_status: open
# priority_tier: P2
# tags: graph_theory, turan_number

def erdos_e1079_let_r_geq_4_if_g_is_a_graph_on_n_vertice() -> int
  requires true
  ensures result == 0  # open: E-1079
  decreases 0
=
  return 0


def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
