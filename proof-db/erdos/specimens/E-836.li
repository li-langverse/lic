# E-836: Let $r\geq 2$ and $G$ be a $r$-uniform hypergraph with chromatic number $3$ (that is, there is a $3$-colouring of the vertices of $G$ such that no edge is monochromatic). Suppose any two edges of $G$ have a non-empty intersection. Must $G$ contain $O(r^2)$ many vertices? Must there be two edges which meet in $\gg r$ many vertices?
# erdos_number: 836
# erdos_status: open
# priority_tier: P2
# tags: chromatic_number, graph_theory, hypergraphs

def erdos_e836_let_r_geq_2_and_g_be_a_r_uniform_hypergr() -> int
  requires true
  ensures result == 0  # open: E-836
  decreases 0
=
  return 0

extern proc erdos_e836_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
