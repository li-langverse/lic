import bioeng
import gpu

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  if bioeng_gpu_surrogate_smoke() != 0:
    return 1
  if gpu_batch_score_stub(bioeng_gpu_batch_size_stub(), gpu_backend_rocm()) != 64:
    return 2
  return gpu_surrogate_smoke()
