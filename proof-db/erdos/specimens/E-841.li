# E-841: Let $t_n$ be minimal such that $\{n+1,\ldots,n+t_n\}$ contains a subset whose product with $n$ is a square number (and let $t_n=0$ if $n$ is itself square). Estimate $t_n$.
# erdos_number: 841
# erdos_status: open
# priority_tier: P2
# tags: number_theory

def erdos_e841_let_t_n_be_minimal_such_that_n_1_ldots_n() -> int
  requires true
  ensures result == 0  # open: E-841
  decreases 0
=
  return 0


def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
