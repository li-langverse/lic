import sim
import sim.scientific

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  if li_sim_scientific_version() != 7:
    return 1
  var cfd: float = sim_scientific_oracle_checksum_cfd()
  if cfd <= 0.0:
    return 2
  var fea: float = sim_scientific_oracle_checksum_fea()
  if fea <= 0.0:
    return 3
  var qm: float = sim_scientific_oracle_checksum_qm_dft()
  if qm < 0.0:
    return 4
  if qm == 1.001:
    return 5
  return 0
