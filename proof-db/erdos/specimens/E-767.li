# E-767: Let $g_k(n)$ be the maximal number of edges possible on a graph with $n$ vertices which does not contain a cycle with $k$ chords incident to a vertex on the cycle. Is it true that\[g_k(n)=(k+1)n-(k+1)^2\]for $n$ sufficiently large?
# erdos_number: 767
# erdos_status: proved
# priority_tier: P2
# tags: graph_theory, turan_number

def erdos_e767_let_g_k_n_be_the_maximal_number_of_edges() -> int
  requires true
  ensures result == 0  # open: E-767
  decreases 0
=
  return 0

extern proc erdos_e767_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
