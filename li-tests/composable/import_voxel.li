# PH-VOXEL-0 — import voxel (li-voxel composable gate).

import voxel

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  var v: int = li_voxel_version()
  if v < 1:
    return 1
  return 0
