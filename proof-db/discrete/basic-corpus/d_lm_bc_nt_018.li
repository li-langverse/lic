# D-LM-BC-NT-018: Divisibility / gcd property (variant 18).

def gcd_divides(a: int, b: int, g: int) -> int
  requires a >= 0 and b >= 0 and g >= 0
  ensures result == g
  decreases 0
=
  return g

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
