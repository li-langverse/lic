# E-407: Let $w(n)$ count the number of solutions to\[n=2^a+3^b+2^c3^d\]with $a,b,c,d\geq 0$ integers. Is it true that $w(n)$ is bounded by some absolute constant?
# erdos_number: 407
# erdos_status: proved
# priority_tier: P2
# tags: number_theory

def erdos_e407_let_w_n_count_the_number_of_solutions_to() -> int
  requires true
  ensures result == 0  # open: E-407
  decreases 0
=
  return 0

extern proc erdos_e407_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
