# E-471: Given a finite set of primes $Q=Q_0$, define a sequence of sets $Q_i$ by letting $Q_{i+1}$ be $Q_i$ together with all primes formed by adding three distinct elements of $Q_i$. Is there some initial choice of $Q$ such that the $Q_i$ become arbitrarily large?
# erdos_number: 471
# erdos_status: proved
# priority_tier: P2
# tags: number_theory

def erdos_e471_given_a_finite_set_of_primes_q_q_0_defin() -> int
  requires true
  ensures result == 0  # open: E-471
  decreases 0
=
  return 0

extern proc erdos_e471_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
