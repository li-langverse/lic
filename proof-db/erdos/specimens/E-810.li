# E-810: Does there exist some $\epsilon>0$ such that, for all sufficiently large $n$, there exists a graph $G$ on $n$ vertices with at least $\epsilon n^2$ many edges such that the edges can be coloured with $n$ colours so that every $C_4$ receives $4$ distinct colours?
# erdos_number: 810
# erdos_status: open
# priority_tier: P2
# tags: graph_theory, ramsey_theory

def erdos_e810_does_there_exist_some_epsilon_0_such_tha() -> int
  requires true
  ensures result == 0  # open: E-810
  decreases 0
=
  return 0


def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
