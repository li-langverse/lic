# E-271: Let $A(n)=\{a_0<a_1<\cdots\}$ be the sequence defined by $a_0=0$ and $a_1=n$, and for $k\geq 1$ define $a_{k+1}$ as the least positive integer such that there is no three-term arithmetic progression in $\{a_0,\ldots,a_{k+1}\}$. Can the $a_k$ be explicitly determined? How fast do they grow?
# erdos_number: 271
# erdos_status: open
# priority_tier: P2
# tags: additive_combinatorics, arithmetic_progressions

def erdos_e271_let_a_n_a_0_a_1_cdots_be_the_sequence_de() -> int
  requires true
  ensures result == 0  # open: E-271
  decreases 0
=
  return 0

extern proc erdos_e271_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
