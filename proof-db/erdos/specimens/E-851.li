# E-851: Let $\epsilon>0$. Is there some $r\ll_\epsilon 1$ such that the density of integers of the form $2^k+n$, where $k\geq 0$ and $n$ has at most $r$ prime divisors, is at least $1-\epsilon$?
# erdos_number: 851
# erdos_status: proved
# priority_tier: P2
# tags: number_theory

def erdos_e851_let_epsilon_0_is_there_some_r_ll_epsilon() -> int
  requires true
  ensures result == 0  # open: E-851
  decreases 0
=
  return 0

extern proc erdos_e851_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
