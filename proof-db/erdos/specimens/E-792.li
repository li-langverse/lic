# E-792: Let $f(n)$ be maximal such that in any $A\subset \mathbb{Z}$ with $\lvert A\rvert=n$ there exists some sum-free subset $B\subseteq A$ with $\lvert B\rvert \geq f(n)$, so that there are no solutions to\[a+b=c\]with $a,b,c\in B$. Estimate $f(n)$.
# erdos_number: 792
# erdos_status: open
# priority_tier: P2
# tags: additive_combinatorics

def erdos_e792_let_f_n_be_maximal_such_that_in_any_a_su() -> int
  requires true
  ensures result == 0  # open: E-792
  decreases 0
=
  return 0


def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
