# WP-RL-04: serial 8-env in-process batch (no fork IPC).

import ml.rl
import sim

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  if async_env_worker_slot_count() != 8:
    return 1
  if async_env_pool_tick_stub(8) != 0:
    return 2
  return 0
