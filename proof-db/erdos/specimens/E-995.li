# E-995: Let $n_1<n_2<\cdots$ be a lacunary sequence of integers and $f\in L^2([0,1])$. Estimate the growth of, for almost all $\alpha$,\[\sum_{1\leq k\leq N}f(\{ \alpha n_k\}).\]For example, is it true that, for almost all $\alpha$,\[\sum_{1\leq k\leq N}f(\{ \alpha n_k\})=o(N\sqrt{\log\log N})?\]
# erdos_number: 995
# erdos_status: open
# priority_tier: P2
# tags: analysis, discrepancy

def erdos_e995_let_n_1_n_2_cdots_be_a_lacunary_sequence() -> int
  requires true
  ensures result == 0  # open: E-995
  decreases 0
=
  return 0

extern proc erdos_e995_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
