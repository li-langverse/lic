# E-827: Let $n_k$ be minimal such that if $n_k$ points in $\mathbb{R}^2$ are in general position then there exists a subset of $k$ points such that all $\binom{k}{3}$ triples determine circles of different radii. Determine $n_k$.
# erdos_number: 827
# erdos_status: open
# priority_tier: P2
# tags: geometry

def erdos_e827_let_n_k_be_minimal_such_that_if_n_k_poin() -> int
  requires true
  ensures result == 0  # open: E-827
  decreases 0
=
  return 0


def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
