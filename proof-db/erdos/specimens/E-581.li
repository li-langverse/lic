# E-581: Let $f(m)$ be the maximal $k$ such that a triangle-free graph on $m$ edges must contain a bipartite graph with $k$ edges. Determine $f(m)$.
# erdos_number: 581
# erdos_status: open
# priority_tier: P2
# tags: graph_theory

def erdos_e581_let_f_m_be_the_maximal_k_such_that_a_tri() -> int
  requires true
  ensures result == 0  # open: E-581
  decreases 0
=
  return 0


def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
