# E-589: Let $g(n)$ be maximal such that in any set of $n$ points in $\mathbb{R}^2$ with no four points on a line there exists a subset on $g(n)$ points with no three points on a line. Estimate $g(n)$.
# erdos_number: 589
# erdos_status: open
# priority_tier: P2
# tags: geometry

def erdos_e589_let_g_n_be_maximal_such_that_in_any_set() -> int
  requires true
  ensures result == 0  # open: E-589
  decreases 0
=
  return 0


def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
