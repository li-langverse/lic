# PH-ML / DL / RL / LLM Wave 12 (2026-05-31)

## Summary

Final deferred sprint after Wave 11: LIG emit env progress, full `@gpu` LKIR launch pipeline smoke, safetensors 64 B/tensor mmap chunk, real GPU matmul hint via runtime LIG probe, li-httpd trusted route scaffold, RL fork/spawn IPC, SB3/Ray/TF competitive rows, 16×16 matmul perf.

## Deferred

- Vendor PTX/HS/MSL emit (progress bit only)
- `import ml` in li-llm
- In-process Li fork(2)
