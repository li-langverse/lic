# E-1037: Let $G$ be a graph on $n$ vertices in which every degree occurs at most twice, and the number of distinct degree is $>(\frac{1}{2}+\epsilon)n$. Must $G$ contain a trivial (empty or complete) subgraph of size 'much larger' than $\log n$?
# erdos_number: 1037
# erdos_status: open
# priority_tier: P2
# tags: graph_theory

def erdos_e1037_let_g_be_a_graph_on_n_vertices_in_which() -> int
  requires true
  ensures result == 0  # open: E-1037
  decreases 0
=
  return 0

extern proc erdos_e1037_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
