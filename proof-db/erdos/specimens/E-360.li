# E-360: Let $f(n)$ be minimal such that $\{1,\ldots,n-1\}$ can be partitioned into $f(n)$ classes so that $n$ cannot be expressed as a sum of distinct elements from the same class. How fast does $f(n)$ grow?
# erdos_number: 360
# erdos_status: open
# priority_tier: P2
# tags: number_theory

def erdos_e360_let_f_n_be_minimal_such_that_1_ldots_n_1() -> int
  requires true
  ensures result == 0  # open: E-360
  decreases 0
=
  return 0

extern proc erdos_e360_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
