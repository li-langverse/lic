# P-float: `li_rt_sqrt` + abs error bound — closed via Li.Discharge.sqrt_open_bound_spec (trusted libm axiom).
extern def li_rt_sqrt(x: float) -> float
  requires true
  ensures true
  decreases 0

def sqrt_open(x: float) raises IO -> float
  requires x >= 0.0
  ensures abs(result * result - x) < 1e-12
  decreases 0
=
  return li_rt_sqrt(x)
