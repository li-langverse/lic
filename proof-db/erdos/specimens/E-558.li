# Catalog: E-558
# Statement: Let $R_k(G)$ denote the minimal $m$ such that if the edges of $K_m$ are $k$-coloured then there is a monochromatic copy of $G$. Determine\[R_k(K_{s,t})\]where $K_{s,t}$ is the complete bipartite graph with $s$ vertices in one component and 
# formalization_status: li_open
# discharge: pending — open formalization target (Phase 4)
# erdos_status (literature): open

def formal_target_e_558() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
