# Orchestrator note — UX gap orchestration (`14f767e4`)

**Date:** 2026-06-11  
**Agent:** `swarm_observer`  
**Research goal:** `swarm_coverage` (north_star_fit: ecosystem, ai)  
**Dimension:** `ux`  
**Worker:** `14f767e4`

---

## Executive summary

| Field | Value |
|-------|-------|
| Swarm posture | **Degraded (conditional)** — grade **C** (76.1); `unattended_safe: true` |
| Gap prep | **Blocked in-container** — PyYAML missing; last apply @ `00:05:46Z` (~22h stale) |
| Open gaps | **62** (0 `ui_ux` kind; studio UX = `plan_debt`) |
| UX audit posture | **Docs-only** preflight; 5 GUI targets in handoff not in routine briefing |
| CP artifacts | **Missing** — no `state.json` / `latest-report.json` |
| Unattended? | **Conditional** — agents run; operators lack honest health + UX audit pane |

---

## UX reconciliation (easy pillar — operator surfaces)

Confirms prior UX workers (`20cccf19`, `0b1ac8dd`, `ea69cd5a`) with refreshed scorecard @ `22:45Z`. No material improvement in orchestration UX blockers since the 18:29Z pass.

### Operator blind spots

1. **No control-plane disk mirrors** — retry budgets, interventions, and `swarm_degraded` opaque across Job restarts.
2. **Gap pipeline frozen** — ingest/apply cannot run without PyYAML; 62 gaps last patched @ `00:05:46Z`.
3. **Preflight UX audits** — `ux-audit.json` only exercises `lic-docs`. Five GUI targets in [gui-ux-quality-handoff.md](../gui-ux-quality-handoff.md) absent from briefing.
4. **Briefing heap drift** — scorecard recommends four agents; briefing heap dispatches two.

### Studio / GUI gap routing

| Item | Status | Handoff |
|------|--------|---------|
| `orch-r4-ui-ux-signals` | open, apply deferred | Complete via `ui_ux_quality` → `gui_ux_tester` |
| `studio-ux-16`, `studio-ux-17` | skip — plan not mounted | `studio_ui_ux_builder` when `STUDIO_UI_UX_PLAN_PATH` set |
| `gap-ux-audit-agents-dashboard` | skip in last full sweep | `gui_ux_tester` + dev server ([#38](https://github.com/li-langverse/li-cursor-agents/issues/38)) |
| `gap-ux-audit-world-studio-demo` | Partial (HTML mock) | `studio_ui_ux_builder` |

Do **not** spawn new `studio-ui-ux` systemd loops — use research lane + existing agent ids per [swarm-architecture.md](../swarm-architecture.md).

---

## Gap orchestration (Mode B)

```bash
# Intended each cycle (BLOCKED this run):
cd /workspace/lic
python3 scripts/swarm-gap-ingest.py
python3 scripts/swarm-gap-apply-actions.py
```

**Blocked:** `swarm-gap-ingest: PyYAML required`

| `gap_kind` | Open | UX-relevant action |
|------------|------|-------------------|
| `plan_debt` | 31 | `orch-r4`, studio-ux-16/17, master-plan Doc-c |
| `competitor_feature` | 30 | sim-md stubs — not operator UX |
| `missing_package` | 1 | `li-line-profiler` — dev UX tooling |

### Reconciliation actions (this pass)

1. **`orch-r4-ui-ux-signals`** — handoff to `ui_ux_quality` (`gui_ux_tester`); ingest five `gap-ux-*` rows on next successful ingest.
2. **`studio-ux-16/17`** — defer until `lic-studio-ui` plan mounted at `STUDIO_UI_UX_PLAN_PATH`.
3. **Briefing alignment** — union scorecard agents (`gap_explorer`, `plan_verifier`) into heap on next `benchmarks` change.

---

## Evidence paths

- `/workspace/benchmarks/data/latest/ecosystem-quality-report.json`
- `/workspace/benchmarks/data/latest/swarm-gap-actions.json`
- `/workspace/lic/data/swarm-gap-registry/registry.yaml`
- `/workspace/benchmarks/data/latest/ux-audit.json`
- `/workspace/benchmarks/data/latest/agent-briefing.json`
- `/app/data/runs/swarm_observer-1781214697058.md`
- `/workspace/lic/docs/ecosystem/gui-ux-quality-handoff.md`
- `/app/config/research-goals.yaml` (`swarm_coverage`, `ui_ux_quality`)

---

## Handoffs (north_star_fit: ecosystem, ai — easy operator surfaces)

| Target | Agent | Goal |
|--------|-------|------|
| Five GUI UX targets | `gui_ux_tester` | `ui_ux_quality` |
| Gap registry reconcile | `gap_explorer` | `ecosystem_gaps` |
| Plan audit refresh | `plan_verifier` | briefing preflight |
| CI rollout | `ci_maintainer` | org heap P0 |
