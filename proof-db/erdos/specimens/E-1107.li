# E-1107: Let $r\geq 2$. A number $n$ is $r$-powerful if for every prime $p$ which divides $n$ we have $p^r\mid n$. Is every large integer the sum of at most $r+1$ many $r$-powerful numbers?
# erdos_number: 1107
# erdos_status: open
# priority_tier: P2
# tags: number_theory, powerful

def erdos_e1107_let_r_geq_2_a_number_n_is_r_powerful_if() -> int
  requires true
  ensures result == 0  # open: E-1107
  decreases 0
=
  return 0


def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
