# E-682: Is it true that for almost all $n$ there exists some $m\in (p_n,p_{n+1})$ such that\[p(m) \geq p_{n+1}-p_n,\]where $p(m)$ denotes the least prime factor of $m$?
# erdos_number: 682
# erdos_status: proved
# priority_tier: P2
# tags: number_theory, primes

def erdos_e682_is_it_true_that_for_almost_all_n_there_e() -> int
  requires true
  ensures result == 0  # open: E-682
  decreases 0
=
  return 0


def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
