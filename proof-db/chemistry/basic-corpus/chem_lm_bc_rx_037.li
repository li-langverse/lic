# CHEM-LM-BC-RX-037: Rate law first order -dA/dt = k A (form 37).

def first_order_rate(k: float, a: float) -> float
  requires k >= 0.0 and a >= 0.0
  ensures result == k * a
  decreases 0
=
  return k * a

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
