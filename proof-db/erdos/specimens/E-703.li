# E-703: Let $r\geq 1$ and define $T(n,r)$ to be maximal such that there exists a family $\mathcal{F}$ of subsets of $\{1,\ldots,n\}$ of size $T(n,r)$ such that $\lvert A\cap B\rvert\neq r$ for all $A,B\in \mathcal{F}$. Estimate $T(n,r)$ for $r\geq 2$. In particular, is it true that for every $\epsilon>0$ there exists $\delta>0$ such that for all $\epsilon n<r<(1/2-\epsilon) n$ we have\[T(n,r)<(2-\delta)^n?\]
# erdos_number: 703
# erdos_status: proved
# priority_tier: P2
# tags: combinatorics

def erdos_e703_let_r_geq_1_and_define_t_n_r_to_be_maxim() -> int
  requires true
  ensures result == 0  # open: E-703
  decreases 0
=
  return 0


def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
