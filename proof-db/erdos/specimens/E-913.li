# E-913: Are there infinitely many $n$ such that if\[n(n+1) = \prod_i p_i^{k_i}\]is the factorisation into distinct primes then all exponents $k_i$ are distinct?
# erdos_number: 913
# erdos_status: open
# priority_tier: P2
# tags: number_theory

def erdos_e913_are_there_infinitely_many_n_such_that_if() -> int
  requires true
  ensures result == 0  # open: E-913
  decreases 0
=
  return 0


def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
