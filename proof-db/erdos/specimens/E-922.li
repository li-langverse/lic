# E-922: Let $k\geq 0$. Let $G$ be a graph such that every subgraph $H$ contains an independent set of size $\geq (n-k)/2$, where $n$ is the number of vertices of $H$. Must $G$ have chromatic number at most $k+2$?
# erdos_number: 922
# erdos_status: proved
# priority_tier: P2
# tags: chromatic_number, graph_theory

def erdos_e922_let_k_geq_0_let_g_be_a_graph_such_that_e() -> int
  requires true
  ensures result == 0  # open: E-922
  decreases 0
=
  return 0


def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
