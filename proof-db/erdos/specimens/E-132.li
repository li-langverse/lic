# E-132: Let $A\subset \mathbb{R}^2$ be a set of $n$ points. Must there be two distances which occur at least once but between at most $n$ pairs of points? Must the number of such distances $\to \infty$ as $n\to \infty$?
# erdos_number: 132
# erdos_status: open
# priority_tier: P2
# tags: distances

def erdos_e132_let_a_subset_mathbb_r_2_be_a_set_of_n_po() -> int
  requires true
  ensures result == 0  # open: E-132
  decreases 0
=
  return 0


def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
