# WP-GPU-05 Partial: @gpu applies to `def` bodies only (extern surface rejected).

@gpu
extern def illegal_gpu_extern() -> int

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
