# E-114: If $p(z)\in\mathbb{C}[z]$ is a monic polynomial of degree $n$ then is the length of the curve $\{ z\in \mathbb{C} : \lvert p(z)\rvert=1\}$ maximised when $p(z)=z^n-1$?
# erdos_number: 114
# erdos_status: open
# priority_tier: P2
# tags: analysis, polynomials

def erdos_e114_if_p_z_in_mathbb_c_z_is_a_monic_polynomi() -> int
  requires true
  ensures result == 0  # open: E-114
  decreases 0
=
  return 0

extern proc erdos_e114_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
