# E-328: Suppose $A\subseteq\mathbb{N}$ and $C>0$ is such that $1_A\ast 1_A(n)\leq C$ for all $n\in\mathbb{N}$. Can $A$ be partitioned into $t$ many subsets $A_1,\ldots,A_t$ (where $t=t(C)$ depends only on $C$) such that $1_{A_i}\ast 1_{A_i}(n)<C$ for all $1\leq i\leq t$ and $n\in \mathbb{N}$?
# erdos_number: 328
# erdos_status: open
# priority_tier: P2
# tags: additive_combinatorics, number_theory

def erdos_e328_suppose_a_subseteq_mathbb_n_and_c_0_is_s() -> int
  requires true
  ensures result == 0  # open: E-328
  decreases 0
=
  return 0

extern proc erdos_e328_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
