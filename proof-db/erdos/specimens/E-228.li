# E-228: Does there exist, for all large $n$, a polynomial $P$ of degree $n$, with coefficients $\pm 1$, such that\[\sqrt{n} \ll \lvert P(z) \rvert \ll \sqrt{n}\]for all $\lvert z\rvert =1$, with the implied constants independent of $z$ and $n$?
# erdos_number: 228
# erdos_status: proved
# priority_tier: P2
# tags: analysis, polynomials

def erdos_e228_does_there_exist_for_all_large_n_a_polyn() -> int
  requires true
  ensures result == 0  # open: E-228
  decreases 0
=
  return 0

extern proc erdos_e228_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
