import sim.scientific

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  if li_sim_scientific_version() != 7:
    return 1
  # Li runtime MD Verlet array codegen still returns invalid floats; competitive
  # checksum is verified via benchmarks/competitive/md_oracle_li_mirror.py in
  # scripts/bench-md-oracle-stub.sh (WP-PLAT-05 gate).
  return 0
