# E-507: Let $\alpha(n)$ be such that every set of $n$ points in the unit disk contains three points which determine a triangle of area at most $\alpha(n)$. Estimate $\alpha(n)$.
# erdos_number: 507
# erdos_status: open
# priority_tier: P2
# tags: geometry

def erdos_e507_let_alpha_n_be_such_that_every_set_of_n() -> int
  requires true
  ensures result == 0  # open: E-507
  decreases 0
=
  return 0

extern proc erdos_e507_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
