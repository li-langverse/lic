# md_longrange_pme

WP2 harness stub: shares the `md_lennard_jones` C oracle (`md_core.c`) until an `md_longrange_pme`-specific kernel lands. Catalog path: `benchmarks/tier2_physics/md_longrange_pme`.
