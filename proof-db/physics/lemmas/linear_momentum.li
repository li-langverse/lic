# P-LM-MOM-001: Linear momentum p = m v for point mass.

def linear_momentum(m: float, v: float) -> float
  requires m > 0.0
  ensures result == m * v
  decreases 0
=
  return m * v

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
