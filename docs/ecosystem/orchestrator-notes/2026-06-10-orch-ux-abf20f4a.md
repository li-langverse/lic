# Orchestrator note — UX gap orchestration (`abf20f4a`)

**Date:** 2026-06-10  
**Agent:** `swarm_observer`  
**Research goal:** `swarm_coverage` (north_star_fit: ecosystem, ai)  
**Dimension:** `ux`  
**Worker:** `abf20f4a`

---

## Executive summary

| Field | Value |
|-------|-------|
| Swarm posture | **Degraded (conditional)** — grade **C** (75.6); `unattended_safe: true` |
| Gap prep | **Blocked** — PyYAML missing; last apply @ 03:54Z |
| Open gaps | **62** (0 `ui_ux` kind; studio UX = `plan_debt`) |
| UX audit posture | **Docs-only** preflight; GUI targets not in routine briefing |
| CP artifacts | **Missing** — no `state.json` / `latest-report.json` |
| Unattended? | **Conditional** — agents run; operators lack honest health pane |

---

## UX reconciliation (easy pillar — operator surfaces)

Swarm orchestration UX = how operators **see** health, gaps, and handoffs. This pass confirms prior UX worker findings (`99a0e56b`) with updated scorecard (C vs D) but **same orchestration blockers**.

### Operator blind spots

1. **No `data/control-plane/state.json` or `latest-report.json`** — retry budgets, interventions, and `swarm_degraded` opaque across Job restarts.
2. **Gap pipeline frozen** — ingest/apply cannot run without PyYAML; 62 gaps stale since 03:54Z.
3. **Preflight UX audits** — `ux-audit.json` / `ui-audit.json` only exercise `lic-docs`. Five GUI targets in [gui-ux-quality-handoff.md](../gui-ux-quality-handoff.md) absent from briefing.

### Studio / GUI gap routing

| Item | Status | Handoff |
|------|--------|---------|
| `orch-r4-ui-ux-signals` | open, apply deferred | Complete via `ui_ux_quality` → `gui_ux_tester` |
| `studio-ux-16`, `studio-ux-17` | skip — plan not mounted | `studio_ui_ux_builder` when `STUDIO_UI_UX_PLAN_PATH` set |
| `gap-ux-audit-agents-dashboard` | skip in last full sweep | `gui_ui_tester` + dev server ([#38](https://github.com/li-langverse/li-cursor-agents/issues/38)) |
| `gap-ux-audit-world-studio-demo` | Partial (HTML mock) | `studio_ui_ux_builder` |

Do **not** spawn new `studio-ui-ux` systemd loops — use research lane + existing agent ids.

---

## Gap orchestration (Mode B)

```bash
# Intended each cycle (BLOCKED this run):
cd /workspace/lic
python3 scripts/swarm-gap-ingest.py
python3 scripts/swarm-gap-apply-actions.py
# → benchmarks/data/latest/swarm-gap-actions.json
```

**Blocked:** `swarm-gap-ingest: PyYAML required` — neither `apt python3-yaml` nor `pip` available in container.

| `gap_kind` | Open | UX-relevant action |
|------------|------|-------------------|
| `plan_debt` | 31 | `orch-r4`, studio-ux-16/17, master-plan Doc-c |
| `competitor_feature` | 30 | sim-md stubs — not operator UX |
| `missing_package` | 1 | `li-line-profiler` — dev UX tooling |

---

## Briefing vs scorecard drift

| Source | Top agents |
|--------|------------|
| Briefing heap | `ci_maintainer`, `security_auditor` |
| Scorecard | + `gap_explorer`, `plan_verifier` |

**Fix:** `benchmarks/scripts/enrich-briefing-scorecards.py` (or briefing builder) should union scorecard recommendations into `recommended_agents`.

---

## Control-plane fixes (orchestration only)

1. Persist CP `state.json` + `latest-report.json` each supervisor tick (`li-cursor-agents/src/observer/`).
2. Bake PyYAML into org-research worker image.
3. Enable proactive five-target GUI sweep in briefing preflight.
4. Map `orch-r4-ui-ux-signals` → existing `ui_ux_quality` goal handoff (no new agent ids).

---

## Evidence paths

- `/workspace/benchmarks/data/latest/ecosystem-quality-report.json`
- `/workspace/benchmarks/data/latest/swarm-gap-actions.json`
- `/workspace/benchmarks/data/latest/ux-audit.json`
- `/workspace/benchmarks/data/latest/ui-audit.json`
- `/workspace/lic/docs/ecosystem/gui-ux-quality-handoff.md`
- `/app/data/runs/swarm_observer-1781085267779.md`

---

## Handoffs (swarm goals — no new agent ids)

| To | Reason |
|----|--------|
| `gui_ux_tester` (`ui_ux_quality`) | Five-target GUI sweep + registry UX rows |
| `studio_ui_ux_builder` | studio-ux-16/17 when worktree mounted |
| `gap_explorer` | Reconcile 62 open gaps after ingest unblocked |
| `issue_planner` | CP persistence + image PyYAML issues |
| `ci_maintainer` | 37 repos missing CI (briefing P0) |
| `plan_verifier` | Refresh plan audit preflight (`--skip-slow` gap) |
