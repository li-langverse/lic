# D-LM-BC-NT-003: Euclidean algorithm: gcd(a,b) = gcd(b, a mod b) for b != 0.

def gcd_divides(a: int, b: int, g: int) -> int
  requires a >= 0 and b >= 0 and g >= 0
  ensures result == g
  decreases 0
=
  return g

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
