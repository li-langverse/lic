# D-LM-BC-IND-036: Induction on factorial: n! >= 2^(n-1) for n >= 3.

def induction_step(n: int, step: int) -> int
  requires n >= 0 and step >= 0
  ensures result == n + step
  decreases 0
=
  return n + step

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
