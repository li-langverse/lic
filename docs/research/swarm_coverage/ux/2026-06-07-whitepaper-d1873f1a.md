# Swarm gap orchestration — UX dimension whitepaper (staging)

**Goal id:** `swarm_coverage`  
**Dimension:** `ux`  
**Worker:** `d1873f1a`  
**Generated:** 2026-06-07T17:58Z  
**Publish target:** `research-findings/whitepapers/2026-06/swarm_coverage/ux/` (deferred — repo not mounted)  
**north_star_fit:** Swarm gap orchestration — registry, backlog apply, handoffs — domains: ecosystem, ai

---

## Abstract

Li’s agent swarm exposes a **control-plane UX debt**: operators cannot reliably observe gap-registry apply status, ecosystem health grade, or briefing-vs-execution alignment from the org supervisor dashboard. This audit (org-research worker `d1873f1a`, ux dimension) maps `ui_ux` and `plan_debt` signals for `orch-r4-ui-ux-signals`, documents operator friction evidence, and routes handoffs without inventing new agent registry ids or lic systemd plan loops.

---

## Method

1. Regenerated ecosystem quality scorecard (`python3 scripts/ecosystem-quality-grade.py` in benchmarks).
2. Read briefing snapshot, gap registry (`registry.yaml`), gap actions (`swarm-gap-actions.json`), UX audits, studio-ui digest.
3. Attempted programmatic prep: `swarm-gap-ingest.py` (syntax remediated); apply blocked by missing PyYAML.
4. Bootstrapped control-plane disk mirrors (`state.json`, `latest-report.json`) for dashboard IPC.
5. Compared `recommended_agents` vs recent org-research audit (`org-research-audit.jsonl`).

---

## Results

### Ecosystem posture (2026-06-07)

| Metric | Value |
|--------|-------|
| Overall score | 65.3 (**D**) |
| Unattended safe | **false** |
| Open gaps | 64 (31 plan_debt, 30 competitor_feature, 3 missing_package) |
| Failed PRs | 6 |
| Repos missing CI | 14 |
| Goal runners stopped | 6 / 9 |

### UX rubric coverage gap

Current automated UX audits (`ux-audit.json`, `ui-audit.json`) evaluate **one target**: `lic-docs` static site (pass). They do **not** cover:

- Org supervisor dashboard (swarm health, merge queue, gap apply)
- Studio / world-studio-demo surfaces (partial coverage via studio loop digest only)
- Agent run history UX (opaque persist errors in prior cycles)

**Implication:** `ui_ux_quality` research goal and `gui_ux_tester` must expand scope to **operator tooling**, not only product docs.

### Operator journey defects

| Journey step | Expected | Observed | UX severity |
|--------------|----------|----------|-------------|
| Check swarm health | Dashboard panel + grade | Missing IPC files pre-bootstrap; grade only in benchmarks JSON | high |
| Trace gap apply | Rows from `swarm-gap-actions.json` | No UI; 64 open gaps with stale timestamp (2026-05-31) | high |
| Align briefing priorities | Heap executes pr_merger first | Research lane occupied by `swarm_observer` | medium |
| Diagnose ingest failure | Actionable script error | PyYAML + prior syntax error | medium |

### Studio-ui linkage (`orch-r4`)

From `studio-ui-ux-builder-digest.md`:

- UX-01 wgpu viewport grid — **open** (competitive gap)
- UX-12 Linux harness — **closed** in loop
- Native SDL / capture deps — **partial** (documented honest fallback)

Registry row `gap-plan-pending-swarm-observer-orch-r4-ui-ux-signals` remains `plan_debt` with apply patch **deferred** until `lic-studio-ui` mount enables backlog patch.

---

## Recommendations

1. **Dashboard:** Add swarm health + gap-apply panel (`li-cursor-agents`, labels `ux`, `dashboard`).
2. **Research:** Enable `gui_ux_tester` pass on org-supervisor-dashboard + gap-orchestration flows.
3. **Infra:** Bake `python3-yaml` in org-research worker; set `LI_CURSOR_AGENTS_ROOT=/app`.
4. **Handoffs:** `studio_ui_ux_builder` for UX-01; `pr_merger` for lip#52 queue clarity.
5. **Evidence refresh:** Re-run gap ingest/apply after PyYAML + ingest fix merge.

---

## Evidence index

| Artifact | Path |
|----------|------|
| Quality scorecard | `/workspace/benchmarks/data/latest/ecosystem-quality-report.json` |
| Gap actions | `/workspace/benchmarks/data/latest/swarm-gap-actions.json` |
| Gap registry | `/workspace/lic/data/swarm-gap-registry/registry.yaml` |
| UX audit | `/workspace/benchmarks/data/latest/ux-audit.json` |
| Studio digest | `/workspace/benchmarks/data/latest/studio-ui-ux-builder-digest.md` |
| Orchestrator note | `/workspace/lic/docs/ecosystem/orchestrator-notes/2026-06-07-swarm-coverage-ux-d1873f1a.md` |
| Control-plane bootstrap | `/app/data/control-plane/state.json`, `latest-report.json` |
| This run report | `/app/data/runs/swarm_observer-1780853619198.md` |

---

## Deferred publish

Copy this file to `research-findings/whitepapers/2026-06/swarm_coverage/ux/2026-06-07-d1873f1a.md` when repo mount is available; sync whitepaper index out of band.
