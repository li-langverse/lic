# E-510: If $A\subset \mathbb{Z}$ is a finite set of size $N$ then is there some absolute constant $c>0$ and $\theta$ such that\[\sum_{n\in A}\cos(n\theta) < -cN^{1/2}?\]
# erdos_number: 510
# erdos_status: open
# priority_tier: P2
# tags: analysis

def erdos_e510_if_a_subset_mathbb_z_is_a_finite_set_of() -> int
  requires true
  ensures result == 0  # open: E-510
  decreases 0
=
  return 0


def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
