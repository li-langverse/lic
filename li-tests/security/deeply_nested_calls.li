def wrap(x: int) -> int
  requires true
  ensures result == x + 1
  decreases 0
=
  return x + 1

def deep() -> int
  requires true
  ensures true
  decreases 0
=
  return wrap(wrap(wrap(wrap(wrap(wrap(wrap(wrap(0))))))))
