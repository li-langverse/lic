# M1 wave 9 — Bearer API key on public listener (dev keys; hash at edge in M1.5)
[server]
listen = ":39220"
document_root = "./public"

[limits]
max_body = "1m"
max_header = "16k"

[auth]
require_bearer = true
keys = ["dev-agent-key", "bench-key-2"]

[routes]
"GET /index.html" = "static"
