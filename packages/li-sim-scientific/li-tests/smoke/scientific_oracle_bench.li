import sim.scientific

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  if li_sim_scientific_version() != 7:
    return 1
  var md_run: SimRunResult = run_md_lj_smoke(0)
  var md_chk: float = md_run.checksum
  if md_chk != sim_scientific_oracle_checksum_md():
    return 2
  if run_result_ok(md_run) != 1:
    return 3
  var heat_run: SimRunResult = run_heat_smoke(0)
  var heat_chk: float = heat_run.checksum
  if heat_chk != sim_scientific_oracle_checksum_heat():
    return 4
  if run_result_ok(heat_run) != 1:
    return 5
  return 0
