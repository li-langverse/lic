# E-1174: Does there exist a graph $G$ with no $K_4$ such that every edge colouring of $G$ with countably many colours contains a monochromatic $K_3$? Does there exist a graph $G$ with no $K_{\aleph_1}$ such that every edge colouring of $G$ with countably many colours contains a monochromatic $K_{\aleph_0}$?
# erdos_number: 1174
# erdos_status: open
# priority_tier: P2
# tags: ramsey_theory, set_theory

def erdos_e1174_does_there_exist_a_graph_g_with_no_k_4_s() -> int
  requires true
  ensures result == 0  # open: E-1174
  decreases 0
=
  return 0

extern proc erdos_e1174_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
