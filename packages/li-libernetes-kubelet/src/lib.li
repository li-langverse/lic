def kubelet_stub() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0

def libernetes_kubelet_multi_node_join_selftest() -> int
  requires true
  ensures result == 0
  decreases 0
=
  if kubelet_sync_tick(0) != 0:
    return 1
  if kubelet_sync_tick(1) != 0:
    return 2
  return 0
