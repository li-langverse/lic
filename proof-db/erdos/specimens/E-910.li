# E-910: Does every connected set in $\mathbb{R}^n$ contain a connected subset which is not a point and not homeomorphic to the original set? If $n\geq 2$ does every connected set in $\mathbb{R}^n$ contain more than $2^{\aleph_0}$ many connected subsets?
# erdos_number: 910
# erdos_status: open
# priority_tier: P2
# tags: topology

def erdos_e910_does_every_connected_set_in_mathbb_r_n_c() -> int
  requires true
  ensures result == 0  # open: E-910
  decreases 0
=
  return 0

extern proc erdos_e910_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
