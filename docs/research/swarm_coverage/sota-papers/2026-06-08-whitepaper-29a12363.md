# Swarm gap orchestration — sota-papers dimension

**Goal:** `swarm_coverage`  
**Dimension:** `sota-papers`  
**Worker:** `29a12363`  
**Date:** 2026-06-08  
**north_star_fit:** ecosystem, ai — evidence-backed gap routing for provable HPC/numerics  
**Publish target:** `research-findings/whitepapers/2026-06/swarm_coverage/sota-papers/`

---

## Abstract

This pass audits swarm gap orchestration through a **state-of-the-art papers lens**: whether competitor-feature gaps in the registry cite peer-reviewed or industry SOTA sources, whether those citations route to the correct async research verticals, and whether gap-ingest keeps paper-linked backlogs current. The swarm is **degraded** (grade D); SOTA routing is **defined in registry** but **not refreshing** because ingest is blocked and the goal-directed snapshot is stale (2026-05-30).

---

## 1. Paper-anchored gaps in registry

**Source:** `/workspace/lic/data/swarm-gap-registry/registry.yaml` (updated 2026-05-31)

| Gap id | Competitor class | Citation | Li gap |
|--------|------------------|----------|--------|
| `gap-hpc-fftw-roofline-catalog-row` | Tier-1 FFT micro-bench | [arxiv 2506.08653](https://arxiv.org/html/2506.08653v1) — GPU FFT roofline (cuFFT/VkFFT/heFFTe) | Missing tier-1 FFT catalog row vs vendor GPU baselines |
| `gap-hpc-raja-execution-policies` | Policy-based loop abstractions | [arxiv 2402.08950](https://arxiv.org/html/2402.08950v1) | RAJA vs Li `std/execution` decorators (PH-7e) |
| `gap-hpc-openmp-llvm-lowering-rubric` | Portable OpenMP lowering | [OSTI 2224192](https://www.osti.gov/servlets/purl/2224192) | Prescriptive vs descriptive lowering rubric for codegen |

**Coverage:** 3 of 30 open `competitor_feature` rows include explicit paper URLs. The remainder reference ecosystem-explorer JSON, GitHub repos, or verticals.toml stubs without bibliographic anchors.

**Implication:** `gap_explorer` should attach SOTA references (arxiv, DOI, or vendor whitepapers) when opening new competitor rows — improves research lane dispatch quality under the `sota-papers` supervisor dimension.

---

## 2. Research vertical mapping

Per `docs/ecosystem/research-verticals.md`, paper-anchored gaps map to enabled goals in `config/research-goals.yaml`:

| Paper theme | Goal id | Agent | Session | Publish path |
|-------------|---------|-------|---------|--------------|
| FFT roofline / microbench | `numerics_sota` | `numerics_researcher` | no | `research-findings/whitepapers/2026-05/numerics_sota/` |
| RAJA / OpenMP / Kokkos-class execution | `scientific_distributed_computing` | `numerics_researcher` | yes | `…/scientific_distributed_computing/` |
| MD/QM sim algorithms (plan_debt) | `md_sim_algorithms`, `chem_sim_algorithms` | `numerics_researcher` | yes | respective publish subdirs |

**Handoff chain (registry → backlog → goal):**

1. `swarm-gap-apply-actions.py` patches runner backlogs (last success 2026-05-31).
2. Research lane (`pickNextGoalForAgent`) rotates by priority/cadence — **one SDK slot**; do not spawn 19 parallel researchers.
3. `swarm_observer` meta-audit verifies routing; does not implement product code in `lic`.

---

## 3. Sim / chem / MD plan_debt with literature expectations

Open plan_debt rows tied to simulation literature surveys (no arxiv in registry yet):

| Runner | Todo | Expected SOTA survey target |
|--------|------|----------------------------|
| `sim-chem-research` | `chem-r2-dft-scf-gap` | DFT/SCF scaling literature (Gaussian/ORCA class) |
| `sim-md-research` | `md-r3-oracle-plan` | MD integrator / neighbor-list oracle benchmarks |
| `sim` | `sim-p2-qm-dft-scf` | QM method parity vs reference codes |

**chem-r0-sota-survey** is **closed** in registry — template for enriching remaining chem/md rows with paper citations on next `gap_explorer` pass.

---

## 4. Gap-ingest integrity (metadata supply chain)

**Finding:** `swarm-gap-ingest.py` cannot run — `PyYAML required`. Registry `updated_at: 2026-05-31`; briefing @ 2026-06-08.

**Risk:** New SOTA papers discovered by `gap_explorer` or org research workers do not propagate to backlogs or `swarm-gap-actions.json`. Unattended operation requires:

- `python3-yaml` in briefing/observer container
- Valid ingest on every briefing cycle
- `benchmarks/competitive/verticals.toml` on main (`gap-infra-verticals-toml-missing-benchmarks-main`)

---

## 5. Recommendations

1. **Unblock ingest** — add PyYAML to CI/container; re-run ingest + apply after briefing.
2. **Enrich uncited gaps** — dispatch `gap_explorer` on `ecosystem_gaps` to add arxiv/DOI evidence to 27 competitor rows.
3. **Route paper-anchored HPC gaps** — `numerics_researcher` on `scientific_distributed_computing` + `numerics_sota`; cite PH-7e/PH-5b on handoffs.
4. **Close historical bench-red registry rows** when live audit confirms 0 tier-1 red (current audit: green).
5. **Do not** disable provability gates or spawn new lic systemd plan loops.

---

## Evidence index

| Artifact | Path |
|----------|------|
| Gap registry | `/workspace/lic/data/swarm-gap-registry/registry.yaml` |
| Gap actions (stale) | `/workspace/benchmarks/data/latest/swarm-gap-actions.json` |
| Research verticals | `/workspace/li-cursor-agents/docs/ecosystem/research-verticals.md` |
| Research goals | `/workspace/li-cursor-agents/config/research-goals.yaml` |
| Ecosystem audit | `/workspace/benchmarks/data/latest/ecosystem-audit.json` |
| Quality scorecard | `/workspace/benchmarks/data/latest/ecosystem-quality-report.json` |
| Observer digest | `/app/data/runs/swarm_observer-1780959496653.md` |
