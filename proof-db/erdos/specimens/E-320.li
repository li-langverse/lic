# E-320: Let $S(N)$ count the number of distinct sums of the form $\sum_{n\in A}\frac{1}{n}$ for $A\subseteq \{1,\ldots,N\}$. Estimate $S(N)$.
# erdos_number: 320
# erdos_status: open
# priority_tier: P2
# tags: number_theory, unit_fractions

def erdos_e320_let_s_n_count_the_number_of_distinct_sum() -> int
  requires true
  ensures result == 0  # open: E-320
  decreases 0
=
  return 0

extern proc erdos_e320_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
