# E-663: Let $k\geq 2$ and $q(n,k)$ denote the least prime which does not divide $\prod_{1\leq i\leq k}(n+i)$. Is it true that, if $k$ is fixed and $n$ is sufficiently large, we have\[q(n,k)<(1+o(1))\log n?\]
# erdos_number: 663
# erdos_status: open
# priority_tier: P2
# tags: number_theory

def erdos_e663_let_k_geq_2_and_q_n_k_denote_the_least_p() -> int
  requires true
  ensures result == 0  # open: E-663
  decreases 0
=
  return 0


def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
