# Proof database entries

TOML entry files for the Li proof database catalog (`proof-db` reporter).

See [proof database index](../README.md) and [proof-database.md](../../proof-database.md) for schema and workflow.

Each `*.toml` in this directory registers lemmas, axioms, or corpus rows consumed by `python3 scripts/proof-db/reporter.py`.
