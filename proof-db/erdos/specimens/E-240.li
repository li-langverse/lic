# E-240: Is there an infinite set of primes $P$ such that if $\{a_1<a_2<\cdots\}$ is the set of integers divisible only by primes in $P$ then $\lim a_{i+1}-a_i=\infty$?
# erdos_number: 240
# erdos_status: proved
# priority_tier: P2
# tags: number_theory, primes

def erdos_e240_is_there_an_infinite_set_of_primes_p_suc() -> int
  requires true
  ensures result == 0  # open: E-240
  decreases 0
=
  return 0

extern proc erdos_e240_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
