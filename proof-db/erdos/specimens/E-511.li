# E-511: Let $f(z)\in \mathbb{C}[z]$ be a monic polynomial of degree $n$. Is it true that, for every $c>1$, the set\[\{ z\in \mathbb{C} : \lvert f(z)\rvert< 1\}\]has at most $O_c(1)$ many connected components of diameter $>c$ (where the implied constant is in particular independent of $n$)?
# erdos_number: 511
# erdos_status: open
# priority_tier: P2
# tags: analysis

def erdos_e511_let_f_z_in_mathbb_c_z_be_a_monic_polynom() -> int
  requires true
  ensures result == 0  # open: E-511
  decreases 0
=
  return 0

extern proc erdos_e511_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
