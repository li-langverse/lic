# E-1048: If $f\in \mathbb{C}[x]$ is a monic polynomial with all roots satisfying $\lvert z\rvert \leq r$ for some $r<2$, then must\[\{ z: \lvert f(z)\rvert <1\}\]have a connected component with diameter $>2-r$?
# erdos_number: 1048
# erdos_status: open
# priority_tier: P2
# tags: analysis

def erdos_e1048_if_f_in_mathbb_c_x_is_a_monic_polynomial() -> int
  requires true
  ensures result == 0  # open: E-1048
  decreases 0
=
  return 0

extern proc erdos_e1048_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
