# Spin-up: World Studio + agent diagnose (PH-AGENT / GD-3).
import studio
import studio.ai

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  if studio_native_shell_smoke() != 0:
    return 1
  return studio_ai_agent_diagnose_smoke()
