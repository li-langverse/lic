# E-231: Let $S$ be a string of length $2^k-1$ formed from an alphabet of $k$ characters. Must $S$ contain an abelian square: two consecutive blocks $x$ and $y$ such that $y$ is a permutation of $x$?
# erdos_number: 231
# erdos_status: open
# priority_tier: P2
# tags: combinatorics

def erdos_e231_let_s_be_a_string_of_length_2_k_1_formed() -> int
  requires true
  ensures result == 0  # open: E-231
  decreases 0
=
  return 0

extern proc erdos_e231_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
