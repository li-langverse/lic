# E-72: Is there a set $A\subset \mathbb{N}$ of density $0$ and a constant $c>0$ such that every graph on sufficiently many vertices with average degree $\geq c$ contains a cycle whose length is in $A$?
# erdos_number: 72
# erdos_status: proved
# priority_tier: P2
# tags: cycles, graph_theory

def erdos_e72_is_there_a_set_a_subset_mathbb_n_of_dens() -> int
  requires true
  ensures result == 0  # open: E-72
  decreases 0
=
  return 0


def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
