# Swarm gap orchestration — security dimension

**Goal:** `swarm_coverage`  
**Dimension:** `security`  
**Worker:** `a71429e5`  
**Agent:** `swarm_observer`  
**Generated:** 2026-06-10T13:10Z  
**north_star_fit:** ecosystem, ai (orchestration); security lens: secure pillar  
**Publish target:** `research-findings/whitepapers/2026-06/swarm_coverage/security/` (staged; repo not mounted)

---

## Abstract

This pass audits whether the Li agent **swarm** can orchestrate security-related gap closure without human intervention. Execution health is good (zero error runs sampled, SDK auth present) but **gap ingest remains blocked** (PyYAML), **CWE catalog coverage is incomplete** (19/25 MITRE Top-25 missing), and **`security_auditor` is recommended but not dispatched**. Security plan_debt rows (`sec-r1`–`sec-r3`) are correctly patched to backlogs and must hand off via the `offensive_security` research goal — not retired systemd loops.

---

## Method

1. Scorecard: `ecosystem-quality-grade.py` → grade C (75.6), `unattended_safe: true` (13:09Z)
2. Briefing: `agent-briefing.json` @ 13:08Z — `security_auditor` + `ci_maintainer` recommended
3. CWE feed: `security-cwe-feed.json` vs `lic/security/cve-catalog.json`
4. Gap registry: 62 open rows; security-research plan_debt subset
5. Dry-run: `swarm-gap-ingest.py` (PyYAML failure; pip unavailable)
6. Control plane: `state.json` / `latest-report.json` absence
7. Run sample: `data/runs/` — 1 running (`swarm_observer`), 0 errors

---

## Findings

### 1. CWE catalog gap (high)

| Metric | Value |
|--------|-------|
| Top-25 baseline | 25 |
| Catalog rows | 15 |
| Missing in catalog | **19** |

Representative missing: CWE-79 (XSS), CWE-89 (SQLi), CWE-352 (CSRF), CWE-798 (hardcoded credentials).

**Implication:** `security_auditor` cannot close the org security scorecard until catalog backfill is human-approved.

### 2. Security plan_debt routing (medium)

Three `security-research` runner todos remain open (`sec-r1`–`sec-r3`). Apply-actions (03:54Z) patched them to `security-research-backlog.md`. Orchestration policy:

- Route via `config/research-goals.yaml` → `offensive_security` → `security_auditor`
- Do **not** spawn `security-research` systemd plan loop

`sec-r0-cwe-delta` is marked completed in backlog; catalog gap persists — reconcile on next `security_auditor` run.

### 3. Briefing vs heap drift (high)

Scorecard recommends `gap_explorer`, `plan_verifier`, `security_auditor`, `ci_maintainer`. Briefing `heap_plan.flat_tasks` only schedules `ci_maintainer`. Security P0 is visible to meta-agents but not enqueued for leaf execution.

### 4. Gap ingest dependency (high)

Without PyYAML, registry cannot refresh from goal-directed snapshots. Security gaps discovered after 03:54Z are invisible to apply-actions. Worker image lacks `pip`/`python3-yaml`.

### 5. Control-plane observability (medium)

`data/control-plane/state.json` and `latest-report.json` are absent. Programmatic observer cannot persist retry counts or surface `swarm_degraded` reliably across K8s Job restarts.

---

## Recommendations

1. **Image:** Add `python3-yaml` to org-research worker Dockerfile.
2. **Dispatch:** Enqueue `offensive_security` goal when `sec-r*` rows are pending.
3. **Catalog:** Human-governed PR to backfill 19 CWE rows in `cve-catalog.json`.
4. **CP:** Persist observer state each supervisor tick.
5. **Briefing:** Merge scorecard `recommended_agents` into `heap_plan` for security + gap_explorer.

---

## Evidence paths

- `/workspace/benchmarks/data/latest/ecosystem-quality-report.json`
- `/workspace/benchmarks/data/latest/security-cwe-feed.json`
- `/workspace/benchmarks/data/latest/swarm-gap-actions.json`
- `/workspace/lic/data/swarm-gap-registry/registry.yaml`
- `/workspace/lic/docs/ecosystem/security-research-backlog.md`
- `/app/data/runs/swarm_observer-1781094608319.md`

---

## Validity

| Criterion | Grade |
|-----------|-------|
| Data freshness | B (briefing 13:08Z; gap actions 03:54Z) |
| Reproducibility | A (scripts documented; PyYAML block is deterministic) |
| Actionability | B (handoffs identified; human gates on catalog PR) |

**Status:** active · **Agent:** `swarm_observer`
