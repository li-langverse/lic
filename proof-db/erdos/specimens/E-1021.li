# E-1021: Is it true that, for every $k\geq 3$, there is a constant $c_k>0$ such that\[\mathrm{ex}(n,G_k) \ll n^{3/2-c_k},\]where $G_k$ is the bipartite graph between $\{y_1,\ldots,y_k\}$ and $\{z_1,\ldots,z_{\binom{k}{2}}\}$, with each $z_j$ joined to a unique pair of $y_i$?
# erdos_number: 1021
# erdos_status: proved
# priority_tier: P2
# tags: graph_theory

def erdos_e1021_is_it_true_that_for_every_k_geq_3_there() -> int
  requires true
  ensures result == 0  # open: E-1021
  decreases 0
=
  return 0

extern proc erdos_e1021_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
