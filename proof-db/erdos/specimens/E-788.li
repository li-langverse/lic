# E-788: Let $f(n)$ be maximal such that if $B\subset (2n,4n)\cap \mathbb{N}$ there exists some $C\subset (n,2n)\cap \mathbb{N}$ such that $c_1+c_2\not\in B$ for all $c_1\neq c_2\in C$ and $\lvert C\rvert+\lvert B\rvert \geq f(n)$. Estimate $f(n)$. In particular is it true that $f(n)\leq n^{1/2+o(1)}$?
# erdos_number: 788
# erdos_status: open
# priority_tier: P2
# tags: additive_combinatorics

def erdos_e788_let_f_n_be_maximal_such_that_if_b_subset() -> int
  requires true
  ensures result == 0  # open: E-788
  decreases 0
=
  return 0

extern proc erdos_e788_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
