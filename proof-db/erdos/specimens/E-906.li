# E-906: Is there an entire non-zero function $f:\mathbb{C}\to \mathbb{C}$ such that, for any infinite sequence $n_1<n_2<\cdots$, the set\[\{ z: f^{(n_k)}(z)=0 \textrm{ for some }k\geq 1\}\]is everywhere dense?
# erdos_number: 906
# erdos_status: open
# priority_tier: P2
# tags: analysis, iterated_functions

def erdos_e906_is_there_an_entire_non_zero_function_f_m() -> int
  requires true
  ensures result == 0  # open: E-906
  decreases 0
=
  return 0

extern proc erdos_e906_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
