# E-945: Let $F(x)$ be the maximal $k$ such that there exist $n+1,\ldots,n+k\leq x$ with $\tau(n+1),\ldots,\tau(n+k)$ all distinct (where $\tau(m)$ counts the divisors of $m$). Estimate $F(x)$. In particular, is it true that\[F(x) \leq (\log x)^{O(1)}?\]In other words, is there a constant $C>0$ such that, for all large $x$, every interval $[x,x+(\log x)^C]$ contains two integers with the same number of divisors?
# erdos_number: 945
# erdos_status: open
# priority_tier: P2
# tags: divisors, number_theory

def erdos_e945_let_f_x_be_the_maximal_k_such_that_there() -> int
  requires true
  ensures result == 0  # open: E-945
  decreases 0
=
  return 0

extern proc erdos_e945_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
