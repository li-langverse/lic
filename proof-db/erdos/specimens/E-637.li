# E-637: If $G$ is a graph on $n$ vertices which contains no complete graph or independent set on $\gg \log n$ vertices then $G$ contains an induced subgraph on $\gg n$ vertices which contains $\gg n^{1/2}$ distinct degrees.
# erdos_number: 637
# erdos_status: proved
# priority_tier: P2
# tags: graph_theory, ramsey_theory

def erdos_e637_if_g_is_a_graph_on_n_vertices_which_cont() -> int
  requires true
  ensures result == 0  # open: E-637
  decreases 0
=
  return 0


def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
