import array

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  var da: ArrayDesc = array_desc_2d(2, 4, 4, array_storage_flat())
  var db: ArrayDesc = array_desc_2d(2, 4, 4, array_storage_flat())
  if array_broadcast_compatible(da, db) != 0:
    return 1
  var d1: ArrayDesc = array_desc_1d(1, array_storage_flat())
  var d4: ArrayDesc = array_desc_1d(4, array_storage_flat())
  if array_broadcast_compatible(d1, d4) != 1:
    return 2
  var ok: ArrayDesc = array_desc_2d(1, 4, 4, array_storage_flat())
  var full: ArrayDesc = array_desc_2d(3, 4, 4, array_storage_flat())
  if array_broadcast_compatible(ok, full) != 1:
    return 3
  if li_array_broadcast_compatible(d1, d4) != 1:
    return 4
  return 0
