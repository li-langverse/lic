# `@` on int arrays must fail at typecheck (v1 requires float elem for dot/GEMM).

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  var a: array[4, int]
  var b: array[4, int]
  var s: int = a @ b
  return 0
