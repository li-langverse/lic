# E-838: Let $f(n)$ be maximal such that any $n$ points in $\mathbb{R}^2$, with no three on a line, determine at least $f(n)$ different convex subsets. Estimate $f(n)$ - in particular, does there exist a constant $c$ such that\[\lim \frac{\log f(n)}{(\log n)^2}=c?\]
# erdos_number: 838
# erdos_status: open
# priority_tier: P2
# tags: convex, geometry

def erdos_e838_let_f_n_be_maximal_such_that_any_n_point() -> int
  requires true
  ensures result == 0  # open: E-838
  decreases 0
=
  return 0

extern proc erdos_e838_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
