# Swarm gap orchestration — performance dimension

**Goal:** `swarm_coverage`  
**Dimension:** `performance`  
**Worker:** `b768df04`  
**Date:** 2026-06-08  
**Publish target:** `research-findings/whitepapers/2026-06/swarm_coverage/performance/`

---

## Abstract

Meta-audit of the Li agent swarm under the **performance** lens for gap orchestration. The control plane is **degraded but operable**: ecosystem quality grade **C** (74.1), `unattended_safe: false`. Numerics posture is strong (145 green, 0 red) with **2 yellow** and **5 near-threshold** workloads. The primary orchestration blocker is the gap ingest/apply pipeline (syntax error remediated; PyYAML missing in org-research worker image).

**north_star_fit:** ecosystem, ai — PH-7e (proof-before-perf)

---

## 1. Ecosystem quality scorecard

| Metric | Value | Evidence |
|--------|-------|----------|
| Overall score | 74.1 | `ecosystem-quality-report.json` |
| Grade | C | same |
| Unattended safe | false | same |
| Gap pressure | 60 (64 open) | `swarm-gap-actions.json` |
| Swarm execution | 100 (1 run sampled) | `LI_CURSOR_AGENTS_ROOT=/app` |

Regenerated: `2026-06-08T15:40:25Z` with `runs_dir=/app/data/runs`.

---

## 2. Benchmark performance posture

From `ecosystem-audit.json` benchmarks embed:

- **Green:** 145 workloads
- **Red:** 0
- **Yellow:** `num_eig_symmetric`, `num_root_newton`
- **Near-threshold (1.18–1.20× C++):** BFGS, semi-implicit Euler, Euler, RK4, CG

**Interpretation:** Performance debt is concentrated in eigen/root solvers (yellow) and integration/optimization kernels approaching parity. Per pillar order, closures require proved codegen paths (Phase 7e) before SIMD matmul or unsafe shortcuts.

---

## 3. Gap orchestration state

| Pipeline step | Status |
|---------------|--------|
| `registry.yaml` | Present; 64 open gaps |
| `swarm-gap-ingest.py` | Syntax fixed (line 229 Path fallback) |
| `swarm-gap-apply-actions.py` | Blocked — `ModuleNotFoundError: yaml` |
| Backlog patches | Stale since 2026-05-31 |

Performance-relevant registry rows:

- `gap-competitor-pure-li-ph7e-catalog` → `numerics_researcher`, `bench_improver`
- `gap-hpc-kokkos-*`, `gap-hpc-petsc-*` → research lane (no new systemd loops)
- Phase 7e `plan_debt` master-plan partials → `plan_verifier`

---

## 4. Swarm health vs briefing alignment

**Briefing recommended (P0):** `pr_merger`, `ci_maintainer`, `security_auditor`  
**Scorecard recommended:** `gap_explorer`, `ci_maintainer`, `plan_verifier`, `security_auditor`, `pr_merger`

**Drift:** Low — merge and CI maintain top heap priority; gap_explorer should follow once ingest/apply is green.

**Prior errors (24h):** `swarm_observer` security dimension SDK flake (`tools=0`, 33.9s); UX dimension incomplete — not performance-critical.

---

## 5. Recommendations

1. Merge **lic#ingest-fix** (this pass) to unblock `verticals.toml` stub ingest.
2. Bake **python3-yaml** in org-research worker image (`li-cursor-agents` deploy).
3. Set **`LI_CURSOR_AGENTS_ROOT=/app`** on grader + observer preflight.
4. Dispatch **`bench_improver`** + **`numerics_researcher`** for yellow/near-threshold rows.
5. Human-gated: lic sim-scientific PR stack (#1397–#1409) — pick one PR per merge-plan warnings.

---

## References

- `docs/ecosystem/research-verticals.md` — `swarm_coverage` meta goal
- `docs/ecosystem/orchestrator-notes/2026-06-08-orch-r20-performance-gap-orchestration.md`
- Master plan Phase 7e: `docs/superpowers/plans/2026-05-14-li-master-plan.md`
