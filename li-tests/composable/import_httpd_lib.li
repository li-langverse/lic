# Composable gate: import package lib API from another module (not exec-only binary).
import li_httpd

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  httpd_stop(httpd_serve(9090))
  return httpd_tag()
