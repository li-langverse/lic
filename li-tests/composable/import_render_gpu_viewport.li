import render
import gpu

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  if li_render_version() < 3:
    return 1
  if gpu_select_backend_stub(gpu_backend_rocm()) != gpu_backend_rocm():
    return 2
  if render_gpu_viewport_smoke() != 0:
    return 3
  return gpu_surrogate_smoke()
