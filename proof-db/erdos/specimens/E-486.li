# E-486: Let $A\subseteq \mathbb{N}$, and for each $n\in A$ choose some $X_n\subseteq \mathbb{Z}/n\mathbb{Z}$. Let\[B = \{ m\in \mathbb{N} : m\not\in X_n\pmod{n}\textrm{ for all }n\in A\textrm{ with }m>n\}.\]Must $B$ have a logarithmic density, i.e. is it true that\[\lim_{x\to \infty} \frac{1}{\log x}\sum_{\substack{m\in B\\ m<x}}\frac{1}{m}\]exists?
# erdos_number: 486
# erdos_status: open
# priority_tier: P2
# tags: number_theory, primitive_sets

def erdos_e486_let_a_subseteq_mathbb_n_and_for_each_n_i() -> int
  requires true
  ensures result == 0  # open: E-486
  decreases 0
=
  return 0

extern proc erdos_e486_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
