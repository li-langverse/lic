# E-881: Let $A\subset\mathbb{N}$ be an additive basis of order $k$ which is minimal, in the sense that if $B\subset A$ is any infinite set then $A\backslash B$ is not a basis of order $k$. Must there exist an infinite $B\subset A$ such that $A\backslash B$ is a basis of order $k+1$?
# erdos_number: 881
# erdos_status: open
# priority_tier: P2
# tags: additive_basis, number_theory

def erdos_e881_let_a_subset_mathbb_n_be_an_additive_bas() -> int
  requires true
  ensures result == 0  # open: E-881
  decreases 0
=
  return 0

extern proc erdos_e881_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
