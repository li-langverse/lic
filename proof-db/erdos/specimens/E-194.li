# E-194: Let $k\geq 3$. Must any ordering of $\mathbb{R}$ contain a monotone $k$-term arithmetic progression, that is, some $x_1<\cdots<x_k$ which forms an increasing or decreasing $k$-term arithmetic progression?
# erdos_number: 194
# erdos_status: open
# priority_tier: P2
# tags: arithmetic_progressions

def erdos_e194_let_k_geq_3_must_any_ordering_of_mathbb() -> int
  requires true
  ensures result == 0  # open: E-194
  decreases 0
=
  return 0

extern proc erdos_e194_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
