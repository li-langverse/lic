# E-308: Let $N\geq 1$. What is the smallest integer not representable as the sum of distinct unit fractions with denominators from $\{1,\ldots,N\}$? Is it true that the set of integers representable as such has the shape $\{1,\ldots,m\}$ for some $m$?
# erdos_number: 308
# erdos_status: proved
# priority_tier: P2
# tags: number_theory, unit_fractions

def erdos_e308_let_n_geq_1_what_is_the_smallest_integer() -> int
  requires true
  ensures result == 0  # open: E-308
  decreases 0
=
  return 0

extern proc erdos_e308_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
