# Swarm gap orchestration — security dimension

**Goal:** `swarm_coverage`  
**Dimension:** `security`  
**Worker:** `df5206c2`  
**Agent:** `swarm_observer`  
**Generated:** 2026-06-10T08:24Z  
**north_star_fit:** ecosystem, ai (orchestration); security lens: secure pillar  
**Publish target:** `research-findings/whitepapers/2026-06/swarm_coverage/security/` (staged; repo not mounted)

---

## Abstract

This pass audits whether the Li agent **swarm** can orchestrate security-related gap closure without human intervention. We find execution health is good (zero error runs sampled) but **gap ingest is blocked** (PyYAML), **CWE catalog coverage is incomplete** (19/25 MITRE Top-25 missing), and **security leaf agents are not dispatched** despite briefing P0. Security plan_debt rows (`sec-r0`–`sec-r3`) are correctly patched to backlogs but require handoff to `security_auditor` via the `offensive_security` research goal — not retired systemd loops.

---

## Method

1. Scorecard: `ecosystem-quality-grade.py` → grade C (75.6), `unattended_safe: true`
2. Briefing: `agent-briefing.json` @ 08:22Z — `security_auditor` recommended
3. CWE feed: `security-cwe-feed.json` vs `lic/security/cve-catalog.json`
4. Gap registry: 62 open rows; security-research plan_debt subset
5. Dry-run: `swarm-gap-ingest.py` / `swarm-gap-apply-actions.py` (PyYAML failure)
6. Control plane: `state.json` / `latest-report.json` absence

---

## Findings

### 1. CWE catalog gap (high)

| Metric | Value |
|--------|-------|
| Top-25 baseline | 25 |
| Catalog rows | 15 |
| Missing in catalog | **19** |

Representative missing: CWE-79 (XSS), CWE-89 (SQLi), CWE-352 (CSRF), CWE-798 (hardcoded credentials).

**Implication:** `security_auditor` cannot close the org security scorecard until catalog backfill is human-approved.

### 2. Security plan_debt routing (medium)

Four `security-research` runner todos remain open in the registry. Apply-actions (03:54Z) patched three to `security-research-backlog.md`. Orchestration policy:

- Route via `config/research-goals.yaml` → `offensive_security` → `security_auditor`
- Do **not** spawn `security-research` systemd plan loop

### 3. Tier-DB security bench (low)

`tier-db-security.json` status **stub** — `injection_blocked` and `rls_bypass_blocked` have no measured pass_rate. Blocks evidence for PH-DB-SEC / WP-N4.

### 4. Gap ingest dependency (high)

Without PyYAML, registry cannot refresh from goal-directed snapshots. Security gaps discovered after 03:54Z are invisible to apply-actions.

### 5. Control-plane observability (medium)

Missing CP disk mirrors prevent auditing `observer.retry_counts` and `stopped_agents` across Job pods — a **meta-security** concern for unattended operation.

---

## Recommendations

1. **Image:** add `python3-yaml` to org-research worker image
2. **Dispatch:** enqueue `security_auditor` for CWE delta + `sec-r1` httpd fuzz
3. **Governance:** human PR for `cve-catalog.json` Top-25 backfill
4. **Bench:** implement tier-db-security scenarios before claiming DB security parity
5. **CP:** persist observer state each supervisor tick

---

## Evidence paths

| Artifact | Path |
|----------|------|
| Scorecard | `/workspace/benchmarks/data/latest/ecosystem-quality-report.json` |
| CWE feed | `/workspace/benchmarks/data/latest/security-cwe-feed.json` |
| Gap actions | `/workspace/benchmarks/data/latest/swarm-gap-actions.json` |
| Registry | `/workspace/lic/data/swarm-gap-registry/registry.yaml` |
| Observer run | `/app/data/runs/swarm_observer-1781076394562.md` |
| Tier-DB security | `/workspace/benchmarks/data/latest/tier-db-security.json` |

---

## Validity

| Criterion | Grade |
|-----------|-------|
| Data freshness | B — briefing 08:22Z; gap actions 03:54Z |
| Reproducibility | A — scripts and paths cited |
| Actionability | B — blocked on PyYAML + human CWE governance |

**Status:** active (staged for `research-findings` publish when repo mounted)
