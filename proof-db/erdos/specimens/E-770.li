# E-770: Let $h(n)$ be minimal such that $2^n-1,3^n-1,\ldots,h(n)^n-1$ are mutually coprime. Does, for every prime $p$, the density $\delta_p$ of integers with $h(n)=p$ exist? Does $\liminf h(n)=\infty$? Is it true that if $p$ is the greatest prime such that $p-1\mid n$ and $p>n^\epsilon$ then $h(n)=p$?
# erdos_number: 770
# erdos_status: open
# priority_tier: P2
# tags: number_theory

def erdos_e770_let_h_n_be_minimal_such_that_2_n_1_3_n_1() -> int
  requires true
  ensures result == 0  # open: E-770
  decreases 0
=
  return 0

extern proc erdos_e770_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
