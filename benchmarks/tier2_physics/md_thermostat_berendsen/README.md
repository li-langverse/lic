# md_thermostat_berendsen

WP2 harness stub: shares the `md_lennard_jones` C oracle (`md_core.c`) until an `md_thermostat_berendsen`-specific kernel lands. Catalog path: `benchmarks/tier2_physics/md_thermostat_berendsen`.
