# Swarm gap orchestration — security dimension

**Goal:** `swarm_coverage`  
**Worker:** `d8314b70`  
**Date:** 2026-06-07  
**Publish target:** `research-findings/whitepapers/2026-06/swarm_coverage/security/`  
**north_star_fit:** ecosystem, ai — secure multi-agent control plane; proof → easy → fast

---

## Abstract

This pass audits Li agent **swarm** health through a **security lens**: CWE catalog coverage, security-research plan debt in the gap registry, and orchestration failures that block unattended offensive-security work. The programmatic gap pipeline was restored; three `sec-r*` todos are patched to `security-research-backlog.md` and ready for `security_auditor` dispatch via the `offensive_security` research goal.

---

## Security posture signals

| Signal | Value | Evidence |
|--------|-------|----------|
| CWE Top25 in catalog | 6 / 25 | `benchmarks/data/latest/security-cwe-feed.json` |
| Missing from catalog | **19** | Same; human-gated backfill |
| Security workflows on org repos | 0 missing (audit) | `agent-briefing.json` recommended_agents |
| Open security plan_debt gaps | 3 | `swarm-gap-registry` sec-r1/2/3 |
| httpd exploit tier5 rows | partial (runner snapshot stale 2026-05-30) | `lic/data/goal-directed-agents/snapshot.json` |

**Missing CWE examples (not in `lic/security/cve-catalog.json`):** CWE-79, CWE-89, CWE-20, CWE-22, CWE-352, CWE-434, CWE-862, CWE-476, CWE-287, CWE-502, CWE-77, CWE-119, CWE-798, CWE-918, CWE-306, CWE-269, CWE-94, CWE-863, CWE-276.

---

## Gap orchestration (Mode B)

### Registry reconciliation

After ingest + apply (2026-06-07T00:17:28Z):

- **Open gaps:** 62 (down from 64 stale snapshot; 2 std module gaps closed in registry).
- **Security plan_debt:** all three rows patched to pending in `docs/ecosystem/security-research-backlog.md`.

### Routing (no new agent ids)

| Todo | Agent / goal | Action |
|------|--------------|--------|
| `sec-r1-httpd-fuzz-smoke` | `security_auditor` / `offensive_security` | Fuzz smoke vs li-httpd; cite tier5 nginx_mitigations |
| `sec-r2-tier5-gap-exploit` | `security_auditor` | Close empty exploit rows in nginx_mitigations.toml |
| `sec-r3-runtime-surface` | `security_auditor` | Runtime attack surface survey (lic build, httpd) |

### Orchestration blockers (security impact)

1. **`swarm-gap-ingest.py` env fallback** — fixed this pass; merge to `lic/main` to stop repeated observer failures.
2. **PyYAML not in worker image** — ad-hoc `apt install`; bake `python3-yaml` in org-research Job.
3. **Control-plane observer artifacts missing** — no `retry_counts` / auto-heal visibility when Supabase store offline.
4. **`security_cwe_audit` preflight skipped** (`--skip-slow`) — briefing recommends `security_auditor` but full audit not run.

---

## Self-heal vs human-only

| Class | Items |
|-------|-------|
| Programmatic (after fix) | Gap ingest/apply; scorecard refresh; backlog patch for sec-r* |
| Agent dispatch | `security_auditor`, `ci_maintainer`, `pr_merger` |
| Human-only | CWE catalog JSON edits; lip#52 merge approval already set; governance PR stacks |

---

## Recommendations

1. Enable `security_cwe_audit` in default briefing preflight when `security_auditor` is P0.
2. Map 19 CWE Top25 rows into `cve-catalog.json` via human-reviewed PR (no auto-merge).
3. Dispatch `security_auditor` on `sec-r1-httpd-fuzz-smoke` before bench/perf work (proof-before-perf).
4. Persist control-plane `latest-report.json` / `state.json` on each supervisor tick (offline fallback).

---

## Evidence paths

- `/workspace/benchmarks/data/latest/ecosystem-quality-report.json`
- `/workspace/benchmarks/data/latest/agent-briefing.json`
- `/workspace/benchmarks/data/latest/security-cwe-feed.json`
- `/workspace/benchmarks/data/latest/swarm-gap-actions.json`
- `/workspace/lic/data/swarm-gap-registry/registry.yaml`
- `/workspace/lic/docs/ecosystem/security-research-backlog.md`
- `/workspace/lic/docs/ecosystem/orchestrator-notes/2026-06-07-orch-r8-security-d8314b70.md`
- `/app/data/runs/swarm_observer-1780789203473.md`
