# E-531: Let $F(k)$ be the minimal $N$ such that if we two-colour $\{1,\ldots,N\}$ there is a set $A$ of size $k$ such that all subset sums $\sum_{a\in S}a$ (for $\emptyset\neq S\subseteq A$) are monochromatic. Estimate $F(k)$.
# erdos_number: 531
# erdos_status: open
# priority_tier: P2
# tags: number_theory, ramsey_theory

def erdos_e531_let_f_k_be_the_minimal_n_such_that_if_we() -> int
  requires true
  ensures result == 0  # open: E-531
  decreases 0
=
  return 0


def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
