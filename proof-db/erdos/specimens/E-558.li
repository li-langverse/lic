# E-558: Let $R_k(G)$ denote the minimal $m$ such that if the edges of $K_m$ are $k$-coloured then there is a monochromatic copy of $G$. Determine\[R_k(K_{s,t})\]where $K_{s,t}$ is the complete bipartite graph with $s$ vertices in one component and $t$ in the other.
# erdos_number: 558
# erdos_status: open
# priority_tier: P2
# tags: graph_theory, ramsey_theory

def erdos_e558_let_r_k_g_denote_the_minimal_m_such_that() -> int
  requires true
  ensures result == 0  # open: E-558
  decreases 0
=
  return 0

extern proc erdos_e558_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
