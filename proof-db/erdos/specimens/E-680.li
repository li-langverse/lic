# E-680: Is it true that, for all sufficiently large $n$, there exists some $k$ such that\[p(n+k)>k^2+1,\]where $p(m)$ denotes the least prime factor of $m$? Can one prove this is false if we replace $k^2+1$ by $e^{(1+\epsilon)\sqrt{k}}+C_\epsilon$, for all $\epsilon>0$, where $C_\epsilon>0$ is some constant?
# erdos_number: 680
# erdos_status: open
# priority_tier: P2
# tags: number_theory, primes

def erdos_e680_is_it_true_that_for_all_sufficiently_lar() -> int
  requires true
  ensures result == 0  # open: E-680
  decreases 0
=
  return 0

extern proc erdos_e680_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
