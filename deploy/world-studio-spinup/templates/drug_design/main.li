# Spin-up: drug design LITL (PH-DRUG).
import studio
import sim.drug_design
import chem

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  if lab_loop_full_smoke() != 0:
    return 1
  return chem_litl_dft_smoke(8)
