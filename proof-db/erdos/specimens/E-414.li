# E-414: Let $h_1(n)=h(n)=n+\tau(n)$ (where $\tau(n)$ counts the number of divisors of $n$) and $h_k(n)=h(h_{k-1}(n))$. Is it true, for any $m,n$, there exist $i$ and $j$ such that $h_i(m)=h_j(n)$?
# erdos_number: 414
# erdos_status: open
# priority_tier: P2
# tags: iterated_functions, number_theory

def erdos_e414_let_h_1_n_h_n_n_tau_n_where_tau_n_counts() -> int
  requires true
  ensures result == 0  # open: E-414
  decreases 0
=
  return 0

extern proc erdos_e414_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
