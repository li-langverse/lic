# E-942: Let $h(n)$ count the number of powerful (if $p\mid m$ then $p^2\mid m$) integers in $[n^2,(n+1)^2)$. Estimate $h(n)$. In particular is there some constant $c>0$ such that\[h(n) < (\log n)^{c+o(1)}\]and, for infinitely many $n$,\[h(n) >(\log n)^{c-o(1)}?\]
# erdos_number: 942
# erdos_status: open
# priority_tier: P2
# tags: number_theory, powerful

def erdos_e942_let_h_n_count_the_number_of_powerful_if() -> int
  requires true
  ensures result == 0  # open: E-942
  decreases 0
=
  return 0

extern proc erdos_e942_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
