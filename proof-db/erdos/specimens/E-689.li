# E-689: Let $n$ be sufficiently large. Is there some choice of congruence class $a_p$ for all primes $2\leq p\leq n$ such that every integer in $[1,n]$ satisfies at least two of the congruences $\equiv a_p\pmod{p}$?
# erdos_number: 689
# erdos_status: open
# priority_tier: P2
# tags: number_theory

def erdos_e689_let_n_be_sufficiently_large_is_there_som() -> int
  requires true
  ensures result == 0  # open: E-689
  decreases 0
=
  return 0

extern proc erdos_e689_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
