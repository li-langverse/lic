# E-805: For which functions $g(n)$ with $n>g(n)\geq (\log n)^2$ is there a graph on $n$ vertices in which every induced subgraph on $g(n)$ vertices contains a clique of size $\geq \log n$ and an independent set of size $\geq \log n$? In particular, is there such a graph for $g(n)=(\log n)^3$?
# erdos_number: 805
# erdos_status: open
# priority_tier: P2
# tags: graph_theory

def erdos_e805_for_which_functions_g_n_with_n_g_n_geq_l() -> int
  requires true
  ensures result == 0  # open: E-805
  decreases 0
=
  return 0


def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
