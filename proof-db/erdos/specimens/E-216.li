# E-216: Let $g(k)$ be the smallest integer (if any such exists) such that any $g(k)$ points in $\mathbb{R}^2$ contains an empty convex $k$-gon (i.e. with no point in the interior). Does $g(k)$ exist? If so, estimate $g(k)$.
# erdos_number: 216
# erdos_status: open
# priority_tier: P2
# tags: convex, geometry

def erdos_e216_let_g_k_be_the_smallest_integer_if_any_s() -> int
  requires true
  ensures result == 0  # open: E-216
  decreases 0
=
  return 0


def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
