# E-688: Define $\epsilon_n$ to be maximal such that there exists some choice of congruence class $a_p$ for all primes $n^{\epsilon_n}<p\leq n$ such that every integer in $[1,n]$ satisfies at least one of the congruences $\equiv a_p\pmod{p}$. Estimate $\epsilon_n$ - in particular is it true that $\epsilon_n=o(1)$?
# erdos_number: 688
# erdos_status: open
# priority_tier: P2
# tags: number_theory

def erdos_e688_define_epsilon_n_to_be_maximal_such_that() -> int
  requires true
  ensures result == 0  # open: E-688
  decreases 0
=
  return 0


def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
