# Swarm gap orchestration — UX dimension whitepaper

**Goal:** `swarm_coverage`  
**Dimension:** `ux`  
**Worker:** `0e217961`  
**Generated:** 2026-06-08T12:45Z  
**north_star_fit:** ecosystem, ai — operator and product UX signals in the swarm control plane  
**Publish target:** `research-findings/whitepapers/2026-06/swarm_coverage/ux/`

## Abstract

This pass audits how **UX quality** intersects with swarm gap orchestration: studio-ui plan debt, operator dashboard observability, and docs-site UX baselines. The swarm remains **unattended_safe** at grade C (75.1) but **gap apply is blocked** on infrastructure (PyYAML + stale actions artifact). UX-specific registry rows show snapshot drift; routing to `gui_ux_tester` and `ui_ux_quality` is recommended over new systemd loops.

## UX audit signals

### Docs (passing)

`ux-audit.json` and `ui-audit.json` (2026-05-30/31) report **lic-docs** static site pass: nav clarity 0.85, zero axe violations, 242 HTML files. No friction points recorded.

### Studio UI (partial)

`studio-ui-ux-builder-digest.md` documents PH-UX gates green on simulate hooks; gaps:

- UX-01 wgpu viewport grid
- studio-ux-16 palette latency bench
- studio-ux-17 GPU fail recovery (mock-only UX-08)
- Native SDL capture (`libsdl2` missing on host)

### Operator dashboard (gap)

`org-supervisor-dashboard` lacks:

- Swarm health summary (`swarm_health`, `interventions`)
- Gap-apply status panel (`swarm-gap-actions.json` freshness)
- Research lane dimension cursor (`org-research-active.json`)

This is the primary **ui_ux** control-plane debt for unattended operation.

## Registry reconciliation (ux lens)

| Gap id | Kind | UX relevance | Action |
|--------|------|--------------|--------|
| `orch-r4-ui-ux-signals` | plan_debt | Meta — this pass | Close after snapshot refresh + dashboard panel spec |
| `studio-ux-16-palette-search-latency` | plan_debt | Product UX perf | `gui_ux_tester` → bench gate |
| `studio-ux-17-gpu-fail-recovery` | plan_debt | Error UX | `gui_ux_tester` → native strip |
| `orch-r3-missing-package-sweep` | plan_debt | Low UX impact | `issue_planner` |

No `ui_ux` `gap_kind` rows in current `by_kind` breakdown — studio rows are classified `plan_debt` in registry.

## Scorecard impact

Regenerated `ecosystem-quality-report.json` (2026-06-08T12:37:52Z):

- **overall_score:** 75.1 (C)
- **gap_pressure:** 60 (64 open gaps)
- **swarm_execution:** 100 (1 run sampled — this observer; prior 25% error rate stale)
- **unattended_safe:** true

## Recommendations

1. Bake `python3-yaml` + `LI_CURSOR_AGENTS_ROOT=/app` in org-research Job image.
2. Add dashboard panels for swarm health and gap-apply (li-cursor-agents).
3. Dispatch `gui_ux_tester` on `ui_ux_quality` for studio-ux-16/17 + dashboard audit.
4. Refresh goal-directed snapshot to close stale studio-ui registry rows.
5. Merge lip#52; route `ci_maintainer` for 12 repos missing CI.

## References

- `docs/ecosystem/research-verticals.md` — `ui_ux_quality`, `swarm_coverage` goals
- `docs/ecosystem/orchestrator-notes/2026-06-08-orch-r4-ui-ux-signals.md`
- `/app/data/runs/swarm_observer-1780919657792.md`
