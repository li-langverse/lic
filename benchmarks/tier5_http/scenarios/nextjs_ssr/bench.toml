name = "nextjs_ssr"
phase = "m1"
enabled = true

[server]
kind = "proxy"
backend = "nextjs_toy"
backend_port = 19002

[verify]
requests = [
  { method = "GET", path = "/ssr/page", expect_status = 200, body_contains = "toy-ssr" },
]

[load]
tool = "wrk"
threads = 4
connections = 20
duration_sec = 8

[metrics]
collect = ["rps", "p50_latency_ms", "p99_latency_ms", "ttfb_ms"]

[parity]
# Loopback proxy overhead vs nginx oracle (~0.80×); wrk -c20 (see proxy_loopback variant).
rps_ratio_min = 0.80
ttfb_ratio_min = 0.85
p99_ratio_max = 2.0
