# Compiler audit reports (Phase 5+)

Agents **must not** edit `compiler/` or discharge-pipeline C++. File findings here for human verification.

Expected per bug: `BUG-C-NN.md` with reproduction, classification, and recommended fix (report-only).
