import llm

def main() -> int raises IO, Alloc
  requires true
  ensures result == 0
  decreases 0
=
  if li_llm_version() < 9:
    return 1
  var progress: int = llm_kv_device_buffer_progress()
  if progress < 0:
    return 2
  if progress > 1:
    return 3
  return 0
