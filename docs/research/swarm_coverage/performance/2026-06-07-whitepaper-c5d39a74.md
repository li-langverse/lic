# Swarm gap orchestration — performance dimension

**Goal id:** `swarm_coverage`  
**Dimension:** `performance`  
**Worker:** `c5d39a74`  
**Agent:** `swarm_observer`  
**Generated:** 2026-06-07T03:08Z  
**north_star_fit:** ecosystem, ai — domains: ecosystem, ai; PH-7e (SIMD/parallel), PH-5b (numerics/HPC)

---

## Abstract

This pass audits swarm gap orchestration through a **performance lens**: benchmark posture, performance-related registry gaps, httpd/sim plan debt, and control-plane bottlenecks that delay perf work from reaching `bench_improver` / `numerics_researcher`. Tier-1 benchmarks show **no red rows** but **regression pressure** on integrators and optimizers near the 1.2× vs C++ gate. Gap ingest/apply was **blocked** by a SyntaxError and missing PyYAML — fixed this run; 62 open gaps remain.

---

## 1. Benchmark posture (evidence)

Source: `/workspace/benchmarks/data/latest/ecosystem-audit.json` (generated 2026-06-01)

| Signal | Count | IDs |
|--------|------:|-----|
| Red (tier-1) | 0 | — |
| Yellow | 2 | `num_eig_symmetric`, `num_root_newton` |
| Green | 145 | — |
| Near threshold (~1.18–1.20×) | 5 | `num_opt_bfgs`, `num_integ_semi_implicit`, `num_integ_euler`, `num_integ_rk4`, `num_cg` |

**Interpretation:** Li numerics are broadly competitive (145 green). Performance orchestration should prioritize **near-threshold** rows before opening new competitor_feature codegen — consistent with proof-before-perf pillar order.

---

## 2. Performance gaps in registry (62 open)

Source: `/workspace/lic/data/swarm-gap-registry/registry.yaml`, apply: `/workspace/benchmarks/data/latest/swarm-gap-actions.json`

### 2.1 Competitor feature (performance-critical subset)

| Gap id | Title | PH | Handoff |
|--------|-------|-----|---------|
| `gap-benchmark-red-matmul-naive-tier1` | matmul_naive 1.73× vs cpp | PH-7e | bench_improver |
| `gap-benchmark-red-num-gmres-tier1` | num_gmres 1.68× vs cpp | PH-5b | numerics_researcher |
| `gap-benchmark-red-num-opt-line-search-tier1` | line_search 2.00× vs cpp | PH-5b | numerics_researcher |
| `gap-hpc-kokkos-execution-memory-spaces` | Kokkos execution model | PH-7e | numerics_researcher |
| `gap-competitor-pure-li-ph7e-catalog` | pure_li catalog variants | PH-7e | bench_improver |

### 2.2 Plan debt (runtime perf gates)

| Runner | Pending todo | Perf gate |
|--------|--------------|-----------|
| httpd | `gap-phase2-perf-wrk-soak` | wrk soak ≥30s vs nginx |
| httpd | `gap-phase2-streaming-wrk` | SSE/WS wrk with timing |
| sim | `sim-p1-num-dot-axpy` | BLAS-1 kernel parity |
| sim | `sim-p1-md-neighbor-cell` | MD neighbor list perf |
| sim | `sim-p2-qm-dft-scf` | QM DFT SCF (algo 418) |

Backlog patches applied to `sim-algorithm-backlog.md` this run.

### 2.3 Missing package (profiling)

| Gap id | Package | Role |
|--------|---------|------|
| `gap-line-profiler-001` | li-line-profiler | HPC agent loop line profiling |

---

## 3. Swarm orchestration performance

| Bottleneck | Impact | Remediation |
|------------|--------|-------------|
| `swarm-gap-ingest.py:229` SyntaxError | Zero vertical stub re-ingest | Fixed Path fallback (lic PR) |
| Missing PyYAML | Apply script no-op | Bake `python3-yaml` in worker image |
| `runs_sampled=0` | Grader blind to worker runs | Set `LI_RUNS_DIR=/app/data/runs` in k8s worker |
| Stale snapshot (2026-05-30) | False plan_debt rows | Refresh `goal-directed-agents-snapshot.py` |
| 189 redundant PR pairs | Merge queue latency | `pr_branch_hygiene` preflight (skipped) |

---

## 4. Recommendations

1. **Dispatch order:** `pr_merger` (lip#52) → `ci_maintainer` → `bench_improver` (near_threshold sweep) → `numerics_researcher` (competitor_feature perf rows).
2. **Research goals:** Route httpd perf todos via `server_platform`; sim todos via `md_sim_algorithms` / `chem_sim_algorithms` — not new systemd loops.
3. **Control plane:** Persist observer state each supervisor tick; align runs_dir for container workers.
4. **Proof gate:** Defer PH-7e SIMD matmul lowering until master-plan 2e–2f partials close — track via `plan_verifier`.

---

## 5. Evidence index

| Artifact | Path |
|----------|------|
| Ecosystem quality scorecard | `/workspace/benchmarks/data/latest/ecosystem-quality-report.json` |
| Agent briefing | `/workspace/benchmarks/data/latest/agent-briefing.json` |
| Gap registry | `/workspace/lic/data/swarm-gap-registry/registry.yaml` |
| Gap apply log | `/workspace/benchmarks/data/latest/swarm-gap-actions.json` |
| Goal-directed snapshot | `/workspace/lic/data/goal-directed-agents/snapshot.json` |
| Swarm observer report | `/app/data/runs/swarm_observer-1780800006467.md` |
| Orchestrator note | `/workspace/lic/docs/ecosystem/orchestrator-notes/2026-06-07-orch-r5-performance-c5d39a74.md` |

---

## Publish target

When `research-findings` is mounted:

`research-findings/whitepapers/2026-06/swarm_coverage/performance/2026-06-07-whitepaper-c5d39a74.md`
