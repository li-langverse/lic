# E-793: Let $F(n)$ be the maximum possible size of a subset $A\subseteq\{1,\ldots,n\}$ such that $a\nmid bc$ whenever $a,b,c\in A$ with $a\neq b$ and $a\neq c$. Is there a constant $C$ such that\[F(n)=\pi(n)+(C+o(1))n^{2/3}(\log n)^{-2}?\]
# erdos_number: 793
# erdos_status: open
# priority_tier: P2
# tags: number_theory

def erdos_e793_let_f_n_be_the_maximum_possible_size_of() -> int
  requires true
  ensures result == 0  # open: E-793
  decreases 0
=
  return 0

extern proc erdos_e793_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
