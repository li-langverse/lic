# Swarm gap orchestration — security dimension

**Goal id:** `swarm_coverage`  
**Dimension:** security  
**Worker:** `3a064c05`  
**Generated:** 2026-06-07T06:34Z  
**Publish target:** `research-findings/whitepapers/2026-06/swarm_coverage/security/`  
**north_star_fit:** ecosystem, ai — secure pillar; proof-before-perf on runtime/httpd surfaces

## Abstract

This pass audits Li swarm health through a **security lens**: CWE catalog coverage, security-research plan debt in the gap registry, and control-plane blockers that prevent automated gap reconciliation. The swarm is **degraded** (grade D, 69.6) primarily due to ecosystem posture (failed PRs, missing CI) and a **64-gap backlog** whose ingest/apply pipeline is broken in the org-research worker (PyYAML + recurring ingest syntax defect).

## Security findings

### 1. CWE catalog gap (critical for unattended operation)

| Signal | Value | Evidence |
|--------|-------|----------|
| Top25 in feed | 25 | `security-cwe-feed.json` |
| In `lic/security/cve-catalog.json` | 15 CWE ids | same |
| **Missing from catalog** | **19** | `top25_missing_in_catalog` |

Missing includes CWE-79 (XSS), CWE-89 (SQLi), CWE-20 (input validation), CWE-352 (CSRF), CWE-798 (hardcoded credentials). Briefing correctly prioritizes `security_auditor`; catalog changes remain **human-gated** (no auto-merge to `trusted.lean` or governance paths).

### 2. Security plan_debt gaps (registry)

Three open rows on runner `security-research`:

- **sec-r1-httpd-fuzz-smoke** — fuzz/smoke against li-httpd tier5 scenarios
- **sec-r2-tier5-gap-exploit** — close exploit rows with empty nginx mitigation
- **sec-r3-runtime-surface** — lic runtime + httpd attack surface inventory

Related: **ph-db wp-n5-security-bench** (database security bench; separate runner).

Handoff target: existing `offensive_security` goal (`security_auditor`, priority 9) — no new agent registry ids.

### 3. httpd security posture (snapshot)

Goal-directed snapshot shows httpd runner **completed** tier5 exploit suite rows (`gap-exploit-owasp-cwe-suite`, `gap-phase2-exploit-nginx-regression`). Pending perf soak rows (`gap-phase2-perf-wrk-soak`, `gap-phase2-streaming-wrk`) are **performance**, not security blockers.

### 4. Control-plane security of swarm itself

| Risk | Severity | Mitigation |
|------|----------|------------|
| Gap ingest SyntaxError (L229) | high | Fixed locally; merge lic PR |
| PyYAML absent in worker | high | Bake `python3-yaml` in org-research image |
| CP state/report not persisted on tick | medium | Observer bootstrap each run; fix supervisor persist |
| `org_agent_kit_audit` fails (roadmap missing) | low | Mount roadmap sibling or skip in research worker |
| 22 failed PR CI (incl. metrics churn) | medium | Consolidate observer PR stack; `ci_maintainer` |

## Gap taxonomy (security-relevant)

| gap_kind | count (open) | security role |
|----------|--------------|---------------|
| plan_debt | 31 | sec-r1..r3, wp-n5-security-bench, httpd exploit rows |
| competitor_feature | 30 | indirect (HPC/web parity) |
| missing_package | 3 | no direct security blockers |

## Recommendations

1. Merge consolidated lic PR for `swarm-gap-ingest.py` Path fallback.
2. Bake PyYAML + `LI_CURSOR_AGENTS_ROOT=/app` in org-research worker entrypoint.
3. Dispatch `security_auditor` on `offensive_security` goal for CWE backfill proposal (human review).
4. After ingest unblocked: re-run ingest → apply → refresh `swarm-gap-actions.json`.
5. Refresh goal-directed snapshot; close `orch-r3-missing-package-sweep` / `orch-r4-ui-ux-signals` when verified.

## References

- `benchmarks/data/latest/security-cwe-feed.json`
- `lic/data/swarm-gap-registry/registry.yaml`
- `lic/data/goal-directed-agents/snapshot.json`
- `docs/ecosystem/research-verticals.md` — `offensive_security` goal
