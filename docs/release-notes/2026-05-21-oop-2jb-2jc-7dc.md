# Phase 2j-b/c + 7d-c AST disjoint policy

## Summary

Adds **private `def`** (import merge strips non-public procs), **object mutation via return-assign** for `rigid_integrate_semi_implicit` and method sugar, and moves **parallel disjoint** enforcement to **`check_module_policies`** (AST) with a narrow source fallback for false-disjoint exploits.

## Agent continuation

1. **Read** `docs/superpowers/plans/2026-05-20-li-oop-roadmap.md` § **2j-c** (true in-out MIR write-back still open).
2. **Run** `LI_REPO_ROOT=$PWD cmake --build build && LI_REPO_ROOT=$PWD ./li-tests/run_all.sh`.
3. **Then** merge **#134** (2j-a) + this branch; implement callee-slot write-back without return type change.
4. **Blocked on** httpd packages — other agents own **#84** / **#87**.

## Changed

| Path | What |
|------|------|
| `compiler/ast.hpp`, `parser.cpp`, `import_resolve.cpp` | `ProcDecl.visibility`; `private def` / `public def` |
| `packages/li-physics-rigid`, `li-physics-runtime` | `rigid_integrate_semi_implicit` → `RigidBody` + `return b` |
| `li-tests/composable/import_physics_runtime.li` | assign return; **verify_ok**; runtime exit 0 |
| `li-tests/encapsulation/private_method_*.li`, `object_method_mutate.li` | 2j-b/c tests |
| `compiler/types/policy.cpp`, `policy_module.cpp` | AST-first disjoint; false-proof string guard |
| `li-tests/race_shared_memory/overlap_par_slice.li` | AST policy compile_fail |
| Master plan + OOP roadmap trackers | 2j in progress |

## Not changed

- `packages/li-http/**`, `li_rt_httpd.c` serve paths.
- **2j-d–f** traits/inheritance; **2i** matrix `@` lowering.
- True **`var` in-out** across `CallProc` without return (LLVM slot alias).

## Breaking

`rigid_integrate_semi_implicit` return type **`unit` → `RigidBody`** — callers must assign (`body = integrate(...)`).

## Security

N/A — stronger private API surface on import.

## Performance

N/A

## Downstream

- Physics packages using integrate must assign return value.
