# E-852: Let $d_n=p_{n+1}-p_n$, where $p_n$ is the $n$th prime. Let $h(x)$ be maximal such that for some $n<x$ the numbers $d_n,d_{n+1},\ldots,d_{n+h(x)-1}$ are all distinct. Estimate $h(x)$. In particular, is it true that\[h(x) >(\log x)^c\]for some constant $c>0$, and\[h(x)=o(\log x)?\]
# erdos_number: 852
# erdos_status: open
# priority_tier: P2
# tags: number_theory, primes

def erdos_e852_let_d_n_p_n_1_p_n_where_p_n_is_the_n_th() -> int
  requires true
  ensures result == 0  # open: E-852
  decreases 0
=
  return 0


def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
