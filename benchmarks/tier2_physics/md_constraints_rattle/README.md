# md_constraints_rattle

WP2 harness stub: shares the `md_lennard_jones` C oracle (`md_core.c`) until an `md_constraints_rattle`-specific kernel lands. Catalog path: `benchmarks/tier2_physics/md_constraints_rattle`.
