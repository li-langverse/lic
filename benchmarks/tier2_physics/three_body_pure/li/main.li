# three_body_pure — shared C oracle (mini three-body; CI-scale steps in core).

extern proc li_three_body_kernel()
  requires true
  ensures true

def main() raises IO -> int
  requires true
  ensures result == 0
  decreases 0
=
  li_three_body_kernel()
  return 0
