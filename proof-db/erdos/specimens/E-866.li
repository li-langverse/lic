# E-866: Let $k\geq 3$ and $g_k(N)$ be minimal such that if $A\subseteq \{1,\ldots,2N\}$ has $\lvert A\rvert \geq N+g_k(N)$ then there exist integers $b_1,\ldots,b_k$ such that all $\binom{k}{2}$ pairwise sums are in $A$ (but the $b_i$ themselves need not be in $A$). Estimate $g_k(N)$.
# erdos_number: 866
# erdos_status: open
# priority_tier: P2
# tags: additive_combinatorics, number_theory

def erdos_e866_let_k_geq_3_and_g_k_n_be_minimal_such_th() -> int
  requires true
  ensures result == 0  # open: E-866
  decreases 0
=
  return 0

extern proc erdos_e866_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
