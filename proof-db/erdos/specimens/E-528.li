# E-528: Let $f(n,k)$ count the number of self-avoiding walks of $n$ steps (beginning at the origin) in $\mathbb{Z}^k$ (i.e. those walks which do not intersect themselves). Determine\[C_k=\lim_{n\to\infty}f(n,k)^{1/n}.\]
# erdos_number: 528
# erdos_status: open
# priority_tier: P2
# tags: geometry

def erdos_e528_let_f_n_k_count_the_number_of_self_avoid() -> int
  requires true
  ensures result == 0  # open: E-528
  decreases 0
=
  return 0


def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
