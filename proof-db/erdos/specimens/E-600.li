# E-600: Let $e(n,r)$ be minimal such that every graph on $n$ vertices with at least $e(n,r)$ edges, each edge contained in at least one triangle, must have an edge contained in at least $r$ triangles. Let $r\geq 2$. Is it true that\[e(n,r+1)-e(n,r)\to \infty\]as $n\to \infty$? Is it true that\[\frac{e(n,r+1)}{e(n,r)}\to 1\]as $n\to \infty$?
# erdos_number: 600
# erdos_status: open
# priority_tier: P2
# tags: graph_theory

def erdos_e600_let_e_n_r_be_minimal_such_that_every_gra() -> int
  requires true
  ensures result == 0  # open: E-600
  decreases 0
=
  return 0


def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
