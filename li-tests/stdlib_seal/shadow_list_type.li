# EXPLOIT: user type shadows builtin list.
# MUST NOT compile — expected: stdlib_symbol_shadow: list

type list = int

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
