import lig.present

def main() -> int raises IO
  requires true
  ensures result == 0
  decreases 0
=
  if lig_kernel_present_wgpu_readback() != 4:
    return 1
  if lig_present_wgpu_readback_active() != 1:
    return 0
  return 0
