# Swarm gap orchestration — api-coverage dimension

**Goal id:** `swarm_coverage`  
**Dimension:** `api-coverage`  
**Worker:** `69b99054`  
**Generated:** 2026-06-08T04:37Z  
**Agent:** `swarm_observer`  
**north_star_fit:** Swarm gap orchestration — registry, backlog apply, handoffs — domains: ecosystem, ai

## Abstract

This pass audits **API and script surfaces** that connect the swarm gap registry to automated backlog apply and control-plane observation. The pipeline is **degraded**: ingest fails on a Python syntax error, apply requires PyYAML, disk control-plane mirrors are absent, and the ecosystem grader samples zero runs due to a path mismatch between org-research workers (`/app`) and the grader default (`/workspace/li-cursor-agents`).

## Method

1. Read `benchmarks/data/latest/ecosystem-quality-report.json` (regenerated 2026-06-08).
2. Read `benchmarks/data/latest/swarm-gap-actions.json` and `lic/data/swarm-gap-registry/registry.yaml`.
3. Execute `lic/scripts/swarm-gap-ingest.py` and `swarm-gap-apply-actions.py`.
4. Compare briefing `recommended_agents` vs org-research dispatch (`org-research-active.json`).
5. Inspect control-plane artifact presence under `/app/data/control-plane/`.

## Findings

### Ecosystem quality (refreshed)

| Metric | Value |
|--------|-------|
| `overall_score` | 65.3 |
| `grade` | D |
| `unattended_safe` | **false** |
| `gap_pressure` | 60 (64 open gaps) |
| `swarm_execution` | 65 (`runs_sampled=0`) |
| `ecosystem_posture` | 63 (6 failed PRs, 12 repos missing CI) |

Evidence: `/workspace/benchmarks/data/latest/ecosystem-quality-report.json`

### Gap pipeline api-coverage

| Script / artifact | Result |
|-------------------|--------|
| `swarm-gap-ingest.py` | **SyntaxError** at line 229 (Path fallback) — fixed in branch |
| `swarm-gap-apply-actions.py` | **Exit 1** — `PyYAML required` |
| `swarm-gap-actions.json` | Stale snapshot from 2026-05-31; 64 open gaps |

### Control-plane read API gaps

- No `state.json` or `latest-report.json` → programmatic observer in `src/observer/` cannot report self-heal actions.
- Grader `inputs.runs_dir` points to empty sibling path; org-research runs land in `/app/data/runs/`.
- Recommended MCP additions (prior passes): `read_ecosystem_quality_report`, `read_swarm_gap_registry`.

### Product API signal (lis)

Failed PR **lis#41** (*feat(registry): agent-first capabilities, validate, MCP stub*) blocks agent-first registry API coverage. Related: lis#40, lis#42 CI failures.

## Recommendations

1. **P0:** Ship ingest syntax fix + bake PyYAML in org-research image.
2. **P0:** Set `LI_CURSOR_AGENTS_ROOT=/app` (or symlink runs dir) so grader/observer sample org-research runs.
3. **P1:** Supervisor tick materializes `data/control-plane/{state,latest-report}.json`.
4. **P1:** MCP tools for registry YAML and quality scorecard reads.
5. **P2:** After ingest unblocked, close stale `orch-r3`/`orch-r4` registry rows via snapshot refresh.

## Evidence paths

- `/workspace/benchmarks/data/latest/ecosystem-quality-report.json`
- `/workspace/benchmarks/data/latest/swarm-gap-actions.json`
- `/workspace/lic/data/swarm-gap-registry/registry.yaml`
- `/workspace/benchmarks/data/latest/agent-briefing.json`
- `/app/data/runs/swarm_observer-1780891748252.md`
- `/workspace/lic/docs/ecosystem/orchestrator-notes/2026-06-08-orch-r8-api-coverage-gaps.md`

## Validity

| Field | Value |
|-------|-------|
| `validity_grade` | C — infrastructure blockers reproduced; gap counts from stale apply snapshot |
| `status` | active |
