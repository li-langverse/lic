# E-1184: Let $f(n,k)$ count the number of $1\leq i\leq k$ such that $P(n+i)>k$ (where $P(m)$ is the largest prime divisor of $m$). Is it true that, if $\alpha>1$ is such that $n=k^{\alpha+o(1)}$, then\[f(n,k)=(1-\rho(\alpha)+o(1))k,\]where $\rho$ is the Dickman function ?
# erdos_number: 1184
# erdos_status: open
# priority_tier: P2
# tags: number_theory, primes

def erdos_e1184_let_f_n_k_count_the_number_of_1_leq_i_le() -> int
  requires true
  ensures result == 0  # open: E-1184
  decreases 0
=
  return 0

extern proc erdos_e1184_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
