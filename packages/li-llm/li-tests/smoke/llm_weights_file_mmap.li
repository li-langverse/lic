import llm

def main() -> int raises IO, Alloc
  requires true
  ensures result == 0
  decreases 0
=
  var st: str = "benchmarks/fixtures/ph-ml-weights/model.safetensors"
  var gg: str = "benchmarks/fixtures/ph-ml-weights/model.gguf"
  if llm_weights_file_mmap(st) <= 0:
    return 1
  if llm_weights_file_mmap(gg) <= 0:
    return 2
  return 0
