# E-885: For integer $n\geq 1$ we define the factor difference set of $n$ by\[D(n) = \{\lvert a-b\rvert : n=ab\}.\]Is it true that, for every $k\geq 1$, there exist integers $N_1<\cdots<N_k$ such that\[\lvert \cap_i D(N_i)\rvert \geq k?\]
# erdos_number: 885
# erdos_status: open
# priority_tier: P2
# tags: divisors, number_theory

def erdos_e885_for_integer_n_geq_1_we_define_the_factor() -> int
  requires true
  ensures result == 0  # open: E-885
  decreases 0
=
  return 0


def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
