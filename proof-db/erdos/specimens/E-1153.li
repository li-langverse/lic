# Catalog: E-1153
# Statement: For $x_1,\ldots,x_n\in [-1,1]$ let\[l_k(x)=\frac{\prod_{i\neq k}(x-x_i)}{\prod_{i\neq k}(x_k-x_i)},\]which are such that $l_k(x_k)=1$ and $l_k(x_i)=0$ for $i\neq k$. Let\[\lambda(x)=\sum_k \lvert l_k(x)\rvert.\]Is it true that, for any fixe
# formalization_status: li_open
# discharge: pending — open formalization target (Phase 4)
# erdos_status (literature): proved

def formal_target_e_1153() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
