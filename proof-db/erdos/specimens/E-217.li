# E-217: For which $n$ are there $n$ points in $\mathbb{R}^2$, no three on a line and no four on a circle, which determine $n-1$ distinct distances and so that (in some ordering of the distances) the $i$th distance occurs $i$ times?
# erdos_number: 217
# erdos_status: open
# priority_tier: P2
# tags: distances, geometry

def erdos_e217_for_which_n_are_there_n_points_in_mathbb() -> int
  requires true
  ensures result == 0  # open: E-217
  decreases 0
=
  return 0

extern proc erdos_e217_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
