# E-1082: Let $A\subset \mathbb{R}^2$ be a set of $n$ points with no three on a line. Does $A$ determine at least $\lfloor n/2\rfloor$ distinct distances? In fact, must there exist a single point from which there are at least $\lfloor n/2\rfloor$ distinct distances?
# erdos_number: 1082
# erdos_status: open
# priority_tier: P2
# tags: distances, geometry

def erdos_e1082_let_a_subset_mathbb_r_2_be_a_set_of_n_po() -> int
  requires true
  ensures result == 0  # open: E-1082
  decreases 0
=
  return 0

extern proc erdos_e1082_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
