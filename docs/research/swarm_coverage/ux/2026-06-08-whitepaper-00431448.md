# Swarm gap orchestration — UX dimension

**Goal:** `swarm_coverage`  
**Dimension:** `ux`  
**Worker:** `00431448`  
**Date:** 2026-06-08  
**north_star_fit:** ecosystem, ai — swarm self-healing must surface UX debt without human triage

---

## Abstract

This pass audits UI/UX signals relevant to swarm gap orchestration: whether UX gaps are discoverable, classified, and routed through the async research lane (`ui_ux_quality` → `gui_ux_tester`) rather than retired systemd plan loops. The studio-ui-ux plan loop is **complete** (24 todos), but the swarm-gap registry still lists studio-ui rows as open `plan_debt` because the goal-directed snapshot is stale (2026-05-30). Docs UX passes automated rubrics; the benchmark dashboard has 12 P1 `chart_pending` rows. Swarm posture is **degraded** (grade D, 65.3) and not unattended-safe until control-plane artifacts persist and gap apply runs with PyYAML.

---

## UX audit matrix

| Surface | Class | Status | Key metric | Evidence |
|---------|-------|--------|------------|----------|
| Li docs site | docs | pass | nav_clarity 0.85 | `benchmarks/data/latest/ux-audit.json` |
| Li docs a11y | docs | pass | 0 axe violations | `benchmarks/data/latest/ui-audit.json` |
| Benchmark dashboard | internal tooling | P1 debt | 12 chart_pending | `benchmarks/data/latest/dashboard-gap-report.json` |
| Studio / world UI | native GUI | loop complete | 24/24 todos done | `lic/data/studio-ui-ux-plan-loop/state.json` |
| Swarm dashboard | control plane | gap | no `state.json` / `latest-report.json` | `/app/data/control-plane/` |
| Gap registry UX taxonomy | orchestration | gap | 0 `ui_ux` rows | `lic/data/swarm-gap-registry/registry.yaml` |

---

## Findings

### 1. Registry–loop drift (high)

Studio-ui-ux completed `studio-ux-16` (palette search latency) and `studio-ux-17` (GPU fail recovery) on 2026-05-30, but registry rows like `gap-plan-pending-studio-ui-ux-studio-ux-16-palette-search-latency` remain `open`. Root cause: snapshot `generated_at: 2026-05-30T09:15:50Z` not refreshed; ingest reads stale `plan_pending`.

**Orchestration fix:** Refresh `lic/data/goal-directed-agents/snapshot.json`; re-run ingest; close completed studio-ui rows; promote remaining UX work to `gap_kind: ui_ux`.

### 2. `orch-r4-ui-ux-signals` still open (medium)

Swarm-observer plan todo `orch-r4` maps UX signal wiring. This note completes the orchestration deliverable; registry row should close on next ingest.

### 3. Dashboard chart UX debt (medium)

12 P1 `chart_pending` bases (httpd, stdlib benches) degrade operator UX when reviewing ecosystem health. Route to `gui_ux_tester` + `docs_maintainer` via briefing, not lic product commits from observer.

### 4. Control-plane operator UX (high)

Missing `/app/data/control-plane/state.json` and `latest-report.json` means the org supervisor dashboard cannot show swarm health, retry budgets, or interventions. Operators lack unattended visibility.

---

## Recommendations

1. **Close studio-ui registry rows** after snapshot refresh (evidence: `studio-ui-ux-plan-loop/state.json`).
2. **Handoff `gui_ux_tester`** under `ui_ux_quality` for benchmark dashboard chart completion and post-studio proactive sweep.
3. **Bootstrap control-plane disk artifacts** each supervisor tick (`li-cursor-agents` PR).
4. **Bake PyYAML** in org-research worker image so `swarm-gap-apply-actions.py` can patch backlogs.
5. **Set `LI_CURSOR_AGENTS_ROOT=/app`** so ecosystem grader samples runs from `/app/data/runs`.

---

## Related goals

| `goal_id` | Agent | Role |
|-----------|-------|------|
| `swarm_coverage` | `swarm_observer` | Meta audit (this document) |
| `ui_ux_quality` | `gui_ux_tester` | Ongoing UX sweeps |
| `ecosystem_gaps` | `gap_explorer` | `ui_ux` taxonomy promotion |

---

## References

- `docs/ecosystem/research-verticals.md` — non-vertical goals table
- `lic/docs/ecosystem/orchestrator-notes/2026-06-08-orch-r4-ui-ux-signals.md`
- `/app/data/runs/swarm_observer-1780944860699.md` — full observer digest
