# Catalog: E-999
# Statement: For any function $f:\mathbb{N}\to \mathbb{N}$ the property that, for almost all $\alpha$\[\left\lvert \alpha-\frac{p}{q}\right\rvert < \frac{f(q)}{q}\]has infinitely many solutions with $(p,q)=1$, is equivalent to\[\sum_{q\geq 1}\phi(q)\fra
# formalization_status: li_open
# discharge: pending — open formalization target (Phase 4)
# erdos_status (literature): proved

def formal_target_e_999() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
