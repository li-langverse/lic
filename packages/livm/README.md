# livm

Multi-OS VM runtime for libernetes via the **Li-native hypervisor** (`li-hypervisor` / LiOS ABI).

KVM/QEMU/libvirt are **not** production backends. Wave 1–2 scaffolds may still name `kvm.li` — legacy stubs only.
