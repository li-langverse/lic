# E-1004: Let $c>0$. If $x$ is sufficiently large then does there exist $n\leq x$ such that the values of $\phi(n+k)$ are all distinct for $1\leq k\leq (\log x)^c$, where $\phi$ is the Euler totient function?
# erdos_number: 1004
# erdos_status: open
# priority_tier: P2
# tags: number_theory

def erdos_e1004_let_c_0_if_x_is_sufficiently_large_then() -> int
  requires true
  ensures result == 0  # open: E-1004
  decreases 0
=
  return 0

extern proc erdos_e1004_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
