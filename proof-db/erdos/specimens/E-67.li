# E-67: If $f:\mathbb{N}\to \{-1,+1\}$ then is it true that for every $C>0$ there exist $d,m\geq 1$ such that\[\left\lvert \sum_{1\leq k\leq m}f(kd)\right\rvert > C?\]
# erdos_number: 67
# erdos_status: proved
# priority_tier: P2
# tags: discrepancy

def erdos_e67_if_f_mathbb_n_to_1_1_then_is_it_true_tha() -> int
  requires true
  ensures result == 0  # open: E-67
  decreases 0
=
  return 0

extern proc erdos_e67_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
