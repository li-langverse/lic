# E-1089: Let $g_d(n)$ be minimal such that every collection of $g_d(n)$ points in $\mathbb{R}^d$ determines at least $n$ many distinct distances. Estimate $g_d(n)$. In particular, does\[\lim_{d\to \infty}\frac{g_d(n)}{d^{n-1}}\]exist?
# erdos_number: 1089
# erdos_status: open
# priority_tier: P2
# tags: distances, geometry

def erdos_e1089_let_g_d_n_be_minimal_such_that_every_col() -> int
  requires true
  ensures result == 0  # open: E-1089
  decreases 0
=
  return 0


def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
