# Swarm gap orchestration — api-coverage dimension

**Goal:** `swarm_coverage`  
**Dimension:** `api-coverage`  
**Agent:** `swarm_observer`  
**Worker:** `e31d7662`  
**Run:** `1780848226802`  
**Generated:** 2026-06-07T16:20Z  
**north_star_fit:** ecosystem, ai — orchestration must not bypass proof gates  
**Publish target:** `research-findings/whitepapers/2026-06/swarm_coverage/api-coverage/`

---

## Abstract

This whitepaper audits the **API and script surface** that supports Li swarm gap orchestration: registry ingest, backlog apply, briefing preflight, and MCP tool coverage. The swarm is **degraded but recoverable** (ecosystem grade **D**, 66.8). Two infrastructure defects block unattended gap refresh: a **syntax error** in `swarm-gap-ingest.py` (fixed locally) and a **missing PyYAML** dependency in the org-research Job runtime. GitHub API rate limiting further degrades briefing health. Handoff routing in the gap registry remains complete — all 64 open gaps specify `handoff_to` agents.

---

## Method

1. Regenerated `ecosystem-quality-report.json` (was 7 days stale).
2. Read `registry.yaml` (92 rows) and `swarm-gap-actions.json` (last apply 2026-05-31).
3. Attempted programmatic ingest/apply per Mode B checklist.
4. Compared briefing `recommended_agents` vs local `data/runs/`.
5. Reviewed `org-swarm-stability-audit.jsonl` and preflight stdout tails.

---

## api-coverage findings

### 1. Gap ingest script API

| Endpoint / entry | Expected | Observed |
|------------------|----------|----------|
| `python3 scripts/swarm-gap-ingest.py` | Update registry from snapshot + explorer | SyntaxError @ line 229 (unterminated string) |
| Post-fix ingest | YAML read/write | `PyYAML required` — module absent |
| `swarm-gap-apply-actions.py` | Patch backlogs | Not executed (ingest gate) |

**Impact:** Registry drifts from goal-directed snapshot; 64 open gaps stale since 2026-05-31.

### 2. Briefing preflight APIs

| Script | Exit | api-coverage issue |
|--------|------|-------------------|
| `ensure-org-repo-ci.py` | 1 | GitHub REST 403 rate limit — cannot enumerate CI status |
| `ensure-org-agent-kit.py` | 1 | Local path `roadmap/agent-kit` missing |
| `ecosystem-audit.py` | 0 | OK |
| `workspace-dirty-sweep.py` | 0 | OK |

### 3. MCP / control-plane read APIs

| Resource | Available in Job | Gap |
|----------|------------------|-----|
| `agent-briefing.json` | Yes (mounted benchmarks) | — |
| `ecosystem-quality-report.json` | Yes (regenerated) | — |
| `swarm-gap-registry/registry.yaml` | Yes (LIC_ROOT) | No MCP tool wrapper for meta agents |
| `data/control-plane/latest-report.json` | **No** | Observer state not mirrored to Job PVC |
| `research-findings` whitepapers | **No** | Publish step deferred |

### 4. Handoff agent registry coverage

100% of open gaps include `handoff_to`. Distribution:

- `swarm_observer`: 31 (plan_debt runner todos)
- `numerics_researcher`: 30+ (competitor features, vertical stubs)
- `plan_verifier`: 9 (master plan partials)
- `issue_planner`: all `missing_package` + plan rows
- `bench_improver`: tier-1 red benchmark gaps

No new agent registry ids required.

---

## Recommendations (api-coverage)

1. **Ship ingest fix + PyYAML** — lic#867 class PR; add dependency to org-research container.
2. **MCP tools** — `read_swarm_gap_registry`, `read_ecosystem_quality_report` for `swarm_observer` / `gap_explorer`.
3. **Preflight resilience** — cache org CI audit results; degrade gracefully on GitHub 403.
4. **Mount control-plane mirrors** on org-research Jobs so meta agents can read `observer.retry_counts`.
5. **Merge benchmarks verticals.toml** — closes `gap-infra-verticals-toml-missing-benchmarks-main`.

---

## Validity

| Criterion | Grade | Note |
|-----------|-------|------|
| Reproducibility | B | Scripts documented; PyYAML blocks full replay |
| Evidence freshness | A | Scorecard regenerated 2026-06-07 |
| Actionability | B+ | Local ingest fix ready; human merge required |

---

## References

- `lic/data/swarm-gap-registry/registry.yaml`
- `benchmarks/data/latest/swarm-gap-actions.json`
- `benchmarks/data/latest/ecosystem-quality-report.json`
- `app/data/runs/swarm_observer-1780848226802.md`
- `lic/docs/ecosystem/orchestrator-notes/2026-06-07-swarm-coverage-api-coverage-e31d7662.md`
