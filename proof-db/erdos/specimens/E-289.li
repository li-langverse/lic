# E-289: Is it true that, for all sufficiently large $k$, there exist finite intervals $I_1,\ldots,I_k\subset \mathbb{N}$, distinct, not overlapping or adjacent, with $\lvert I_i\rvert \geq 2$ for $1\leq i\leq k$ such that\[1=\sum_{i=1}^k \sum_{n\in I_i}\frac{1}{n}?\]
# erdos_number: 289
# erdos_status: open
# priority_tier: P2
# tags: number_theory, unit_fractions

def erdos_e289_is_it_true_that_for_all_sufficiently_lar() -> int
  requires true
  ensures result == 0  # open: E-289
  decreases 0
=
  return 0


def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
