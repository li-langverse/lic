# Swarm gap orchestration — API-coverage dimension

**Goal:** `swarm_coverage`  
**Dimension:** `api-coverage`  
**Worker:** `e77aa378`  
**Date:** 2026-06-11  
**north_star_fit:** ecosystem, ai — honest programmatic APIs prerequisite for agent JSON diagnostics and unattended gap orchestration  
**Publish target:** `research-findings/whitepapers/2026-06/swarm_coverage/api-coverage/`

---

## Abstract

This pass audits api-coverage for the `swarm_coverage` meta-goal. Gap orchestration (registry ingest, backlog apply, handoffs) depends on read APIs identical across homelab, K8s org-research Jobs, and CI. On 2026-06-11T23:45Z: file-based scorecard and gap artifacts are mountable; MCP briefing snapshot works; gap ingest CLI remains blocked on PyYAML; control-plane disk persistence and two MCP read tools are still missing — creating orchestration blind spots where `swarm_execution` scores 100 while `gap_pressure` stays at 60 with 62 frozen open gaps.

---

## Method

1. Refresh scorecard: `python3 scripts/ecosystem-quality-grade.py` in benchmarks.
2. Read `registry.yaml` and `swarm-gap-actions.json`.
3. Invoke MCP `get_briefing_snapshot` with `/workspace/benchmarks`.
4. Attempt `swarm-gap-ingest.py` / `swarm-gap-apply-actions.py`.
5. Inspect MCP tool catalog vs observer checklist.
6. Compare briefing `recommended_agents` vs scorecard recommendations.
7. Sample `/app/data/runs/*.json` for terminal errors.

---

## API inventory

### Control-plane REST (when ops-server running)

| Endpoint | Observer use |
|----------|--------------|
| `GET /api/swarm/health` | Retry counts, degraded flag |
| `GET /api/report` | `swarm_health`, interventions, recent runs |
| `GET /api/goals` | Mode B routing |
| `GET /api/runs` | Error classification |

*Unavailable in this Job pod.*

### MCP (`li-ecosystem-context`)

| Tool | Status |
|------|--------|
| `get_briefing_snapshot` | ✅ with `benchmarks_root` |
| `list_pending_handoffs` | ✅ |
| `read_ecosystem_quality_report` | ❌ missing |
| `read_swarm_gap_registry` | ❌ missing |

### File artifacts

| File | This run |
|------|----------|
| `ecosystem-quality-report.json` | ✅ `23:38:55Z` |
| `swarm-gap-actions.json` | ✅ stale `00:05:46Z` |
| `registry.yaml` | ✅ |
| `state.json` / `latest-report.json` | ❌ absent |

---

## Findings

### 1. Gap ingest CLI — PyYAML contract failure

Worker image lacks declared dependency. Registry and apply artifacts frozen ~24h while briefing and audits evolve.

### 2. Control-plane persistence gap

Without disk mirrors, `observer.retry_counts` and intervention history are unobservable — meta-agent cannot verify programmatic self-heal.

### 3. Orchestration signal drift

Briefing heap: `ci_maintainer` only. Scorecard adds `gap_explorer` + `plan_verifier` for 62 open gaps. Gap pressure does not propagate to dispatch API.

### 4. Preflight read failures

- `org_ci_audit` exit 1 — 35 repos missing CI; GitHub 404 on deleted repos.
- `org_agent_kit_audit` exit 1 — `roadmap/agent-kit` not mounted.

### 5. Open gap pressure

62 open: 30 `competitor_feature`, 31 `plan_debt`, 1 `missing_package`. Last apply: 19 patched, 20 deferred (no runner mapping).

---

## Recommendations

| Priority | Action | Owner |
|----------|--------|-------|
| P0 | Bake `python3-yaml` | deploy / `li-cursor-agents` |
| P0 | MCP read tools for scorecard + registry | `li-ecosystem-context` |
| P1 | CP disk mirrors each tick | observer |
| P1 | Union scorecard agents into briefing heap | benchmarks |
| P2 | Soft-fail agent-kit audit when roadmap unmounted | benchmarks preflight |

---

## Evidence

- `/workspace/benchmarks/data/latest/ecosystem-quality-report.json`
- `/workspace/benchmarks/data/latest/swarm-gap-actions.json`
- `/workspace/lic/data/swarm-gap-registry/registry.yaml`
- `/app/data/runs/swarm_observer-1781218298082.md`
- `/workspace/lic/docs/ecosystem/orchestrator-notes/2026-06-11-orch-api-coverage-e77aa378.md`
