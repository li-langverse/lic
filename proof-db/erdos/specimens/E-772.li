# Catalog: E-772
# Statement: Let $k\geq 1$ and $H_k(n)$ be the maximal $r$ such that if $A\subset\mathbb{N}$ has $\lvert A\rvert=n$ and $\| 1_A\ast 1_A\|_\infty \leq k$ then $A$ contains a Sidon set of size at least $r$. Is it true that $H_k(n)/n^{1/2}\to \infty$? Or e
# formalization_status: li_open
# discharge: pending — open formalization target (Phase 4)
# erdos_status (literature): proved

def formal_target_e_772() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
