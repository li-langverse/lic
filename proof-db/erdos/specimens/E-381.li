# E-381: A number $n$ is highly composite if $\tau(m)<\tau(n)$ for all $m<n$, where $\tau(m)$ counts the number of divisors of $m$. Let $Q(x)$ count the number of highly composite numbers in $[1,x]$. Is it true that\[Q(x)\gg_k (\log x)^k\]for every $k\geq 1$?
# erdos_number: 381
# erdos_status: open
# priority_tier: P2
# tags: divisors, number_theory

def erdos_e381_a_number_n_is_highly_composite_if_tau_m() -> int
  requires true
  ensures result == 0  # open: E-381
  decreases 0
=
  return 0


def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
