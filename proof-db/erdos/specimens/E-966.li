# E-966: Let $k,r\geq 2$. Does there exist a set $A\subseteq \mathbb{N}$ that contains no non-trivial arithmetic progression of length $k+1$, yet in any $r$-colouring of $A$ there must exist a monochromatic non-trivial arithmetic progression of length $k$?
# erdos_number: 966
# erdos_status: proved
# priority_tier: P2
# tags: additive_combinatorics, number_theory, ramsey_theory

def erdos_e966_let_k_r_geq_2_does_there_exist_a_set_a_s() -> int
  requires true
  ensures result == 0  # open: E-966
  decreases 0
=
  return 0

extern proc erdos_e966_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
