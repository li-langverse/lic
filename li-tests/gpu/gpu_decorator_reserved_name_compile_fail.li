# WP-GPU-05 Partial: `gpu` is a reserved decorator name (G-gpu policy).

decorator def gpu(x: int) -> int
  requires true
  ensures result == x
  decreases 0
=
  return x

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
