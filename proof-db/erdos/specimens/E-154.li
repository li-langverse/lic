# E-154: Let $A\subset \{1,\ldots,N\}$ be a Sidon set with $\lvert A\rvert\sim N^{1/2}$. Must $A+A$ be well-distributed over all small moduli? In particular, must about half the elements of $A+A$ be even and half odd?
# erdos_number: 154
# erdos_status: proved
# priority_tier: P2
# tags: sidon_sets

def erdos_e154_let_a_subset_1_ldots_n_be_a_sidon_set_wi() -> int
  requires true
  ensures result == 0  # open: E-154
  decreases 0
=
  return 0

extern proc erdos_e154_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
