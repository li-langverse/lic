# Ergonomic import: physics.relativity (not li_std_physics_relativity).

import physics.relativity

proc main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  var v: int = li_std_physics_relativity_version()
  if v < 1
    return 1
  return 0
