# Composable: physics-runtime imports rigid + numerics packages.

import li_std_physics_runtime

proc main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  var v: int = li_std_physics_runtime_version()
  if v < 2
    return 1
  return 0
