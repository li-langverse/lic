# E-766: Let $f(n;k,l)=\min \mathrm{ex}(n;G)$, where $G$ ranges over all graphs with $k$ vertices and $l$ edges. Give good estimates for $f(n;k,l)$ in the range $k<l\leq k^2/4$. For fixed $k$ and large $n$ is $f(n;k,l)$ a strictly monotone function of $l$?
# erdos_number: 766
# erdos_status: open
# priority_tier: P2
# tags: graph_theory, turan_number

def erdos_e766_let_f_n_k_l_min_mathrm_ex_n_g_where_g_ra() -> int
  requires true
  ensures result == 0  # open: E-766
  decreases 0
=
  return 0

extern proc erdos_e766_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
