# E-385: Let\[F(n) = \max_{\substack{m<n\\ m\textrm{ composite}}} m+p(m),\]where $p(m)$ is the least prime divisor of $m$. Is it true that $F(n)>n$ for all sufficiently large $n$? Does $F(n)-n\to \infty$ as $n\to\infty$?
# erdos_number: 385
# erdos_status: open
# priority_tier: P2
# tags: number_theory

def erdos_e385_let_f_n_max_substack_m_n_m_textrm_compos() -> int
  requires true
  ensures result == 0  # open: E-385
  decreases 0
=
  return 0

extern proc erdos_e385_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
