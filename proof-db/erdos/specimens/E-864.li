# E-864: Let $A\subseteq \{1,\ldots N\}$ be a set such that there exists at most one $n$ with more than one solution to $n=a+b$ (with $a\leq b\in A$). Estimate the maximal possible size of $\lvert A\rvert$ - in particular, is it true that\[\lvert A\rvert \leq (1+o(1))\frac{2}{\sqrt{3}}N^{1/2}?\]
# erdos_number: 864
# erdos_status: open
# priority_tier: P2
# tags: additive_combinatorics, number_theory, sidon_sets

def erdos_e864_let_a_subseteq_1_ldots_n_be_a_set_such_t() -> int
  requires true
  ensures result == 0  # open: E-864
  decreases 0
=
  return 0


def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
