# Swarm coverage — performance dimension whitepaper (staging)

**Goal:** `swarm_coverage`  
**Dimension:** `performance`  
**Worker:** `7167d931`  
**Generated:** 2026-06-07T19:34Z  
**Publish target:** `research-findings/whitepapers/2026-06/swarm_coverage/performance/`  
**north_star_fit:** ecosystem, ai — proof-before-perf (PH-7e, PH-8p)

---

## Thesis

Swarm gap orchestration for the **performance** pillar is **blocked at the control plane**, not at LLM throughput. The dominant cost is **failed gap ingest/apply** (Python syntax + missing PyYAML), **stale goal-directed snapshot**, and **benchmark posture drift** (2 yellow numerics, 5 near-threshold). Until the registry pipeline runs unattended, performance gaps in `competitor_feature` and sim/httpd `plan_debt` rows cannot close via backlog handoffs.

---

## Benchmark posture (2026-06-07)

Source: `/workspace/benchmarks/data/latest/ecosystem-audit.json`

| Class | Count | IDs |
|-------|------:|-----|
| Red (tier-1) | 0 | — |
| Yellow | 2 | `num_eig_symmetric`, `num_root_newton` |
| Near threshold (1.18–1.20× cpp) | 5 | `num_opt_bfgs`, `num_integ_semi_implicit`, `num_integ_euler`, `num_integ_rk4`, `num_cg` |
| Green | 145 | — |

**Interpretation:** Li meets the ≤1.2× cpp tier-1/2 bar on most numerics; yellow rows are the next `bench_improver` targets. Registry still lists historical tier-1 **red** competitor gaps — reconcile after fresh ingest.

---

## Gap registry — performance-relevant open rows

Source: `/workspace/lic/data/swarm-gap-registry/registry.yaml`, `/workspace/benchmarks/data/latest/swarm-gap-actions.json`

| `gap_kind` | Open | Performance handoff |
|------------|-----:|---------------------|
| `competitor_feature` | 30 | `bench_improver`, `numerics_researcher` |
| `plan_debt` | 31 | `plan_verifier`, runner backlogs (sim, httpd, ph-db) |
| `missing_package` | 3 | `issue_planner` (`li-line-profiler`) |

**Top plan_debt (perf):**

- `gap-plan-pending-sim-sim-p1-num-dot-axpy` → sim-algorithm-backlog.md (patched)
- `gap-plan-pending-httpd-gap-phase2-perf-wrk-soak` → httpd wrk soak (snapshot pending)
- `gap-plan-debt-lic-master-plan-phase-8p-parallel-compile-ci-thr` → CI throughput (lic#1096 failing)

---

## Control-plane performance metrics

| Signal | Value | Impact |
|--------|-------|--------|
| Ecosystem grade | D (62.9) | `unattended_safe: false` |
| `runs_sampled` | 0 | Grader blind to execution errors |
| Goal runners stopped | 6 / 9 | sim/httpd/ph-db loops idle |
| Failed PRs | 11 | Blocks merge throughput |
| org-research swarm_observer | ~17–30 min/run recent | Meta-audit cost acceptable; ingest failure wastes cycles |

---

## Recommendations (proof → easy → fast)

1. **Fix gap pipeline** — merge ingest Path fix; bake PyYAML; rerun ingest + apply.
2. **Dispatch `bench_improver`** — yellow numerics before PH-7e SIMD claims.
3. **Dedupe sim PR stack** — one qm_dft PR, close lic#1080–1104 duplicates.
4. **Refresh goal-directed snapshot** — unblock httpd perf wrk soak todo.
5. **Set `LI_CURSOR_AGENTS_ROOT=/app`** — restore grader run sampling.

---

## Citations

- `/workspace/benchmarks/data/latest/ecosystem-quality-report.json`
- `/workspace/benchmarks/data/latest/ecosystem-audit.json`
- `/workspace/lic/docs/ecosystem/orchestrator-notes/2026-06-07-swarm-coverage-performance-7167d931.md`
- `/app/data/runs/swarm_observer-1780859924531.md`
