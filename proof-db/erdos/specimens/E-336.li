# E-336: For $r\geq 2$ let $h(r)$ be the maximal finite $k$ such that there exists a basis $A\subseteq \mathbb{N}$ of order $r$ (so every large integer is the sum of at most $r$ integers from $A$) and exact order $k$ (so every large integer is the sum of exactly $k$ integers from $A$). Find the value of\[\lim_r \frac{h(r)}{r^2}.\]
# erdos_number: 336
# erdos_status: open
# priority_tier: P2
# tags: additive_basis, number_theory

def erdos_e336_for_r_geq_2_let_h_r_be_the_maximal_finit() -> int
  requires true
  ensures result == 0  # open: E-336
  decreases 0
=
  return 0

extern proc erdos_e336_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
