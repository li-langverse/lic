# Orchestrator note — UX gap orchestration (`ea69cd5a`)

**Date:** 2026-06-11  
**Agent:** `swarm_observer`  
**Research goal:** `swarm_coverage` (north_star_fit: ecosystem, ai)  
**Dimension:** `ux`  
**Worker:** `ea69cd5a`

---

## Executive summary

| Field | Value |
|-------|-------|
| Swarm posture | **Degraded (conditional)** — grade **C** (76.1); `unattended_safe: true` |
| Gap prep | **Blocked** — PyYAML missing; last apply @ `00:05:46Z` |
| Open gaps | **62** (0 `ui_ux` kind; studio UX = `plan_debt`) |
| UX audit posture | **Docs-only** preflight; 5 GUI targets in handoff not in routine briefing |
| CP artifacts | **Missing** — no `state.json` / `latest-report.json` |
| Unattended? | **Conditional** — agents run; operators lack honest health + UX audit pane |

---

## UX reconciliation (easy pillar — operator surfaces)

Swarm orchestration UX = how operators **see** health, gaps, and handoffs. This pass confirms prior UX workers (`24dfaba5`, `6a24a258`, `20cccf19`) with refreshed scorecard @ `13:31Z` and unchanged orchestration blockers.

### Operator blind spots

1. **No `/app/data/control-plane/state.json` or `latest-report.json`** — retry budgets, interventions, and `swarm_degraded` opaque across Job restarts.
2. **Gap pipeline frozen in this container** — ingest/apply cannot run without PyYAML; 62 gaps last patched @ `00:05:46Z`.
3. **Preflight UX audits** — `ux-audit.json` only exercises `lic-docs`. Five GUI targets in [gui-ux-quality-handoff.md](../gui-ux-quality-handoff.md) absent from briefing.
4. **Briefing heap drift** — scorecard recommends four agents; briefing heap dispatches two (`ci_maintainer`, `security_auditor`).

### Studio / GUI gap routing

| Item | Status | Handoff |
|------|--------|---------|
| `orch-r4-ui-ux-signals` | open, apply deferred | **Reconciled this pass** → `ui_ux_quality` goal → `gui_ux_tester` |
| `studio-ux-16`, `studio-ux-17` | skip — plan not mounted | `studio_ui_ux_builder` when `STUDIO_UI_UX_PLAN_PATH` set |
| `gap-ux-audit-agents-dashboard` | skip in last full sweep | `gui_ux_tester` + dev server ([#38](https://github.com/li-langverse/li-cursor-agents/issues/38)) |
| `gap-ux-audit-world-studio-demo` | Partial (HTML mock) | `studio_ui_ux_builder` |

Do **not** spawn new `studio-ui-ux` systemd loops — use research lane + existing agent ids per [swarm-architecture.md](../swarm-architecture.md).

---

## Gap orchestration (Mode B)

```bash
# Intended each cycle (BLOCKED this run):
cd /workspace/lic
python3 scripts/swarm-gap-ingest.py
python3 scripts/swarm-gap-apply-actions.py
# → benchmarks/data/latest/swarm-gap-actions.json
```

**Blocked:** `swarm-gap-ingest: PyYAML required (pip install pyyaml)` — bake `python3-yaml` into org-research worker image.

| `gap_kind` | Open | UX-relevant action |
|------------|------|-------------------|
| `plan_debt` | 31 | `orch-r4`, studio-ux-16/17, master-plan Doc-c |
| `competitor_feature` | 30 | sim-md stubs — not operator UX |
| `missing_package` | 1 | `li-line-profiler` — dev UX tooling |

### Reconciliation actions (this pass)

1. **`orch-r4-ui-ux-signals`** — route to existing `ui_ux_quality` goal (`gui_ux_tester`); document five `gap-ux-*` rows for next ingest (handoff doc).
2. **`studio-ux-16/17`** — defer until `lic-studio-ui` plan mounted; env `STUDIO_UI_UX_PLAN_PATH` or fallback to lic plan at `docs/superpowers/plans/2026-05-24-studio-ui-ux-plan-loop.md`.
3. **Briefing alignment** — recommend union of scorecard agents into heap on next `benchmarks` / `li-cursor-agents` change.

---

## Briefing vs scorecard drift

| Source | Top agents |
|--------|------------|
| Briefing heap | `ci_maintainer`, `security_auditor` |
| Scorecard | `gap_explorer`, `ci_maintainer`, `plan_verifier`, `security_auditor` |

**Action:** enqueue `gap_explorer` when `gap_pressure < 80`; enqueue `plan_verifier` when `plan_audit` skipped.

---

## Evidence paths

- `/workspace/benchmarks/data/latest/ecosystem-quality-report.json`
- `/workspace/benchmarks/data/latest/agent-briefing.json`
- `/workspace/benchmarks/data/latest/swarm-gap-actions.json`
- `/workspace/lic/data/swarm-gap-registry/registry.yaml`
- `/workspace/benchmarks/data/latest/ux-audit.json`
- `/app/data/runs/swarm_observer-1781181387019.md`

---

## Handoffs (cite north_star_fit)

| To | Reason |
|----|--------|
| `gui_ux_tester` | `ui_ux_quality` — expand GUI preflight beyond docs |
| `gap_explorer` | 62 open gaps; verticals.toml ingest blocked on benchmarks main |
| `plan_verifier` | master-plan Doc-c (#1476) + stale goal-directed snapshot |
| `issue_planner` | `li-line-profiler` missing_package seed |
