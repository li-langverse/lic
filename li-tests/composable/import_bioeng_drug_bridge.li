# PH-BIOENG + PH-DRUG — DBTL bridge to drug-design LITL stages.

import bioeng
import sim.drug_design

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  if li_sim_drug_design_version() < 1:
    return 1
  if bioeng_from_drug_stage(lab_loop_stage_dft()) != bioeng_stage_test():
    return 2
  return bioeng_dbtl_smoke()
