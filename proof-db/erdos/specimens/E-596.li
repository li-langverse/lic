# E-596: For which graphs $G_1,G_2$ is it true that for every $n\geq 1$ there is a graph $H$ without a $G_1$ but if the edges of $H$ are $n$-coloured then there is a monochromatic copy of $G_2$, and yet for every graph $H$ without a $G_1$ there is an $\aleph_0$-colouring of the edges of $H$ without a monochromatic $G_2$.
# erdos_number: 596
# erdos_status: open
# priority_tier: P2
# tags: graph_theory, ramsey_theory, set_theory

def erdos_e596_for_which_graphs_g_1_g_2_is_it_true_that() -> int
  requires true
  ensures result == 0  # open: E-596
  decreases 0
=
  return 0

extern proc erdos_e596_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
