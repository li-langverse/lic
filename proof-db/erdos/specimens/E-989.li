# Catalog: E-989
# Statement: If $A=\{z_1,z_2,\ldots \}\in \mathbb{R}^2$ is an infinite sequence then let\[f(r)=\max_C \left\lvert \lvert A\cap C\rvert-\pi r^2\right\rvert,\]where the maximum is taken over all circles $C$ of radius $r$. Is $f(r)$ unbounded for every $A$
# formalization_status: li_open
# discharge: pending — open formalization target (Phase 4)
# erdos_status (literature): open

def formal_target_e_989() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
