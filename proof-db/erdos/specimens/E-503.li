# E-503: What is the size of the largest $A\subseteq \mathbb{R}^d$ such that every three points from $A$ determine an isosceles triangle? That is, for any three points $x,y,z$ from $A$, at least two of the distances $\lvert x-y\rvert,\lvert y-z\rvert,\lvert x-z\rvert$ are equal.
# erdos_number: 503
# erdos_status: open
# priority_tier: P2
# tags: distances, geometry

def erdos_e503_what_is_the_size_of_the_largest_a_subset() -> int
  requires true
  ensures result == 0  # open: E-503
  decreases 0
=
  return 0

extern proc erdos_e503_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
