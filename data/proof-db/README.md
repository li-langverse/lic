# Proof database rebuild artifacts

`./scripts/proof-db/rebuild_lemmas.sh` → `latest-report.json` / `latest-report.md`.

**Policy:** `build_failed` or `open` = gap to investigate (VC, discharge, lake) — **not** Li user-code wrongness.

Entries: `docs/verification/proof-database/entries/`.

`./scripts/proof-db-gap2-report.sh` → `gap2-report.md` (three `L-MATH-*` catalog targets).
