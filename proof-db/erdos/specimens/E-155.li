# E-155: Let $F(N)$ be the size of the largest Sidon subset of $\{1,\ldots,N\}$. Is it true that for every $k\geq 1$ we have\[F(N+k)\leq F(N)+1\]for all sufficiently large $N$?
# erdos_number: 155
# erdos_status: open
# priority_tier: P2
# tags: additive_combinatorics, sidon_sets

def erdos_e155_let_f_n_be_the_size_of_the_largest_sidon() -> int
  requires true
  ensures result == 0  # open: E-155
  decreases 0
=
  return 0

extern proc erdos_e155_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
