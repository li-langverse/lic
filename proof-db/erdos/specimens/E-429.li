# E-429: Is it true that, if $A\subseteq \mathbb{N}$ is sparse enough and does not cover all residue classes modulo $p$ for any prime $p$, then there exists some $n$ such that $n+a$ is prime for all $a\in A$?
# erdos_number: 429
# erdos_status: open
# priority_tier: P2
# tags: number_theory

def erdos_e429_is_it_true_that_if_a_subseteq_mathbb_n_i() -> int
  requires true
  ensures result == 0  # open: E-429
  decreases 0
=
  return 0

extern proc erdos_e429_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
