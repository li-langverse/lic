# E-32: Is there a set $A\subset\mathbb{N}$ such that\[\lvert A\cap\{1,\ldots,N\}\rvert = o((\log N)^2)\]and such that every large integer can be written as $p+a$ for some prime $p$ and $a\in A$? Can the bound $O(\log N)$ be achieved? Must such an $A$ satisfy\[\liminf \frac{\lvert A\cap\{1,\ldots,N\}\rvert}{\log N}> 1?\]
# erdos_number: 32
# erdos_status: open
# priority_tier: P2
# tags: additive_basis, number_theory

def erdos_e32_is_there_a_set_a_subset_mathbb_n_such_th() -> int
  requires true
  ensures result == 0  # open: E-32
  decreases 0
=
  return 0


def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
