# D-LM-BC-CMB-017: Binomial coefficient identity (variant 17).

def binomial_symmetry(n: int, k: int) -> int
  requires n >= 0 and 0 <= k and k <= n
  ensures result == n - k
  decreases 0
=
  return n - k

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
