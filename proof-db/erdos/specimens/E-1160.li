# Catalog: E-1160
# Statement: Let $g(n)$ denote the number of groups of order $n$. If $n\leq 2^m$ then $g(n)\leq g(2^m)$.
# formalization_status: li_open
# discharge: pending — open formalization target (Phase 4)
# erdos_status (literature): open

def formal_target_e_1160() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
