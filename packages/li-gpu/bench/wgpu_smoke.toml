# Bench harness hook — read by scripts/bench-studio-viewport-perf.sh
[wgpu_smoke]
hook_version = 1
backend = "wgpu"
status = "stub_pass"
native_device = false
notes = "Deprecated hook — use packages/lig/bench/wgpu_smoke.toml (PH-HW HW-1)"
redirect = "packages/lig/bench/wgpu_smoke.toml"
