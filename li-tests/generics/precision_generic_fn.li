# Pattern B: generic def — same shape as identity.li (verify_ok).

def first[S](x: S, y: S) -> S
  requires true
  ensures result == x
  decreases 0
=
  return x

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
