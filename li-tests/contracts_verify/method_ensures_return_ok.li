type StepCounter = object
  public n: int

def StepCounter_zero(self: StepCounter) -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  var c: StepCounter
  c.n = 5
  c.zero()
  return 0
