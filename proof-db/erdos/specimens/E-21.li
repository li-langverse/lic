# E-21: Let $f(n)$ be minimal such that there is an intersecting family $\mathcal{F}$ of sets of size $n$ (so $A\cap B\neq\emptyset$ for all $A,B\in \mathcal{F}$) with $\lvert \mathcal{F}\rvert=f(n)$ such that any set $S$ with $\lvert S\rvert \leq n-1$ is disjoint from at least one $A\in \mathcal{F}$. Is it true that\[f(n) \ll n?\]
# erdos_number: 21
# erdos_status: proved
# priority_tier: P2
# tags: combinatorics, intersecting_family

def erdos_e21_let_f_n_be_minimal_such_that_there_is_an() -> int
  requires true
  ensures result == 0  # open: E-21
  decreases 0
=
  return 0

extern proc erdos_e21_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
