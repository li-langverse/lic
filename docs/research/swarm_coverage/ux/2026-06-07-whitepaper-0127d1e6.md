# Swarm gap orchestration — UX dimension audit

**Goal:** `swarm_coverage`  
**Dimension:** `ux`  
**Worker:** `0127d1e6`  
**Generated:** 2026-06-07T01:23Z  
**north_star_fit:** Swarm gap orchestration — registry, backlog apply, handoffs — domains: ecosystem, ai  
**Publish target:** `research-findings/whitepapers/2026-06/swarm_coverage/ux/`

---

## Abstract

This pass audits **user-experience and accessibility signals** in the Li agent swarm gap pipeline. The swarm is **degraded** (ecosystem grade **D**, 60.9). Primary UX blockers are a **duplicate PR stack** on benchmarks GPU chip picker ARIA/keyboard navigation (#147) and **two open studio-ui-ux plan todos** not yet routed through `gui_ux_tester`. Gap ingest/apply remains blocked on missing PyYAML in the worker image despite a local syntax fix in `swarm-gap-ingest.py`.

---

## UX signal taxonomy (gap_kind: `ui_ux`)

| Source | Signal | Swarm routing |
|--------|--------|---------------|
| `gui_ux_tester` / `ui_ux_quality` goal | Dashboard a11y, WCAG tabs, keyboard roving | `benchmarks` PR consolidation |
| `studio-ui-ux` plan loop | Particle display, palette latency, GPU fail recovery | Registry `plan_debt` rows → plan todos |
| Briefing `failed_prs` | CI-red UX fixes stacking | `pr_merger` blocked until one PR wins |
| Ecosystem grade `ecosystem_posture` | 26 failed PRs depress score | Meta-audit → `swarm_observer` |

---

## Findings

### 1. Benchmarks GPU chip picker — accessibility debt cluster

Eight or more open PRs share the same intent: **ARIA tablist + roving tabindex** on the GPU chip picker (`#147`). All show failing CI. This creates:

- Operator confusion (which PR to review/merge)
- Inflated `failed_prs` metric (26 total)
- Blocked grade-refresh PRs (#401 metrics chore)

**Evidence:** `agent-briefing.json` → `ecosystem_audit.failed_prs` (repos `benchmarks`, numbers 400–409).

**Recommendation:** Human or `gui_ux_tester` selects canonical PR; close/rebase duplicates. Label: `ux`, `a11y`.

### 2. Studio UI UX plan todos (open)

| Todo | Title fragment | Registry gap id |
|------|----------------|-----------------|
| `studio-ux-16-palette-search-latency` | Palette search latency budget | `gap-plan-pending-studio-ui-ux-studio-ux-16-palette-search-latency` |
| `studio-ux-17-gpu-fail-recovery` | GPU failure recovery UX | `gap-plan-pending-studio-ui-ux-studio-ux-17-gpu-fail-recovery` |

**Evidence:** `lic/data/swarm-gap-registry/registry.yaml` (status: open).

### 3. Gap pipeline UX for operators

Operators cannot refresh gap registry without PyYAML. Ingest script had **SyntaxError at line 229** (unterminated string) — fixed locally:

```python
vert = Path(os.environ.get("BENCHMARKS_COMPETITIVE", ...)) / "verticals.toml"
```

Apply script still fails: `ModuleNotFoundError: yaml`.

**Impact:** `orch-r3` / `orch-r4` registry rows cannot auto-close; backlogs drift from snapshot.

### 4. Control-plane observability gap

`/app/data/control-plane/latest-report.json` and `state.json` were absent at audit start. Bootstrapped minimal exports for dashboard IPC (this pass).

---

## Validity

| Criterion | Grade | Notes |
|-----------|-------|-------|
| Evidence freshness | B | Briefing 2026-06-07; snapshot 2026-05-30 (stale) |
| Reproducibility | C | PyYAML blocks live ingest replay |
| Actionability | A | Clear PR consolidation + image bake steps |

---

## References

- `docs/ecosystem/research-verticals.md` — `ui_ux_quality` non-vertical goal
- `config/research-goals.yaml` — `swarm_coverage` handoff_to includes `gap_explorer`, `plan_verifier`
- `lic/docs/ecosystem/orchestrator-notes/2026-06-07-orch-r4-ui-ux-signals-0127d1e6.md`
