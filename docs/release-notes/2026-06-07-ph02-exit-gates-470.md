# Release notes: 2026-06-07 — ph02-exit-gates-470

**Status:** Ready for review  
**Repo:** li-langverse/lic  
**Issue:** [#470](https://github.com/li-langverse/lic/issues/470)  
**PH / REQ:** PH-02 / Phase **2d** (lexical borrow + type rejection)  
**Author:** code_implementer (worker `97159a01`)

---

## Summary (one sentence)

Verified Phase 2 typechecker exit gates against `li-tests` manifest evidence; sub-plan checkboxes were already `[x]` on main — no compiler or diagnostic change required.

## Agent continuation (required)

1. Read: `docs/superpowers/plans/2026-05-14-phase-02-typechecker.md` § Phase 2 exit gate; `li-tests/manifest.toml` rows cited below.
2. Run: `./li-tests/run_all.sh typecheck`, `./li-tests/run_all.sh borrow`, `LIC_ROOT=$PWD python3 ../benchmarks/scripts/plan-completion-audit.py`.
3. Then: close #470 when CI green; optional benchmarks follow-up to keep `LIC_ROOT` set in org audit CI ([#20](https://github.com/li-langverse/benchmarks/issues/20)).
4. Blocked on: human merge only.

## Verified gates

| Gate | Sub-plan | Manifest evidence | Result |
|------|----------|-------------------|--------|
| `fib.li` typechecks | `[x]` (`li-tests/typecheck/fib.li`) | `typecheck/fib.li` → `verify_ok` | PASS |
| All `bad_*.li` fail with expected errors | `[x]` | `bad_array_index.li` (`out of range`), `bad_numeric_mix.li` / `bad_int_float_add.li` | PASS (8/8 typecheck suite) |
| Borrow double-mut fails cleanly | `[x]` (`li-tests/borrow/double_mut.li`) | `borrow/double_mut.li` → `expected_substr=borrow` | PASS (3/3 borrow suite) |

## Test commands (2026-06-07)

```bash
LI_REPO_ROOT=$PWD ./li-tests/run_all.sh typecheck   # pass=8 fail=0
LI_REPO_ROOT=$PWD ./li-tests/run_all.sh borrow      # pass=3 fail=0
LIC_ROOT=$PWD python3 ../benchmarks/scripts/plan-completion-audit.py
# plan_files_open: 0 rows for phase-02-typechecker exit gates
```

## Not changed (scope fence)

- Compiler C++ implementation — **not** changed
- `docs/superpowers/plans/2026-05-14-phase-02-typechecker.md` — exit gates already `[x]` on main
- `docs/verification/provability-gaps.md` — **G-vc**, **G-bnd**, **G-def**, **G-math-syn** unchanged

## Breaking changes

None.

## Security

N/A — verification-only; type/borrow rejection behavior unchanged.

## Performance

N/A — no codegen or bench threshold changes.

## Downstream

| Repo | Action |
|------|--------|
| benchmarks | Ensure org audit CI sets `LIC_ROOT` so stale #470-class issues stop filing |

## CHANGELOG entry (paste into Unreleased)

```markdown
### Changed
- PH-02 exit gate verification (#470) — confirmed typecheck/borrow manifest suites and plan audit alignment — [2026-06-07-ph02-exit-gates-470.md](docs/release-notes/2026-06-07-ph02-exit-gates-470.md).
```
