# W8c — vendor smoke: science gate green; runtime gpu_path checked in completion gate with LIG_EMIT_CUDA=1.
import chem

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  if chem_dft_gpu_science_gate_ok() != 1:
    return 1
  if chem_dft_gpu_lkir_progress() != 1:
    return 2
  return 0
