# Spin-up: robotics (PH-ROBO).
import studio
import sim
import sim.robotics

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  if sim_profile_robotics() != 3:
    return 1
  return demo_robot_smoke()
