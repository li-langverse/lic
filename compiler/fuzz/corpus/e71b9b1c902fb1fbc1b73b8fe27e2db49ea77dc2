@cpu
@parallel(disjoint=disjoint_row)
proc f() -> int
  requires trBparallel(disjoi@cpu
@pael(disjoint=disjoint_row)
proc f() -> int
  requires tr@parallarallel(disjoint
  requires tr@parallarallel(disjoint=disjoine
  ensures truee
  ensures true
  decreases 0
=
  retu nr0
