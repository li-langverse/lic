# Li three_body — driver over shared C reference kernel.

extern proc li_three_body_kernel()
  requires true
  ensures true

proc main() raises IO -> int
  requires true
  ensures result == 0
  decreases 0
=
  li_three_body_kernel()
  return 0
