# Catalog: E-698
# Statement: Is there some $h(n)\to \infty$ such that for all $2\leq i<j\leq n/2$\[\textrm{gcd}\left( \binom{n}{i},\binom{n}{j}\right) \geq h(n)?\]
# formalization_status: li_open
# discharge: pending — open formalization target (Phase 4)
# erdos_status (literature): proved

def formal_target_e_698() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
