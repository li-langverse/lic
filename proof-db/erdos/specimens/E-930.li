# E-930: Is it true that, for every $r$, there is a $k$ such that if $I_1,\ldots,I_r$ are disjoint intervals of consecutive integers, all of length at least $k$, then\[\prod_{1\leq i\leq r}\prod_{m\in I_i}m\]is not a perfect power?
# erdos_number: 930
# erdos_status: open
# priority_tier: P2
# tags: number_theory

def erdos_e930_is_it_true_that_for_every_r_there_is_a_k() -> int
  requires true
  ensures result == 0  # open: E-930
  decreases 0
=
  return 0


def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
