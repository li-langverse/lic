# E-100: Let $A$ be a set of $n$ points in $\mathbb{R}^2$ such that all pairwise distances are at least $1$ and if two distinct distances differ then they differ by at least $1$. Is the diameter of $A$ $\gg n$?
# erdos_number: 100
# erdos_status: open
# priority_tier: P2
# tags: distances, geometry

def erdos_e100_let_a_be_a_set_of_n_points_in_mathbb_r_2() -> int
  requires true
  ensures result == 0  # open: E-100
  decreases 0
=
  return 0

extern proc erdos_e100_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
