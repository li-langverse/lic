# E-205: Is it true that all sufficiently large $n$ can be written as $2^k+m$ for some $k\geq 0$, where $\Omega(m)<\log\log m$? (Here $\Omega(m)$ is the number of prime divisors of $m$ counted with multiplicity.) What about $<\epsilon \log\log m$? Or some more slowly growing function?
# erdos_number: 205
# erdos_status: open
# priority_tier: P2
# tags: number_theory

def erdos_e205_is_it_true_that_all_sufficiently_large_n() -> int
  requires true
  ensures result == 0  # open: E-205
  decreases 0
=
  return 0


def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
