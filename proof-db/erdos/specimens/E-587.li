# E-587: What is the size of the largest $A\subseteq \{1,\ldots,N\}$ such that, for all $\emptyset\neq S\subseteq A$, $\sum_{n\in S}n$ is not a square?
# erdos_number: 587
# erdos_status: open
# priority_tier: P2
# tags: number_theory

def erdos_e587_what_is_the_size_of_the_largest_a_subset() -> int
  requires true
  ensures result == 0  # open: E-587
  decreases 0
=
  return 0

extern proc erdos_e587_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
