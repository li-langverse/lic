# Swarm gap orchestration — security dimension

**Goal:** `swarm_coverage`  
**Dimension:** `security`  
**Worker:** `88e43005`  
**Date:** 2026-06-07  
**north_star_fit:** ecosystem, secure — swarm meta-orchestration for security plan_debt and CWE catalog alignment  
**Publish target:** `research-findings/whitepapers/2026-06/swarm_coverage/security/`

---

## Abstract

This pass audits whether the Li agent swarm can orchestrate **security-related gap registry rows** without human intervention. Findings: security preflight (`security_cwe_audit`) succeeds, but **gap ingest/apply is blocked** by a syntax error and missing PyYAML; three `security-research` plan todos remain pending; **19 MITRE Top-25 CWEs** are absent from the CVE catalog. The swarm is **not unattended-safe** for security coverage until ingest is repaired and `security_auditor` is dispatched on `sec-r1`–`sec-r3`.

---

## 1. Security posture signals

| Signal | Value | Evidence |
|--------|-------|----------|
| CWE feed sync | OK (exit 0) | `agent-briefing.preflight_runs.security_cwe_audit` |
| Top-25 in catalog | 6/25 present | `security-cwe-feed.json` → `catalog_cwe_count: 15`, `missing_in_catalog: 19` |
| Security workflows on org repos | 0 repos missing security workflow (per briefing heap) | `agent-briefing.recommended_agents` |
| Open security plan_debt | 3 (`sec-r1`–`sec-r3`) | `registry.yaml` |
| Failed security-adjacent PRs | lis#40–42 (registry/edge) | `agent-briefing.ecosystem_audit.failed_prs` |

---

## 2. Gap registry — security rows

### 2.1 plan_debt (`security-research` runner)

All three rows were patched to `docs/ecosystem/security-research-backlog.md` on 2026-05-31 but remain **pending** because the `security-research` goal-directed runner is stopped (stale snapshot 2026-05-30).

| Todo | Content summary | Suggested agent |
|------|-----------------|-----------------|
| `sec-r1-httpd-fuzz-smoke` | httpd fuzz smoke gate | `security_auditor` |
| `sec-r2-tier5-gap-exploit` | tier5 exploit parity | `security_auditor` |
| `sec-r3-runtime-surface` | runtime attack surface survey | `security_auditor` |

**Handoff contract:** cite `north_star_fit: secure, ecosystem`; deliver `docs/security/studies/YYYY-MM-DD-<todo>.md`; no auto-merge on exploit PoCs.

### 2.2 ph-db security bench

`gap-plan-pending-ph-db-wp-n5-security-bench` — deferred (no backlog mapping). Route via `issue_planner` when ph-db runner restarts.

### 2.3 CWE catalog (`missing_package` class)

Not registry rows, but briefing P0: **`security_auditor`** for catalog backfill. Human-gated merge to `lic/security/cve-catalog.json`.

---

## 3. Orchestration blockers (security impact)

1. **`swarm-gap-ingest.py:229` SyntaxError** — prevents closing completed security todos in registry when snapshot updates.
2. **PyYAML missing** — `swarm-gap-apply-actions.py` cannot refresh backlog patches.
3. **Control-plane mirrors absent** — observer cannot auto-retry `security_auditor` or track error streaks.
4. **Stale goal-directed snapshot** — `security-research` runner appears stopped; `sec-r0-cwe-delta` marked completed in backlog but registry ingest cannot sync.

---

## 4. Recommendations

1. Merge ingest syntax fix on `lic` main; bake PyYAML in org-research worker.
2. Dispatch **`security_auditor`** on `offensive_security` goal for `sec-r1` (httpd fuzz smoke).
3. Open human-reviewed issue for CWE Top-25 catalog backfill (19 rows).
4. Triage **lis#41/#42** registry validation CI before edge security claims ship.
5. Persist control-plane `state.json` / `latest-report.json` so programmatic heal can retry security lane.

---

## 5. Evidence index

- `/workspace/benchmarks/data/latest/ecosystem-quality-report.json`
- `/workspace/benchmarks/data/latest/security-cwe-feed.json`
- `/workspace/benchmarks/data/latest/swarm-gap-actions.json`
- `/workspace/lic/data/swarm-gap-registry/registry.yaml`
- `/workspace/lic/docs/ecosystem/security-research-backlog.md`
- `/workspace/lic/docs/ecosystem/orchestrator-notes/2026-06-07-orch-r9-security-88e43005.md`
- `/app/data/runs/swarm_observer-1780844618673.md`

---

## 6. Deferred publication

Copy to `research-findings/whitepapers/2026-06/swarm_coverage/security/` when repo is mounted; update research-findings index out of band.
