# Swarm gap orchestration — UX dimension whitepaper (staging)

**Goal:** `swarm_coverage`  
**Dimension:** `ux`  
**Worker:** `d6e61568`  
**Date:** 2026-06-07  
**Publish target:** `research-findings/whitepapers/2026-06/swarm_coverage/ux/` (deferred — repo not mounted)  
**north_star_fit:** ecosystem, ai — operator clarity before autonomous product changes

---

## Abstract

Li’s agent swarm exposes UX debt in two layers: **operator-facing** (dashboard, briefing, gap-apply visibility) and **product-facing** (studio-ui palette/GPU recovery, scientific viz stubs). This audit maps open registry rows to existing swarm goals (`ui_ux_quality`, `game_engine_ux`) without inventing new systemd plan loops. Ecosystem grade **D (63.9)** and `unattended_safe: false` reflect missing control-plane persistence and blocked gap ingest (PyYAML), not LLM quality.

---

## Operator UX findings

| Signal | Impact | Mitigation |
|--------|--------|------------|
| Missing `state.json` / `latest-report.json` | Dashboard cannot show swarm health or interventions | Observer tick must persist disk mirrors |
| Gap apply file stale (2026-05-31) | Operators cannot trust backlog patch status | Re-run ingest/apply after PyYAML + ingest fix |
| `runs_sampled=0` in scorecard | False-negative “healthy execution” | Set `LI_CURSOR_AGENTS_ROOT=/app` in org-research workers |
| GitHub 403 on org CI audit | Briefing incomplete; ci_maintainer under-informed | Cache + backoff in preflight |
| Org-research retry history | ~16 agent-minutes on persist failures (May 31) | Circuit-break + finalize runs on Job exit |

Evidence: `/workspace/benchmarks/data/latest/ecosystem-quality-report.json`, `/app/data/goal-directed-sprints/org-research-audit.jsonl`.

---

## Product UX gaps (registry)

### Studio UI plan loop

| Todo | UX gate | Registry | Handoff |
|------|---------|----------|---------|
| studio-ux-16 | Palette open ≤50ms, filter ≤30ms | Open (stale) — **shipped** in tree | Close on snapshot refresh |
| studio-ux-17 | GPU fail recovery UX | Open | `gui_ux_tester` |
| studio-ux-21 | wgpu swapchain GPU runner | Patched to plan | Implement lane |
| studio-ux-24 | GPU runner deps | Patched to plan | Implement lane |

Reference iteration: `lic/docs/reports/studio-ui-ux/iterations/20260530T092400Z-studio-ux-16-palette-search-latency.md`.

### Vertical stub honesty (competitor_feature → UX research)

Scientific visualization and cinematic pipeline stubs (`scientific_viz`, `cinematic_encode`, `cinematic_color_grade`, `cinematic_audio_sync`, `mmo_shard`) are patched to sim-md-research backlogs. Li must document **stub vs SOTA** before claiming gaming/HPC UX parity — aligned with proof → easy → fast pillar order.

---

## Recommendations

1. **Dispatch `gui_ux_tester`** on `ui_ux_quality` for studio-ux-17 and viz stub honesty audits.
2. **Dashboard panel** for ecosystem grade + `swarm-gap-actions` apply status (li-cursor-agents).
3. **Bake PyYAML** in org-research worker; merge ingest Path fix (lic).
4. **Refresh goal-directed snapshot** to close false-open studio-ux-16 registry row.
5. **Do not** add lic systemd loops — use async swarm per `docs/ecosystem/swarm-architecture.md`.

---

## Evidence index

- Report: `/app/data/runs/swarm_observer-1780841922772.md`
- Orchestrator: `/workspace/lic/docs/ecosystem/orchestrator-notes/2026-06-07-orch-r4-ui-ux-signals-d6e61568.md`
- Registry: `/workspace/lic/data/swarm-gap-registry/registry.yaml`
- Grade: `/workspace/benchmarks/data/latest/ecosystem-quality-report.json`
