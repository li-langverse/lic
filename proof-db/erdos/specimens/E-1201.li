# E-1201: Is it true that for every $\epsilon,\eta>0$ there exists a $k$ such that the density of $n$ for which\[P(n(n+1)\cdots(n+k))>n^{1-\epsilon}\]is at least $1-\eta$ (where $P(m)$ is the greatest prime divisor of $m$)?
# erdos_number: 1201
# erdos_status: open
# priority_tier: P2
# tags: number_theory, primes

def erdos_e1201_is_it_true_that_for_every_epsilon_eta_0() -> int
  requires true
  ensures result == 0  # open: E-1201
  decreases 0
=
  return 0


def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
