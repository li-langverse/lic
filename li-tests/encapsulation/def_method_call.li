type Counter = object
  public value: int

def Counter_bump(self: var Counter) -> unit
  requires true
  ensures true
  decreases 0
=
  self.value = self.value + 1

def main() -> int
  requires true
  ensures result == 1
  decreases 0
=
  var c: Counter
  c.value = 0
  c.bump()
  return c.value
