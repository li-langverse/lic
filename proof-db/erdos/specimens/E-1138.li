# E-1138: Let $x/2<y<x$ and $C>1$. If $d=\max_{p_n<x} (p_{n+1}-p_n)$, where $p_n$ denotes the $n$th prime, then is it true that\[\pi(y+Cd)-\pi(y)\sim\frac{Cd}{\log y}?\]
# erdos_number: 1138
# erdos_status: open
# priority_tier: P2
# tags: number_theory, primes

def erdos_e1138_let_x_2_y_x_and_c_1_if_d_max_p_n_x_p_n_1() -> int
  requires true
  ensures result == 0  # open: E-1138
  decreases 0
=
  return 0

extern proc erdos_e1138_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
