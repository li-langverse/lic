# Composable gate: import package lib API from another module (not exec-only binary).
import li_httpd

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  let cfg = HttpdConfig(port=9090)
  let h = httpd_serve(cfg)
  discard httpd_ready(h)
  httpd_stop(h)
  return httpd_tag()
