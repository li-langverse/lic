# E-789: Let $h(n)$ be maximal such that if $A\subseteq \mathbb{Z}$ with $\lvert A\rvert=n$ then there is $B\subseteq A$ with $\lvert B\rvert \geq h(n)$ such that if $a_1+\cdots+a_r=b_1+\cdots+b_s$ with $a_i,b_i\in B$ then $r=s$. Estimate $h(n)$.
# erdos_number: 789
# erdos_status: open
# priority_tier: P2
# tags: additive_combinatorics

def erdos_e789_let_h_n_be_maximal_such_that_if_a_subset() -> int
  requires true
  ensures result == 0  # open: E-789
  decreases 0
=
  return 0

extern proc erdos_e789_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
