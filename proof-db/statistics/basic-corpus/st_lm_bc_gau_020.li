# ST-LM-BC-GAU-020: Normal distribution density stub (case 20).

def normal_density_scale(sigma: float) -> float
  requires sigma > 0.0
  ensures result > 0.0
  decreases 0
=
  return 1.0 / sigma

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
