# E-490: Let $A,B\subseteq \{1,\ldots,N\}$ be such that all the products $ab$ with $a\in A$ and $b\in B$ are distinct. Is it true that\[\lvert A\rvert \lvert B\rvert \ll \frac{N^2}{\log N}?\]
# erdos_number: 490
# erdos_status: proved
# priority_tier: P2
# tags: number_theory

def erdos_e490_let_a_b_subseteq_1_ldots_n_be_such_that() -> int
  requires true
  ensures result == 0  # open: E-490
  decreases 0
=
  return 0


def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
