# Swarm gap orchestration under a SOTA-papers lens

**Goal:** `swarm_coverage`  
**Dimension:** `sota-papers`  
**Agent:** `swarm_observer`  
**Run:** `swarm_observer-1780970665278`  
**Worker:** `6d2684ba`  
**Generated:** 2026-06-09T02:42Z  
**north_star_fit:** ecosystem, ai — proof → easy → fast

## Abstract

This whitepaper documents how the Li swarm **gap registry** maps state-of-the-art (SOTA) scientific-computing literature and competitor libraries into orchestrated backlogs — without implementing product code in the observer pass. The registry is the citation index; `swarm-gap-ingest.py` and `swarm-gap-apply-actions.py` are the deterministic apply pipeline; research agents (`numerics_researcher`, `gap_explorer`) consume paper-backed rows.

## Method

1. Read `lic/data/swarm-gap-registry/registry.yaml` and `benchmarks/data/latest/swarm-gap-actions.json`.
2. Classify open gaps by `gap_kind` and extract explicit SOTA references from `evidence` fields.
3. Compare live ecosystem audit (0 benchmark reds) vs registry competitor rows (30 open) for staleness.
4. Verify apply pipeline after remediating ingest blockers (syntax, env, PyYAML).

## Findings

### Registry composition (post-ingest @ 02:42Z)

| Kind | Open | Primary discoverer | SOTA relevance |
|------|------|-------------------|----------------|
| `competitor_feature` | 30 | `gap_explorer` | HPC libraries, tier-1 bench classes, vertical stubs |
| `plan_debt` | 31 | `plan_verifier` | Master-plan phases; sim/security/studio todos |
| `missing_package` | 1 | `gap_explorer` | `li-line-profiler` tooling gap |

### Paper- and library-backed rows (sample)

The registry encodes SOTA signals as structured evidence, enabling research agents to prioritize literature review before implementation:

- **Kokkos execution model** (`gap-hpc-kokkos-execution-memory-spaces`) — parallel abstractions, GPU memory spaces; PH-7e.
- **PETSc Kokkos-native solvers** (`gap-hpc-petsc-kokkos-implicit-pde`) — implicit PDE stack; PH-5b.
- **FFTW roofline** (`gap-hpc-fftw-roofline-catalog-row`) — arxiv 2506.08653 GPU FFT comparison methodology.
- **RAJA policies** (`gap-hpc-raja-execution-policies`) — arxiv 2402.08950 policy-based loops vs Li execution decorators.
- **OpenMP lowering rubric** (`gap-hpc-openmp-llvm-lowering-rubric`) — prescriptive vs descriptive OpenMP (OSTI 2224192).
- **Chapel HPSF** (`gap-competitor-chapel-hpsf-productivity`) — productivity parallel language vs PH-7d.
- **SUNDIALS stiff ODE** (`gap-hpc-sundials-stiff-ode-sensitivity`) — stiff integrator + sensitivity class.

### Orchestration hygiene

- **`chem-r0-sota-survey`** — closed in snapshot; demonstrates completed SOTA survey loop for chemistry vertical.
- **`verticals.toml` missing on main** — blocks stub honesty ingest; docs_maintainer + benchmarks PR required.
- **Retired systemd loops** — `suggested_new_loops` in actions JSON must not spawn new plan loops; route via `config/research-goals.yaml` per `docs/ecosystem/swarm-architecture.md`.

### Agentic AI / swarm meta-orchestration (ai domain)

For the `ai` pillar of `north_star_fit`, gap orchestration itself follows multi-agent patterns described in recent agentic-system literature: a **meta-agent** (`swarm_observer`) audits a programmatic observer (`src/observer/`), reconciles registries, and hands off to specialists without merging governance PRs. Key invariant: **proof-before-perf** — competitor gaps cite PH ids (5b, 7d, 7e) and defer `trusted.lean` to humans.

## Recommendations

1. Merge `competitive/verticals.toml` to benchmarks main — unlocks vertical stub ingest.
2. Dispatch `numerics_researcher` on HPC library gaps with explicit arxiv/OSTI citations.
3. Dispatch `gap_explorer` after verticals land — refresh 30 competitor rows against live audit (0 reds).
4. Bake `python3-yaml` in worker image — deterministic ingest without apt.
5. Publish this whitepaper to `research-findings/whitepapers/2026-06/swarm_coverage/sota-papers/` when repo is mounted.

## Evidence paths

- `/workspace/lic/data/swarm-gap-registry/registry.yaml`
- `/workspace/benchmarks/data/latest/swarm-gap-actions.json`
- `/workspace/benchmarks/data/latest/ecosystem-quality-report.json`
- `/workspace/benchmarks/data/latest/agent-briefing.json`
- `/workspace/lic/docs/ecosystem/orchestrator-notes/2026-06-09-orch-sota-papers-6d2684ba.md`
- `/app/data/runs/swarm_observer-1780970665278.md`

## Validity

| Criterion | Grade | Note |
|-----------|-------|------|
| Reproducibility | B | Ingest/apply scripts deterministic post-fix |
| Freshness | C | Snapshot 2026-05-30; briefing 2026-06-09 |
| Coverage | B | 30 competitor rows audited; not exhaustive of all verticals |
| Actionability | A | Handoffs enumerated per gap_kind taxonomy |

**Status:** active (staging in lic; publish to research-findings pending mount)
