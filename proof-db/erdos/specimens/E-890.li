# E-890: If $\omega_k(n)$ counts the number of distinct prime factors of $n$ which are $>k$, then is it true that, for every $k\geq 1$,\[\liminf_{n\to \infty}\sum_{0\leq i<k}\omega_k(n+i)\leq k?\]Is it true that\[\limsup_{n\to \infty}\left(\sum_{0\leq i<k}\omega(n+i)\right) \frac{\log\log n}{\log n}=1,\]where $\omega$ counts the number of distinct prime factors without restriction?
# erdos_number: 890
# erdos_status: open
# priority_tier: P2
# tags: number_theory, primes

def erdos_e890_if_omega_k_n_counts_the_number_of_distin() -> int
  requires true
  ensures result == 0  # open: E-890
  decreases 0
=
  return 0


def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
