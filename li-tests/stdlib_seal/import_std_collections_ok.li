import std.collections
import std.heap
import std.algorithms

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  var tag: int = collections_std_tag()
  tag = tag + heap_std_tag()
  tag = tag + algorithms_std_tag()
  if tag != 63:
    return 1
  return 0
