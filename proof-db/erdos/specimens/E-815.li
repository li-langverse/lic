# E-815: Let $k\geq 3$ and $n$ be sufficiently large. Is it true that if $G$ is a graph with $n$ vertices and $2n-2$ edges such that every proper induced subgraph has minimum degree $\leq 2$ then $G$ must contain a copy of $C_k$?
# erdos_number: 815
# erdos_status: open
# priority_tier: P2
# tags: graph_theory

def erdos_e815_let_k_geq_3_and_n_be_sufficiently_large() -> int
  requires true
  ensures result == 0  # open: E-815
  decreases 0
=
  return 0


def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
