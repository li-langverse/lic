# E-1213: Let $a,K\geq 1$. Does there exist $f(a,K)$ such that if\[a=a_1<\cdots <a_s\]is a sequence of integers with $a_s> f(a,K)$ and with bounded gaps $a_{i+1}-a_i\leq K$ then there are two distinct intervals $I$ and $J$ such that\[\sum_{i\in I}a_i=\sum_{j\in J}a_j?\]
# erdos_number: 1213
# erdos_status: proved
# priority_tier: P2
# tags: additive_combinatorics

def erdos_e1213_let_a_k_geq_1_does_there_exist_f_a_k_suc() -> int
  requires true
  ensures result == 0  # open: E-1213
  decreases 0
=
  return 0


def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
