# Swarm gap orchestration — performance lens

**Goal:** `swarm_coverage`  
**Dimension:** `performance`  
**Worker:** `7b620e59`  
**Date:** 2026-06-07  
**Publish target:** `research-findings/whitepapers/2026-06/swarm_coverage/performance/`

---

## Abstract

This pass audits whether the Li agent swarm can **orchestrate performance gaps** (benchmark posture, plan_debt sim todos, competitor_feature registry rows) without human intervention. Finding: **orchestration is degraded** — gap ingest/apply scripts fail in the org-research worker, control-plane observer state is absent, and the ecosystem scorecard reads **D (63.9)** with `unattended_safe: false`. Performance-specific backlog routing is documented; closure requires proved product work via existing research goals.

---

## Method

1. Regenerated `ecosystem-quality-report.json` via `benchmarks/scripts/ecosystem-quality-grade.py`.
2. Read `lic/data/swarm-gap-registry/registry.yaml` and `benchmarks/data/latest/swarm-gap-actions.json`.
3. Attempted `lic/scripts/swarm-gap-ingest.py` + `swarm-gap-apply-actions.py`.
4. Cross-checked `ecosystem-audit.json` benchmark yellow/near-threshold rows against open registry gaps.
5. Compared briefing `recommended_agents` vs scorecard dispatch list.

---

## Performance posture (2026-06-07)

| Metric | Value | Evidence |
|---|---|---|
| Tier-1 green | 145 | `ecosystem-audit.json` |
| Yellow | `num_eig_symmetric`, `num_root_newton` | same |
| Near-threshold (1.18–1.20× vs C++) | `num_opt_bfgs`, `num_integ_semi_implicit`, `num_integ_euler`, `num_integ_rk4`, `num_cg` | same |
| Open `competitor_feature` (perf-class) | 30 | `registry.yaml` / `swarm-gap-actions.json` |
| Open plan_debt (sim perf) | `sim-p1-num-dot-axpy`, chem/md research todos | `registry.yaml` |

Benchmark data generated **2026-06-01** — stale for unattended claims; refresh cadence should follow `ecosystem_audit` preflight.

---

## Orchestration health vs performance work

| Blocker | Impact on perf gap closure |
|---|---|
| Ingest SyntaxError (`swarm-gap-ingest.py:229`) | Cannot ingest vertical stubs or close plan_debt rows |
| Missing PyYAML | Cannot apply backlog patches |
| `runs_sampled=0` | Observer cannot verify `bench_improver` / researcher outcomes |
| 6 stopped goal runners | Sim/httpd/security research loops idle on host |

Until ingest/apply succeed, performance gaps remain **documented but not auto-routed** beyond static registry handoffs.

---

## Recommended routing (proof-before-perf)

| Work | Agent / goal | PH |
|---|---|---|
| Yellow eigen + Newton | `bench_improver` → `numerics_sota` | PH-7e |
| Near-threshold integrators | `numerics_researcher` | PH-5b |
| `sim-p1-num-dot-axpy` | sim backlog → implement lane | PH-5b |
| Tier-1 red stubs in registry | `bench_improver` + proved codegen | PH-7e |
| UI palette latency | `gui_ux_tester` → `ui_ux_quality` | — |

---

## Conclusion

Performance gap **inventory is rich** (registry + audit), but **orchestration plumbing is broken** in the current worker. Unattended perf improvement is **not safe** until: (1) ingest/apply scripts run cleanly, (2) control-plane persists observer state, (3) benchmark audit refreshes on cadence. No product code changes in `lic` from this meta pass.

---

## References

- `benchmarks/data/latest/ecosystem-quality-report.json`
- `benchmarks/data/latest/ecosystem-audit.json`
- `lic/data/swarm-gap-registry/registry.yaml`
- `lic/docs/ecosystem/orchestrator-notes/2026-06-07-orch-r5-performance-gap-orchestration-7b620e59.md`
- `docs/ecosystem/research-verticals.md`
