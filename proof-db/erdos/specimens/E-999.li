# E-999: For any function $f:\mathbb{N}\to \mathbb{N}$ the property that, for almost all $\alpha$\[\left\lvert \alpha-\frac{p}{q}\right\rvert < \frac{f(q)}{q}\]has infinitely many solutions with $(p,q)=1$, is equivalent to\[\sum_{q\geq 1}\phi(q)\frac{f(q)}{q}=\infty.\]
# erdos_number: 999
# erdos_status: proved
# priority_tier: P2
# tags: diophantine_approximation, number_theory

def erdos_e999_for_any_function_f_mathbb_n_to_mathbb_n() -> int
  requires true
  ensures result == 0  # open: E-999
  decreases 0
=
  return 0


def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
