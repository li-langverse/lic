# E-997: Call $x_1,x_2,\ldots \in (0,1)$ well-distributed if, for every $\epsilon>0$, if $k$ is sufficiently large then, for all $n>0$ and intervals $I\subseteq [0,1]$,\[\lvert \# \{ n<m\leq n+k : x_m\in I\} - \lvert I\rvert k\rvert < \epsilon k.\]Is it true that, for every $\alpha$, the sequence $\{ \alpha p_n\}$ is not well-distributed, if $p_n$ is the sequence of primes?
# erdos_number: 997
# erdos_status: proved
# priority_tier: P2
# tags: analysis, discrepancy, primes

def erdos_e997_call_x_1_x_2_ldots_in_0_1_well_distribut() -> int
  requires true
  ensures result == 0  # open: E-997
  decreases 0
=
  return 0


def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
