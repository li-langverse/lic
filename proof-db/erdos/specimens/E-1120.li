# E-1120: Let $f\in \mathbb{C}[z]$ be a monic polynomial of degree $n$, all of whose roots satisfy $\lvert z\rvert\leq 1$. Let\[E= \{ z : \lvert f(z)\rvert \leq 1\}.\]What is the shortest length of a path in $E$ joining $z=0$ to $\lvert z\rvert =1$?
# erdos_number: 1120
# erdos_status: open
# priority_tier: P2
# tags: analysis

def erdos_e1120_let_f_in_mathbb_c_z_be_a_monic_polynomia() -> int
  requires true
  ensures result == 0  # open: E-1120
  decreases 0
=
  return 0

extern proc erdos_e1120_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
