# E-56: Let $N\geq p_k$ where $p_k$ is the $k$th prime. Suppose $A\subseteq \{1,\ldots,N\}$ is such that there are no $k+1$ elements of $A$ which are relatively prime. An example is the set of all multiples of the first $k$ primes. Is this the largest such set?
# erdos_number: 56
# erdos_status: open
# priority_tier: P2
# tags: intersecting_family, number_theory

def erdos_e56_let_n_geq_p_k_where_p_k_is_the_k_th_prim() -> int
  requires true
  ensures result == 0  # open: E-56
  decreases 0
=
  return 0


def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
