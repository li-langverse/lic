# E-10: Does there exist a set A of positive integers with sum_{n in A} 1/n = infinity such that A contains no three-term arithmetic progression?
# erdos_number: 10
# erdos_status: open
# priority_tier: P1
# tags: number_theory, additive_combinatorics, arithmetic_progressions
# context: Sets with divergent harmonic sum but no long arithmetic progressions.
# context: A cousin of Erdős–Turán conjectures; ties to Szemerédi-type obstructions..

def erdos_e10_does_there_exist_a_set_a_of_positive_int() -> int
  requires true
  ensures result == 0  # open: E-10
  decreases 0
=
  return 0


def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
