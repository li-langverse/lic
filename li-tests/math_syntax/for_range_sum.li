# G-math-syn: plain `for i in start..<end` (no Python `range()` helper yet).
def main() -> int
  requires true
  ensures result == 6
  decreases 0
=
  var acc: int = 0
  for i in 0..<4
    acc = acc + i
  return acc
