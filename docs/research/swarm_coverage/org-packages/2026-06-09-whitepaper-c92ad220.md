# Swarm gap orchestration — org-packages dimension

**Goal:** `swarm_coverage`  
**Dimension:** `org-packages`  
**Worker:** `c92ad220`  
**Date:** 2026-06-09  
**north_star_fit:** ecosystem, ai — whole-org package/repo audit before perf shortcuts (PH-IO, agent tooling)  
**Publish target:** `research-findings/whitepapers/2026-06/swarm_coverage/org-packages/`

---

## Abstract

This pass audits whether the Li agent swarm correctly discovers, registers, and routes **org-wide package gaps** — missing std modules, seed packages, and repos without CI — through the gap registry and `ecosystem-package-backlog.md`. The swarm is **degraded** (grade D); org-package orchestration is **partially complete** (four PH-IO std modules shipped) but **cannot refresh** because PyYAML-dependent ingest is blocked and ten of twelve “missing CI” repos return GitHub 404.

---

## 1. Missing-package registry posture

**Sources:** `/workspace/lic/data/swarm-gap-registry/registry.yaml`, `/workspace/lic/docs/ecosystem/ecosystem-package-backlog.md`, `/workspace/benchmarks/data/latest/swarm-gap-actions.json`

| Gap id | Module / package | Registry | Backlog | Handoff |
|--------|------------------|----------|---------|---------|
| `gap-missing-std-std-io` | std.io | closed | completed | — |
| `gap-missing-std-std-csv` | std.csv | closed | completed | — |
| `gap-missing-std-std-summary` | std.summary | closed | completed | — |
| `gap-missing-std-std-plot` | std.plot | closed | completed | — |
| `gap-line-profiler-001` | li-line-profiler | **open** | **pending** | `issue_planner`, `package_architect` |

**Drift:** `swarm-gap-actions.json` (2026-05-31) still reports **3** open `missing_package` rows. Re-ingest after PyYAML will align actions with registry (expect **1** open).

---

## 2. Org repo / CI audit

**Sources:** `/workspace/benchmarks/data/latest/agent-briefing.json`, `/workspace/benchmarks/data/latest/org-repo-ci-audit.json`

| Signal | Count | Interpretation |
|--------|------:|----------------|
| `missing_ci_on_main` (briefing) | 12 | Includes planned/private repos |
| `repos_audit_incomplete` (404) | 10 | li-api-kit, li-books-*, li-os, li-research-*, lik, token-telemetry-service |
| `repos_ok` (CI present) | 52+ | Core org packages healthy |

**Recommendation:** `ci_maintainer` should gate scaffolding on successful `gh repo view`. Phantom 404 repos must not inflate `ecosystem_posture` score indefinitely — filter in `ensure-org-repo-ci.py`.

---

## 3. Cross-repo package holes (non-registry)

| Area | Signal | Route |
|------|--------|-------|
| Bench catalog | 114 harness-pending rows | `bench_improver` — not new org packages |
| Workload dirs | 21 missing tier paths | `docs_maintainer` + benchmarks PR |
| Agent kit | roadmap/agent-kit mount missing | `agent_kit_maintainer` |
| CWE catalog | 19 Top-25 missing | `security_auditor` — security package surface |

---

## 4. Apply pipeline status

Last successful apply: **2026-05-31** (`swarm-gap-actions.json`).

**This cycle (2026-06-09):**

```
swarm-gap-ingest: PyYAML required (pip install pyyaml)
swarm-gap-apply-actions: PyYAML required
```

Container lacks `python3-yaml`, `pip`, and apt package. Image-level fix required for unattended org-package gap closure.

---

## 5. Swarm health coupling

| Dimension | Score | Org-packages impact |
|-----------|------:|---------------------|
| `ecosystem_posture` | 67.0 | 12 missing CI + 4 failed PRs |
| `gap_pressure` | 60.0 | Stale 64-open count |
| `briefing_health` | 69.0 | org_ci + agent_kit preflight failures |
| Overall | **66.3 D** | `unattended_safe: false` |

---

## 6. Handoffs (proof → easy → fast)

1. **`issue_planner`** — open li-line-profiler seed issue (PH profiling for HPC agent loops).
2. **`package_architect`** — design li-line-profiler crate layout after issue approved.
3. **`ci_maintainer`** — CI for verified-live org packages only.
4. **`gap_explorer`** — refresh registry after PyYAML; close completed std module rows.
5. **`pr_merger`** — lip#52 (unblocks deploy-pages dependency chain).

No product implementation in this meta pass. No new systemd plan loops.

---

## References

- `lic/docs/ecosystem/ecosystem-package-backlog.md`
- `lic/data/swarm-gap-registry/registry.yaml`
- `benchmarks/data/latest/ecosystem-quality-report.json`
- `li-cursor-agents/docs/ecosystem/research-verticals.md` — `swarm_coverage` goal row
- Orchestrator note: `lic/docs/ecosystem/orchestrator-notes/2026-06-09-orch-org-packages-c92ad220.md`
