# E-125: Let $A = \{ \sum\epsilon_k3^k : \epsilon_k\in \{0,1\}\}$ be the set of integers which have only the digits $0,1$ when written base $3$, and $B=\{ \sum\epsilon_k4^k : \epsilon_k\in \{0,1\}\}$ be the set of integers which have only the digits $0,1$ when written base $4$. Does $A+B$ have positive lower density?
# erdos_number: 125
# erdos_status: open
# priority_tier: P2
# tags: base_representations, number_theory

def erdos_e125_let_a_sum_epsilon_k3_k_epsilon_k_in_0_1() -> int
  requires true
  ensures result == 0  # open: E-125
  decreases 0
=
  return 0

extern proc erdos_e125_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
