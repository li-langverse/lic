# M-AX-ORDER-ANTISYM: order antisymmetry (a ≤ b ∧ b ≤ a → a = b).

def proof_db_order_antisym(a: int, b: int)-> int
  requires a >= 0 and b >= 0
  ensures (a <= b and b <= a) implies (a == b)
  decreases 0
=
  return 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
