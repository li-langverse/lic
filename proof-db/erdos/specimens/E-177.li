# E-177: Find the smallest $h(d)$ such that the following holds. There exists a function $f:\mathbb{N}\to\{-1,1\}$ such that, for every $d\geq 1$,\[\max_{P_d}\left\lvert \sum_{n\in P_d}f(n)\right\rvert\leq h(d),\]where $P_d$ ranges over all finite arithmetic progressions with common difference $d$.
# erdos_number: 177
# erdos_status: open
# priority_tier: P2
# tags: arithmetic_progressions, discrepancy

def erdos_e177_find_the_smallest_h_d_such_that_the_foll() -> int
  requires true
  ensures result == 0  # open: E-177
  decreases 0
=
  return 0

extern proc erdos_e177_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
