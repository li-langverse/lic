# E-823: Let $\alpha\geq 1$. Is there a sequence of integers $n_k,m_k$ such that $n_k/m_k\to \alpha$ and $\sigma(n_k)=\sigma(m_k)$ for all $k\geq 1$, where $\sigma$ is the sum of divisors function?
# erdos_number: 823
# erdos_status: proved
# priority_tier: P2
# tags: number_theory

def erdos_e823_let_alpha_geq_1_is_there_a_sequence_of_i() -> int
  requires true
  ensures result == 0  # open: E-823
  decreases 0
=
  return 0


def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
