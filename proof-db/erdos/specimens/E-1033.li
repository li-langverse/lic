# E-1033: Let $h(n)$ be such that every graph on $n$ vertices with $>n^2/4$ many edges contains a triangle whose vertices have degrees summing to at least $h(n)$. Estimate $h(n)$. In particular, is it true that\[h(n)\geq (2(\sqrt{3}-1)-o(1))n?\]
# erdos_number: 1033
# erdos_status: open
# priority_tier: P2
# tags: graph_theory

def erdos_e1033_let_h_n_be_such_that_every_graph_on_n_ve() -> int
  requires true
  ensures result == 0  # open: E-1033
  decreases 0
=
  return 0


def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
