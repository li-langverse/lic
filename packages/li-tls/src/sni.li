# sni — multi-cert host routing (Phase 4 stub).

def tls_sni_enabled() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0

def tls_sni_selftest() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
