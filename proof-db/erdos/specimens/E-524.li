# E-524: For any $t\in (0,1)$ let $t=\sum_{k=1}^\infty \epsilon_k(t)2^{-k}$ (where $\epsilon_k(t)\in \{0,1\}$). What is the correct order of magnitude (for almost all $t\in(0,1)$) for\[M_n(t)=\max_{x\in [-1,1]}\left\lvert \sum_{k\leq n}(-1)^{\epsilon_k(t)}x^k\right\rvert?\]
# erdos_number: 524
# erdos_status: open
# priority_tier: P2
# tags: analysis, polynomials, probability

def erdos_e524_for_any_t_in_0_1_let_t_sum_k_1_infty_eps() -> int
  requires true
  ensures result == 0  # open: E-524
  decreases 0
=
  return 0

extern proc erdos_e524_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
