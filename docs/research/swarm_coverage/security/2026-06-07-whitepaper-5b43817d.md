# Swarm coverage — security dimension

**Goal:** `swarm_coverage`  
**Dimension:** security  
**Worker:** `5b43817d`  
**Generated:** 2026-06-07T12:14Z  
**Publish target:** `research-findings/whitepapers/2026-06/swarm_coverage/security/` (repo not mounted)

## Abstract

Security-lens audit of the Li agent swarm gap orchestration pipeline. The swarm is **degraded** for unattended operation: CWE catalog coverage is incomplete (19/25 Top-25 gaps), gap ingest/apply is blocked by missing PyYAML, and `CURSOR_API_KEY` is unset. Three security-research plan todos (`sec-r1`..`sec-r3`) remain open in the registry with backlog patches applied but no leaf-agent execution.

## north_star_fit

- **Domains:** ecosystem, ai  
- **Pillar:** secure (after provable, before perf)  
- **PH linkage:** tier5 httpd exploit gates (CWE-class parity vs nginx); no `trusted.lean` changes without human approval

## Security posture signals

| Signal | Value | Evidence |
|--------|-------|----------|
| CWE Top-25 in catalog | 6/25 represented | `security-cwe-feed.json` |
| Top-25 missing in catalog | 19 | `security-cwe-feed.json` → `top25_missing_in_catalog` |
| Security workflow gaps (org) | 0 repos missing | `agent-briefing.json` recommended_agents |
| Open security plan_debt gaps | 3 (`sec-r1`..`r3`) | `registry.yaml` |
| Failed PRs (security-adjacent) | lic#1014 governance | `agent-briefing.json` failed_prs |
| Ecosystem grade | D (64.3) | `ecosystem-quality-report.json` |

## Gap orchestration — security taxonomy

### plan_debt (security-research runner)

Backlog file: `docs/ecosystem/security-research-backlog.md`

| todo | status | swarm handoff |
|------|--------|---------------|
| sec-r0-cwe-delta | completed | — |
| sec-r1-httpd-fuzz-smoke | pending | `security_auditor` / `offensive_security` |
| sec-r2-tier5-gap-exploit | pending | `security_auditor` — human-gated exploit rows |
| sec-r3-runtime-surface | pending | `security_auditor` — runtime surface study |

Apply pipeline last patched these rows on 2026-05-31 (`swarm-gap-actions.json`); re-ingest blocked until PyYAML available.

### competitor_feature (indirect security)

- `gap-infra-verticals-toml-missing-benchmarks-main` — blocks honest stub ingest; affects security benchmark honesty
- httpd phase-2 gaps (`gap-phase2-mitigation-exploits` closed; perf/wrk-soak pending) — tier5 exploit regression gates

## Control-plane security findings

1. **SDK auth gap:** `CURSOR_API_KEY` unset — observer cannot auto-retry failed security_auditor runs.
2. **Gap script integrity:** `swarm-gap-ingest.py:229` had syntax error (unterminated string); fixed this run — prevents registry refresh attacks via broken ingest.
3. **Preflight blind spot:** `security_cwe_audit` skipped (`--skip-slow`); CWE feed sync ran but full audit did not.
4. **Human-only:** lic#1014 governance PR — must not auto-merge; PH-Pkg exit gates.

## Recommendations

1. Dispatch `security_auditor` on `offensive_security` goal for `sec-r1-httpd-fuzz-smoke`.
2. Human-gated issue for CWE Top-25 catalog expansion (19 rows) — `lic/security/cve-catalog.json`.
3. Bake `python3-yaml` in org-research worker; set `LI_CURSOR_AGENTS_ROOT=/app`.
4. Merge lip#52 (deploy-pages dep) via `pr_merger` — low risk, unblocks CI hygiene.

## References

- `/workspace/benchmarks/data/latest/ecosystem-quality-report.json`
- `/workspace/benchmarks/data/latest/security-cwe-feed.json`
- `/workspace/lic/data/swarm-gap-registry/registry.yaml`
- `/workspace/benchmarks/data/latest/swarm-gap-actions.json`
- `/workspace/lic/docs/ecosystem/orchestrator-notes/2026-06-07-orch-r8-security-gap-handoffs-5b43817d.md`
