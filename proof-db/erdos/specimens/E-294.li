# E-294: Let $N\geq 1$ and let $t(N)$ be the least integer $t$ such that there is no solution to\[1=\frac{1}{n_1}+\cdots+\frac{1}{n_k}\]with $t=n_1<\cdots <n_k\leq N$. Estimate $t(N)$.
# erdos_number: 294
# erdos_status: proved
# priority_tier: P2
# tags: number_theory, unit_fractions

def erdos_e294_let_n_geq_1_and_let_t_n_be_the_least_int() -> int
  requires true
  ensures result == 0  # open: E-294
  decreases 0
=
  return 0

extern proc erdos_e294_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
