# E-932: Let $p_k$ denote the $k$th prime. For infinitely many $r$ there are at least two integers $p_r<n<p_{r+1}$ all of whose prime factors are $<p_{r+1}-p_r$.
# erdos_number: 932
# erdos_status: open
# priority_tier: P2
# tags: number_theory

def erdos_e932_let_p_k_denote_the_k_th_prime_for_infini() -> int
  requires true
  ensures result == 0  # open: E-932
  decreases 0
=
  return 0


def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
