# Catalog: E-226
# Statement: Is there an entire non-linear function $f$ such that, for all $x\in\mathbb{R}$, $x$ is rational if and only if $f(x)$ is?
# formalization_status: li_open
# discharge: pending — open formalization target (Phase 4)
# erdos_status (literature): proved

def formal_target_e_226() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
