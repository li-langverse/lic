# WP-GPU-05 Partial: user decorator names cannot use reserved `gpu_` prefix.

decorator def gpu_custom(x: int) -> int
  requires true
  ensures result == x
  decreases 0
=
  return x

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
