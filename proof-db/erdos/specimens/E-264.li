# E-264: Let $a_n$ be a sequence of positive integers such that for every bounded sequence of integers $b_n$ (with $a_n+b_n\neq 0$ and $b_n\neq 0$ for all $n$) the sum\[\sum \frac{1}{a_n+b_n}\]is irrational. Are $a_n=2^n$ or $a_n=n!$ examples of such a sequence?
# erdos_number: 264
# erdos_status: open
# priority_tier: P2
# tags: irrationality

def erdos_e264_let_a_n_be_a_sequence_of_positive_intege() -> int
  requires true
  ensures result == 0  # open: E-264
  decreases 0
=
  return 0


def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
