# E-1029: If $R(k)$ is the Ramsey number for $K_k$, the minimal $n$ such that every $2$-colouring of the edges of $K_n$ contains a monochromatic copy of $K_k$, then\[\frac{R(k)}{k2^{k/2}}\to \infty.\]
# erdos_number: 1029
# erdos_status: open
# priority_tier: P2
# tags: graph_theory, ramsey_theory

def erdos_e1029_if_r_k_is_the_ramsey_number_for_k_k_the() -> int
  requires true
  ensures result == 0  # open: E-1029
  decreases 0
=
  return 0


def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
