# D-LM-BC-NT-013: gcd-lcm relation: gcd(a,b) * lcm(a,b) = |a b| for integers a,b.

def gcd_divides(a: int, b: int, g: int) -> int
  requires a >= 0 and b >= 0 and g >= 0
  ensures result == g
  decreases 0
=
  return g

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
