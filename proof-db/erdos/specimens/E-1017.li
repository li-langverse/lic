# E-1017: Let $f(n,k)$ be such that every graph on $n$ vertices and $k$ edges can be partitioned into at most $f(n,k)$ edge-disjoint complete graphs. Estimate $f(n,k)$ for $k>n^2/4$.
# erdos_number: 1017
# erdos_status: open
# priority_tier: P2
# tags: graph_theory

def erdos_e1017_let_f_n_k_be_such_that_every_graph_on_n() -> int
  requires true
  ensures result == 0  # open: E-1017
  decreases 0
=
  return 0


def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
