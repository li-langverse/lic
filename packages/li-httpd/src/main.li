# Thin demo — composable API lives in lib.li (import li_httpd from other programs).

import li_httpd

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  let cfg = HttpdConfig(port=8080)
  let h = httpd_serve(cfg)
  discard httpd_ready(h)
  httpd_stop(h)
  return httpd_tag()
