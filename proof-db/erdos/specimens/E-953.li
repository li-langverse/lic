# E-953: Let $A\subset \{ x\in \mathbb{R}^2 : \lvert x\rvert <r\}$ be a measurable set with no integer distances, that is, such that $\lvert a-b\rvert \not\in \mathbb{Z}$ for any distinct $a,b\in A$. How large can the measure of $A$ be?
# erdos_number: 953
# erdos_status: open
# priority_tier: P2
# tags: distances, geometry

def erdos_e953_let_a_subset_x_in_mathbb_r_2_lvert_x_rve() -> int
  requires true
  ensures result == 0  # open: E-953
  decreases 0
=
  return 0

extern proc erdos_e953_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
