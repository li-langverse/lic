# E-342: With $a_1=1$ and $a_2=2$ let $a_{n+1}$ for $n\geq 2$ be the least integer $>a_n$ which can be expressed uniquely as $a_i+a_j$ for $i<j\leq n$. What can be said about this sequence? Do infinitely many pairs $a,a+2$ occur? Does this sequence eventually have periodic differences? Is the density $0$?
# erdos_number: 342
# erdos_status: open
# priority_tier: P2
# tags: number_theory

def erdos_e342_with_a_1_1_and_a_2_2_let_a_n_1_for_n_geq() -> int
  requires true
  ensures result == 0  # open: E-342
  decreases 0
=
  return 0

extern proc erdos_e342_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
