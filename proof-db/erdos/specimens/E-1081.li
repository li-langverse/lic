# E-1081: Let $A(x)$ count the number of $n\leq x$ which are the sum of two squarefull numbers (a number $m$ is squarefull if $p\mid m$ implies $p^2\mid m$). Is it true that\[A(x) \sim c \frac{x}{\sqrt{\log x}}\]for some $c>0$?
# erdos_number: 1081
# erdos_status: open
# priority_tier: P2
# tags: number_theory, powerful

def erdos_e1081_let_a_x_count_the_number_of_n_leq_x_whic() -> int
  requires true
  ensures result == 0  # open: E-1081
  decreases 0
=
  return 0


def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
