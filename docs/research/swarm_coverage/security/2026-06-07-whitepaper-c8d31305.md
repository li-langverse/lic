# Swarm gap orchestration — security dimension

**Goal id:** `swarm_coverage`  
**Dimension:** `security`  
**Worker:** `c8d31305`  
**Generated:** 2026-06-07T19:05Z  
**Publish target:** `research-findings/whitepapers/2026-06/swarm_coverage/security/`

## Abstract

This whitepaper audits Li swarm health through a **security lens**: CWE catalog coverage, security-research plan debt, and control-plane self-healing for gap orchestration. The swarm is **degraded but recoverable** — orchestration blockers (gap ingest syntax, missing PyYAML in worker image, grader runs_dir mismatch) prevent unattended operation despite zero agent error rate in the current worker.

## Security findings

### CWE Top-25 catalog gap

| Signal | Value | Evidence |
|--------|-------|----------|
| Catalog CWE rows | 15 | `lic/security/cve-catalog.json` |
| Top-25 missing | 19 | `benchmarks/data/latest/security-cwe-feed.json` |
| New vs previous sync | 0 | Feed sync 2026-06-07T19:01Z |

Missing CWE classes include injection (CWE-79, CWE-89), path traversal (CWE-22), SSRF (CWE-918), and hardcoded credentials (CWE-798). These require **human-gated** catalog expansion — not auto-merge.

### Security-research plan debt

Three open registry rows map to `security-research-backlog.md`:

1. **sec-r1-httpd-fuzz-smoke** — httpd fuzz smoke gate
2. **sec-r2-tier5-gap-exploit** — tier5 exploit parity closure
3. **sec-r3-runtime-surface** — runtime attack surface survey

Gap apply patched all three to `pending` status. Dispatch via `offensive_security` goal → `security_auditor`.

### Swarm security orchestration health

| Control | Status | Risk |
|---------|--------|------|
| Gap ingest | Remediated (Path fallback) | Medium — fix not merged |
| Gap apply | Working (ephemeral PyYAML) | High — image lacks yaml |
| Control-plane persistence | Bootstrapped this run | High — not supervisor-owned |
| `security_cwe_audit` preflight | Skipped | Medium — stale audit signal |
| GitHub API (org_ci_audit) | Rate limited (403) | High — blocks CI audit |

## Recommendations

1. Merge `swarm-gap-ingest.py` Path/env fallback fix to `lic` main
2. Bake `python3-yaml` in org-research worker container image
3. Set `LI_CURSOR_AGENTS_ROOT=/app` in worker env for grader runs sampling
4. Human review: CWE Top-25 catalog backfill (19 rows)
5. Dispatch `security_auditor` for `sec-r1-httpd-fuzz-smoke` after `pr_merger` + `ci_maintainer`

## north_star_fit

- **Domains:** ecosystem, ai
- **Pillar:** secure (org roadmap pillar 3)
- **Proof-before-perf:** security gates (tier5 exploits, fuzz smoke) defer perf work until exploit parity documented
