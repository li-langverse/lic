import lig
def main() raises IO -> int
  requires true
  ensures result == 0
  decreases 0
=
  var rc: int = lig_kernel_run(lig_kernel_matmul_f32(), lig_backend_select_auto())
  if rc != 0:
    return rc
  if lig_validity_gate_pass(0.999) != 1:
    return 2
  return 0
