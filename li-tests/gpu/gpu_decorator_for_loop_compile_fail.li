# WP-GPU-05 Partial: `@gpu` on `for` rejected (grid-stride kernels are Phase 2).

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  var s: int = 0
  @gpu
  for i in 0..<4:
    s = s + i
  return s
