# E-1143: Let $p_1<\cdots<p_u$ be primes and let $k\geq 1$. Let $F_k(p_1,\ldots,p_u)$ be such that every interval of $k$ positive integers contains at least $F_k(p_1,\ldots,p_u)$ multiples of at least one of the $p_i$. Estimate $F_k(p_1,\ldots,p_u)$, particularly in the range $k=\alpha p_u$ for constant $\alpha>2$.
# erdos_number: 1143
# erdos_status: open
# priority_tier: P2
# tags: number_theory, primes

def erdos_e1143_let_p_1_cdots_p_u_be_primes_and_let_k_ge() -> int
  requires true
  ensures result == 0  # open: E-1143
  decreases 0
=
  return 0

extern proc erdos_e1143_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
