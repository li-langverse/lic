# T-PKG-lig-smoke-kernel — matmul pilot via import lig

import lig

def main() -> int raises IO
  requires true
  ensures result == 0
  decreases 0
=
  if lig_kernel_run_auto(lig_kernel_matmul_f32()) != 0:
    return 1
  if lig_validity_gate_pass(0.999) != 1:
    return 2
  return 0
