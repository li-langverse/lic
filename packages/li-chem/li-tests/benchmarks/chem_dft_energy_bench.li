# Competitive bench: time chem_dft_energy_kernel_hartree() loop.
import chem

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  var i: int = 0
  while i < 64
    var e: float = chem_dft_energy_kernel_hartree()
    if e >= 0.0:
      return 1
    i = i + 1
  return 0
