# E-613: Let $n\geq 3$ and $G$ be a graph with $\binom{2n+1}{2}-\binom{n}{2}-1$ edges. Must $G$ be the union of a bipartite graph and a graph with maximum degree less than $n$?
# erdos_number: 613
# erdos_status: open
# priority_tier: P2
# tags: graph_theory, ramsey

def erdos_e613_let_n_geq_3_and_g_be_a_graph_with_binom() -> int
  requires true
  ensures result == 0  # open: E-613
  decreases 0
=
  return 0


def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
