# E-975: Let $f\in \mathbb{Z}[x]$ be an irreducible non-constant polynomial such that $f(n)\geq 1$ for all large $n\in\mathbb{N}$. Does there exist a constant $c=c(f)>0$ such that\[\sum_{n\leq X} \tau(f(n))\sim cX\log X,\]where $\tau$ is the divisor function?
# erdos_number: 975
# erdos_status: open
# priority_tier: P2
# tags: divisors, number_theory, polynomials

def erdos_e975_let_f_in_mathbb_z_x_be_an_irreducible_no() -> int
  requires true
  ensures result == 0  # open: E-975
  decreases 0
=
  return 0


def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
