# E-748: Let $f(n)$ count the number of sum-free $A\subseteq \{1,\ldots,n\}$, i.e. $A$ contains no solutions to $a=b+c$ with $a,b,c\in A$. Is it true that\[f(n)=2^{(1+o(1))\frac{n}{2}}?\]
# erdos_number: 748
# erdos_status: proved
# priority_tier: P2
# tags: number_theory

def erdos_e748_let_f_n_count_the_number_of_sum_free_a_s() -> int
  requires true
  ensures result == 0  # open: E-748
  decreases 0
=
  return 0

extern proc erdos_e748_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
