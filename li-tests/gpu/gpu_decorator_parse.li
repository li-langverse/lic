# WP-GPU-04 stub: @gpu parse smoke (MIR lowering blocked — G-gpu Partial).

@gpu
def gpu_add_one(x: float) -> float
  requires true
  ensures result >= x
  decreases 0
=
  return x + 1.0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  if gpu_add_one(1.0) < 1.0:
    return 1
  return 0
