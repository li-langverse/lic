# Spin-up: scientific / engineering viz (PH-SCI).
import studio
import sim.scientific

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  if studio_native_viewport_bridge_smoke() != 0:
    return 1
  return scientific_drug_stack_smoke()
