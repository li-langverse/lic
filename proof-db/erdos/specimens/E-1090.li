# E-1090: Let $k\geq 3$. Does there exist a finite set $A\subset \mathbb{R}^2$ such that, in any $2$-colouring of $A$, there exists a line which contains at least $k$ points from $A$, and all the points of $A$ on the line have the same colour?
# erdos_number: 1090
# erdos_status: proved
# priority_tier: P2
# tags: geometry, ramsey_theory

def erdos_e1090_let_k_geq_3_does_there_exist_a_finite_se() -> int
  requires true
  ensures result == 0  # open: E-1090
  decreases 0
=
  return 0


def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
