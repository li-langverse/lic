# E-977: If $P(m)$ is the greatest prime divisor of $m$, then is it true that\[\frac{P(2^n-1)}{n}\to \infty\]as $n\to \infty$?
# erdos_number: 977
# erdos_status: proved
# priority_tier: P2
# tags: number_theory

def erdos_e977_if_p_m_is_the_greatest_prime_divisor_of() -> int
  requires true
  ensures result == 0  # open: E-977
  decreases 0
=
  return 0

extern proc erdos_e977_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
