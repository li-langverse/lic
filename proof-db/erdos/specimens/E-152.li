# E-152: For any $M\geq 1$, if $A\subset \mathbb{N}$ is a sufficiently large finite Sidon set then there are at least $M$ many $a\in A+A$ such that $a+1,a-1\not\in A+A$.
# erdos_number: 152
# erdos_status: proved
# priority_tier: P2
# tags: sidon_sets

def erdos_e152_for_any_m_geq_1_if_a_subset_mathbb_n_is() -> int
  requires true
  ensures result == 0  # open: E-152
  decreases 0
=
  return 0

extern proc erdos_e152_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
