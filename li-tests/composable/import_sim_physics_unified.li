# Unified bridge: physics.runtime sim_step_physics + sim.step_physics stub (separate packages).
import physics.runtime
import sim

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  if sim_step_physics(0, 2, 16) != 1:
    return 1
  if step_physics(0, 2, 16) != 1:
    return 2
  return 0
