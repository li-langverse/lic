# E-30: Let $h(N)$ be the maximum size of a Sidon set in $\{1,\ldots,N\}$. Is it true that, for every $\epsilon>0$,\[h(N) = N^{1/2}+O_\epsilon(N^\epsilon)?\]
# erdos_number: 30
# erdos_status: open
# priority_tier: P2
# tags: additive_combinatorics, number_theory, sidon_sets

def erdos_e30_let_h_n_be_the_maximum_size_of_a_sidon_s() -> int
  requires true
  ensures result == 0  # open: E-30
  decreases 0
=
  return 0


def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
