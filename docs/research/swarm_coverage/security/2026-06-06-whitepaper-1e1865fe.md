# Swarm gap orchestration — security dimension

**Goal id:** `swarm_coverage`  
**Dimension:** `security`  
**Worker:** `1e1865fe`  
**Agent:** `swarm_observer`  
**Generated:** 2026-06-06T21:24:00Z  
**north_star_fit:** ecosystem, ai — provable secure runtime before perf shortcuts

**Publish target:** `research-findings/whitepapers/2026-06/swarm_coverage/security/` (staging only — repo not mounted)

---

## Abstract

This pass audits swarm health through a **security lens**: whether gap orchestration reliably routes offensive-security plan debt, whether CWE catalog coverage matches briefing signals, and whether control-plane self-heal can run unattended. The ecosystem grade is **D (60.9)** with `unattended_safe: false`. Three security-research plan todos remain open; nineteen MITRE Top 25 CWE classes are absent from `cve-catalog.json`. Gap ingest/apply remains blocked by missing PyYAML despite a remediated syntax error in `swarm-gap-ingest.py`.

---

## Findings

### 1. Security plan_debt gaps (registry ↔ backlog)

Open rows in `lic/data/swarm-gap-registry/registry.yaml`:

- `sec-r1-httpd-fuzz-smoke` — HTTPD fuzz smoke vs tier5 exploit suite
- `sec-r2-tier5-gap-exploit` — close nginx_mitigations.toml exploit gaps
- `sec-r3-runtime-surface` — runtime attack surface survey (`lic` RT / sandbox)

All three are `status: pending` in `lic/docs/ecosystem/security-research-backlog.md`. Correct handoff: **`security_auditor`** on goal **`offensive_security`** (cadence 12h, priority 9) — not a new systemd loop.

### 2. CWE catalog coverage

From `benchmarks/data/latest/security-cwe-feed.json` (2026-06-06T21:23Z):

| Metric | Value |
|--------|-------|
| Top 25 baseline | 25 |
| Catalog entries | 15 |
| Missing in catalog | **19** |
| Catalog gaps (script) | 0 |

Briefing correctly elevates `security_auditor` at P0. `security_cwe_audit` preflight was **skipped** (`--skip-slow`), weakening unattended CWE drift detection.

### 3. Control-plane security posture

| Signal | Severity | Evidence |
|--------|----------|----------|
| No persisted observer state | high | ENOENT `/app/data/control-plane/state.json` (bootstrapped this run) |
| Gap apply blocked | high | PyYAML missing; ingest syntax fixed |
| GitHub rate limit | medium | `org_ci_audit` HTTP 403 |
| 34 failed PR CI | high | `ecosystem-audit.metrics.failed_prs` |
| Agent-kit audit fail | low | `/workspace/roadmap/agent-kit` not found |

`CURSOR_API_KEY` is **set** — SDK auth is not the blocker this cycle.

### 4. Goal orientation drift

| Briefing P0 | Scorecard | Drift? |
|-------------|-----------|--------|
| `pr_merger` (lip#52) | yes | aligned |
| `ci_maintainer` (14 repos) | yes | aligned |
| `security_auditor` (CWE Top25) | yes | aligned |

No priority inversion detected; execution blocked by infra (rate limit, PyYAML, merge queue).

---

## Recommendations

1. **Merge infra fix:** bake PyYAML in org-research worker; re-run ingest + apply.
2. **Dispatch `security_auditor`** on `sec-r1-httpd-fuzz-smoke` immediately after merge queue clears lip#52.
3. **Human-gated:** backfill 19 CWE rows in `lic/security/cve-catalog.json`.
4. **Observer:** persist CP report/state on every tick (`src/control-plane/state.ts` mirror path).
5. **Preflight:** run `security_cwe_audit` when `security_auditor` is P0 (remove from `--skip-slow` set).

---

## Validity

| Field | Value |
|-------|-------|
| validity_grade | B (evidence from live briefing + scorecard; gap apply not re-run) |
| status | staging |
| run_id | 1780780200201 |

---

## References

- `benchmarks/data/latest/ecosystem-quality-report.json`
- `benchmarks/data/latest/security-cwe-feed.json`
- `lic/docs/ecosystem/orchestrator-notes/2026-06-06-orch-r8-security-1e1865fe.md`
- `/app/data/runs/swarm_observer-1780780200201.md`
