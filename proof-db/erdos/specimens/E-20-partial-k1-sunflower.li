# partial: k=1 sunflower — every non-empty family of 1-sets is a 1-sunflower
# discharge: ok
# Exponential sunflower bound (k>=2) remains open.

def f_sunflower_k1(n: int) -> int
  requires n >= 1
  ensures result == 1
  decreases 0
=
  return 1

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  var bound: int = f_sunflower_k1(10)
  return 0
