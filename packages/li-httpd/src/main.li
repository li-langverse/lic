# Thin demo — composable API lives in lib.li (import li_httpd from other programs).

import li_httpd

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  httpd_stop(httpd_serve(8080))
  return httpd_tag()
