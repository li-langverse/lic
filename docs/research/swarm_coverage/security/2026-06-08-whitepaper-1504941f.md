# Swarm coverage — security dimension whitepaper

**Goal id:** `swarm_coverage`  
**Dimension:** `security`  
**Worker:** `1504941f`  
**Date:** 2026-06-08  
**Publish target:** `research-findings/whitepapers/2026-06/swarm_coverage/security/`  
**north_star_fit:** ecosystem, ai — provable, secure agent orchestration

## Abstract

This pass audits swarm gap orchestration through a **security lens**: whether security plan debt is routed to the correct agents, whether CWE/catalog signals reach `security_auditor`, and whether control-plane self-heal can run gap ingest/apply without human intervention.

## Key findings

### 1. Security plan_debt is backlogged but runner is stopped

The `security-research` goal-directed runner is **not running** (`snapshot.json` @ 2026-05-30). Three open registry gaps (`sec-r1`–`sec-r3`) were re-patched to `security-research-backlog.md` on 2026-06-08 after ingest/apply recovery.

**Recommendation:** Dispatch `security_auditor` on the `offensive_security` research goal — not a revived systemd plan loop.

### 2. CWE Top25 catalog gap (19 missing)

`security-cwe-feed.json` (2026-06-08T17:50Z) reports **19** MITRE Top25 CWEs missing from `cve-catalog.json`. `sec-r0-cwe-delta` is completed; catalog expansion remains human-gated.

### 3. Control-plane security of gap orchestration

| Blocker | Severity | Remediation |
|---------|----------|-------------|
| `swarm-gap-ingest.py` SyntaxError line 229 | critical | **Fixed** this cycle |
| `PyYAML` missing in container | high | **Fixed** (`apt install python3-yaml`) |
| `state.json` / `latest-report.json` absent | medium | **Bootstrapped** |
| `runs_sampled=0` (grader path) | medium | Set `LI_CURSOR_AGENTS_ROOT=/app` in worker image |

### 4. Failed PRs with security surface

Three `lis` PRs (#40–42) have failing CI on registry/edge/MCP work — security-relevant attack surface. Route to `bug_fixer`, not auto-merge.

## Gap taxonomy (security-relevant rows)

| `gap_kind` | Open (post-ingest) | Security role |
|------------|-------------------|---------------|
| `plan_debt` | 31 | `sec-r1`–`r3` → `security_auditor`; httpd tier5 → `offensive_security` |
| `competitor_feature` | 30 | tier5 exploit stubs — research lane only |
| `missing_package` | 1 | `li-line-profiler` — perf tooling, not security-critical |

## Evidence paths

- `/workspace/benchmarks/data/latest/ecosystem-quality-report.json`
- `/workspace/benchmarks/data/latest/swarm-gap-actions.json`
- `/workspace/benchmarks/data/latest/security-cwe-feed.json`
- `/workspace/lic/data/swarm-gap-registry/registry.yaml`
- `/workspace/lic/docs/ecosystem/security-research-backlog.md`
- `/app/data/runs/swarm_observer-1780938559863.md`
