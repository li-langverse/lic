# E-485: Let $f(k)$ be the minimum number of terms in $P(x)^2$, where $P\in \mathbb{Q}[x]$ ranges over all polynomials with exactly $k$ non-zero terms. Is it true that $f(k)\to\infty$ as $k\to \infty$?
# erdos_number: 485
# erdos_status: proved
# priority_tier: P2
# tags: analysis, polynomials

def erdos_e485_let_f_k_be_the_minimum_number_of_terms_i() -> int
  requires true
  ensures result == 0  # open: E-485
  decreases 0
=
  return 0


def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
