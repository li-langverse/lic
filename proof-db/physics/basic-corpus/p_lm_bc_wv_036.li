# P-LM-BC-WV-036: Sound speed c = sqrt(gamma R T / M) ideal gas.

def p_lm_bc_wv_036_witness(x: float) -> float
  requires true
  ensures result == x
  decreases 0
=
  return x

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
