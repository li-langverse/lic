# std/physics/custom — arbitrary law facade (PH-PHYS-CUSTOM).
# Prefer: import physics.custom  |  workspace packages/physics.custom.

def physics_custom_std_tag() -> int
  requires true
  ensures result == 1
  decreases 0
=
  return 1
