# E-186: Let $F(N)$ be the maximal size of $A\subseteq \{1,\ldots,N\}$ which is 'non-averaging', so that no $n\in A$ is the arithmetic mean of at least two elements in $A$. What is the order of growth of $F(N)$?
# erdos_number: 186
# erdos_status: open
# priority_tier: P2
# tags: additive_combinatorics

def erdos_e186_let_f_n_be_the_maximal_size_of_a_subsete() -> int
  requires true
  ensures result == 0  # open: E-186
  decreases 0
=
  return 0

extern proc erdos_e186_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
