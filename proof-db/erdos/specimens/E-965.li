# E-965: Is it true that, for any $2$-colouring of $\mathbb{R}$, there is a set $A\subseteq \mathbb{R}$ of cardinality $\aleph_1$ such that all sums $a+b$ with $a\neq b$ and $a,b\in A$ are the same colour?
# erdos_number: 965
# erdos_status: open
# priority_tier: P2
# tags: ramsey_theory

def erdos_e965_is_it_true_that_for_any_2_colouring_of_m() -> int
  requires true
  ensures result == 0  # open: E-965
  decreases 0
=
  return 0

extern proc erdos_e965_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
