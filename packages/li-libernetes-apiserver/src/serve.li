# Apiserver serve entry — wires informer_sync cache loop (Wave 3).
def apiserver_serve_stub() -> int
  requires true
  ensures result == 0
  decreases 0
=
  let informer_sync = informer_sync_resync()
  return informer_sync
