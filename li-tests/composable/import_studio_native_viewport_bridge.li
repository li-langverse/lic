import studio
import render
import scene

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  if studio_native_viewport_bridge_smoke() != 0:
    return 1
  if render_studio_viewport_tick_stub(1280, 720) != 1:
    return 2
  if studio_viewport_bind_render_stub(1280, 720, 4) != render_studio_viewport_tick_stub(1280, 720) + 2003:
    return 3
  return scene_smoke()
