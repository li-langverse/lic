# E-448: Let $\tau(n)$ count the divisors of $n$ and $\tau^+(n)$ count the number of $k$ such that $n$ has a divisor in $[2^k,2^{k+1})$. Is it true that, for all $\epsilon>0$,\[\tau^+(n) < \epsilon \tau(n)\]for almost all $n$?
# erdos_number: 448
# erdos_status: open
# priority_tier: P2
# tags: divisors, number_theory

def erdos_e448_let_tau_n_count_the_divisors_of_n_and_ta() -> int
  requires true
  ensures result == 0  # open: E-448
  decreases 0
=
  return 0

extern proc erdos_e448_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
