# E-588: Let $f_k(n)$ be minimal such that if $n$ points in $\mathbb{R}^2$ have no $k+1$ points on a line then there must be at most $f_k(n)$ many lines containing at least $k$ points. Is it true that\[f_k(n)=o(n^2)\]for $k\geq 4$?
# erdos_number: 588
# erdos_status: open
# priority_tier: P2
# tags: geometry

def erdos_e588_let_f_k_n_be_minimal_such_that_if_n_poin() -> int
  requires true
  ensures result == 0  # open: E-588
  decreases 0
=
  return 0

extern proc erdos_e588_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
