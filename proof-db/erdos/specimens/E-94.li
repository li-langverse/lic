# E-94: Suppose $n$ points in $\mathbb{R}^2$ determine a convex polygon and the set of distances between them is $\{u_1,\ldots,u_t\}$. Suppose $u_i$ appears as the distance between $f(u_i)$ many pairs of points. Then\[\sum_i f(u_i)^2 \ll n^3.\]
# erdos_number: 94
# erdos_status: proved
# priority_tier: P2
# tags: convex, distances, geometry

def erdos_e94_suppose_n_points_in_mathbb_r_2_determine() -> int
  requires true
  ensures result == 0  # open: E-94
  decreases 0
=
  return 0

extern proc erdos_e94_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
