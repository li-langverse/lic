[server]
listen = ":39224"
document_root = "./public"

[limits]
max_body = "1m"
max_header = "16k"
rate_limit_rps = 60
concurrent_streams = 50

[upstreams.api]
peers = ["http://127.0.0.1:3000"]

[routes]
"POST /v1/* require=traceparent" = "proxy:api"
