# E-785: Let $A,B\subseteq \mathbb{N}$ be infinite sets such that $A+B$ contains all large integers. Let $A(x)=\lvert A\cap [1,x]\rvert$ and similarly for $B(x)$. Is it true that if $A(x)B(x)\sim x$ then\[A(x)B(x)-x\to \infty\]as $x\to \infty$?
# erdos_number: 785
# erdos_status: proved
# priority_tier: P2
# tags: additive_combinatorics

def erdos_e785_let_a_b_subseteq_mathbb_n_be_infinite_se() -> int
  requires true
  ensures result == 0  # open: E-785
  decreases 0
=
  return 0

extern proc erdos_e785_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
