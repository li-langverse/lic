def good_parallel(grid: var array[64, array[64, f64]]) -> unit
  requires true
  ensures true
  decreases 0
=
  parallel for i in 0..<64
    requires disjoint_row(i, grid)
    invariant row_ok(i, grid)
    decreases 64 - i
  =
    grid[i][0] = grid[i][0] + 1.0
