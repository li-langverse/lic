# Swarm gap orchestration — performance dimension

**Goal:** `swarm_coverage`  
**Dimension:** `performance`  
**Worker:** `5bc2e236`  
**Generated:** 2026-06-08T05:31Z  
**north_star_fit:** Swarm gap orchestration — registry, backlog apply, handoffs — domains: ecosystem, ai  
**Publish target:** `research-findings/whitepapers/2026-06/swarm_coverage/performance/` (out-of-band when repo mounted)

---

## Abstract

This pass audits Li swarm health through a **performance lens**: where does orchestration waste cycles, and which open gaps block proof-before-fast numerics and httpd soak gates? The ecosystem quality scorecard regenerated at **65.3 (D)** with `unattended_safe: false`. The dominant performance bottleneck is **control-plane friction** (gap ingest blocked, runs_dir path mismatch, 6 stopped goal runners), not LLM token latency. Numerics posture remains strong (145 green, 2 yellow, 5 near-threshold vs C++) but PH-7e closure is the right performance work — after proof.

---

## 1. Orchestration performance metrics

| Signal | Value | Evidence |
|--------|-------|----------|
| Ecosystem grade | D / 65.3 | `benchmarks/data/latest/ecosystem-quality-report.json` |
| `runs_sampled` | 0 | Grader `runs_dir` = `/workspace/li-cursor-agents/data/runs` (empty); live runs at `/app/data/runs` |
| Gap ingest latency | ∞ (blocked) | PyYAML missing; syntax error on line 229 remediated this pass |
| Open registry gaps | 64 | `swarm-gap-actions.json` |
| Goal runners stopped | 6 / 9 | `lic/data/goal-directed-agents/snapshot.json` |
| Preflight failures | 2 | `org_ci_audit`, `org_agent_kit_audit` |
| Briefing P0 queue | `pr_merger` → `ci_maintainer` | `agent-briefing.json` heap_plan |

**Interpretation:** The swarm burns supervisor ticks on meta-audit retries when persist paths diverge. Setting `LI_CURSOR_AGENTS_ROOT=/app` in org-research Jobs and baking `python3-yaml` removes two recurring blockers (~20–40 min/retry storm in prior `org-research-audit.jsonl` samples).

---

## 2. Numerics performance posture (PH-7e)

From `agent-briefing.json` → `ecosystem_audit.benchmarks` (matrix dated 2026-06-01):

| Tier | Workload ids | Action |
|------|--------------|--------|
| Yellow | `num_eig_symmetric`, `num_root_newton` | `bench_improver` + proved codegen path (lic#11 horner / PH-7e) |
| Near threshold | `num_opt_bfgs`, `num_integ_*`, `num_cg` | Monitor; no unsafe SIMD shortcuts |
| Green | 145 | Maintain regression gates |

**Gap registry linkage:** `plan_debt` rows for Phase 7e partial SIMD (`gap-plan-debt-lic-master-plan-phase-7e-math-simd-parallel-lowe`) → `plan_verifier` snapshot, then `numerics_researcher` via `numerics_sota` goal — not a new lic systemd loop.

---

## 3. httpd / server performance gaps

Registry `plan_debt` rows for httpd wrk soak (`gap-phase2-perf-wrk-soak`, streaming wrk variants) should route to:

- **Implement lane** / `server_platform` research goal
- **Handoff:** `goal_researcher` after `lic build` proof on handler paths

Soak benchmarks measure throughput; they do not bypass provability gates.

---

## 4. Self-heal vs human intervention

| Auto-heal (programmatic observer) | Status |
|-----------------------------------|--------|
| Error streak retries | N/A — `runs_sampled=0`, no error runs in `/app/data/runs` |
| `bug_fixer` / `workspace_sweeper` | Not dispatched — `workspace_dirty_sweep` clean |
| Gap apply patches | Blocked — PyYAML |

| Control-plane fix (this run) | File |
|------------------------------|------|
| `verticals.toml` multi-candidate Path | `lic/scripts/swarm-gap-ingest.py` |

---

## 5. Recommendations (performance-ordered)

1. **P0 infra:** Bake PyYAML + `LI_CURSOR_AGENTS_ROOT=/app` on org-research Jobs (`li-cursor-agents`).
2. **P0 dispatch:** `pr_merger` (lip#52) → `ci_maintainer` (12 repos) — unblocks merge throughput.
3. **P1 numerics:** Close yellow eigen/newton via proved PH-7e path (`bench_improver`, `numerics_researcher`).
4. **P1 gaps:** Re-run ingest/apply after PyYAML; route 30 `competitor_feature` stubs to research lane.
5. **P2 httpd:** Dispatch wrk soak plan todos via implement lane after ingest green.

---

## Evidence index

- `/workspace/benchmarks/data/latest/ecosystem-quality-report.json`
- `/workspace/benchmarks/data/latest/agent-briefing.json`
- `/workspace/benchmarks/data/latest/swarm-gap-actions.json`
- `/workspace/lic/data/swarm-gap-registry/registry.yaml`
- `/app/data/control-plane/state.json` (bootstrapped)
- `/app/data/goal-directed-sprints/org-research-audit.jsonl`
