# E-450: How large must $y=y(\epsilon,n)$ be such that the number of integers in $(x,x+y)$ with a divisor in $(n,2n)$ is at most $\epsilon y$?
# erdos_number: 450
# erdos_status: open
# priority_tier: P2
# tags: divisors, number_theory

def erdos_e450_how_large_must_y_y_epsilon_n_be_such_tha() -> int
  requires true
  ensures result == 0  # open: E-450
  decreases 0
=
  return 0

extern proc erdos_e450_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
