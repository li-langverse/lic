# Stub — libernetes VirtualMachine CRD spec (Wave 1 livm; KubeVirt API shape for compat)
apiVersion: kubevirt.io/v1
kind: VirtualMachine
metadata:
  name: example
spec:
  runStrategy: Always
  template:
    spec:
      domain:
        devices: {}
      volumes: []
