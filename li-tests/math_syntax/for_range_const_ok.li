# G-math-syn: `for i in range(n)` with const-int witness from prior binding.
def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  var n: int = 4
  for i in range(n)
    discard
  return 0
