# G-math-syn: `for i in range(n)` with const-int witness from prior binding.
def main() -> int
  requires true
  ensures result == 6
  decreases 0
=
  var n: int = 4
  var acc: int = 0
  for i in range(n)
    acc = acc + i
  return acc
