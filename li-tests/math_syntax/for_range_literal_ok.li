# G-math-syn: `for i in range(n)` with literal compile-time bound.
def main() -> int
  requires true
  ensures result == 6
  decreases 0
=
  var acc: int = 0
  for i in range(4)
    acc = acc + i
  return acc
