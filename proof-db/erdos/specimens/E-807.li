# E-807: The bipartition number $\tau(G)$ of a graph $G$ is the smallest number of pairwise edge disjoint complete bipartite graphs whose union is $G$. The independence number $\alpha(G)$ is the size of the largest independent subset of $G$. Is it true that, if $G$ is a random graph on $n$ vertices with edge probability $1/2$, then\[\tau(G)=n-\alpha(G)\]almost surely?
# erdos_number: 807
# erdos_status: open
# priority_tier: P2
# tags: graph_theory

def erdos_e807_the_bipartition_number_tau_g_of_a_graph() -> int
  requires true
  ensures result == 0  # open: E-807
  decreases 0
=
  return 0

extern proc erdos_e807_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
