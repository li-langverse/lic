# E-1018: Let $\epsilon>0$. Is there a constant $C_\epsilon$ such that, for all large $n$, every graph on $n$ vertices with at least $n^{1+\epsilon}$ edges must contain a subgraph on at most $C_\epsilon$ vertices which is non-planar?
# erdos_number: 1018
# erdos_status: open
# priority_tier: P2
# tags: graph_theory, planar_graphs

def erdos_e1018_let_epsilon_0_is_there_a_constant_c_epsi() -> int
  requires true
  ensures result == 0  # open: E-1018
  decreases 0
=
  return 0


def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
