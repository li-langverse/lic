# E-504: Let $\alpha_n$ be the supremum of all $0\leq \alpha\leq \pi$ such that in every set $A\subset \mathbb{R}^2$ of size $n$ there exist three distinct points $x,y,z\in A$ such that the angle determined by $xyz$ is at least $\alpha$. Determine $\alpha_n$.
# erdos_number: 504
# erdos_status: open
# priority_tier: P2
# tags: geometry

def erdos_e504_let_alpha_n_be_the_supremum_of_all_0_leq() -> int
  requires true
  ensures result == 0  # open: E-504
  decreases 0
=
  return 0

extern proc erdos_e504_open_witness() -> int
  requires true
  ensures result == 0
  decreases 0

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
