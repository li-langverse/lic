# D-LM-BC-CMB-027: Hockey-stick identity: sum_{i=r}^n C(i,r) = C(n+1,r+1).

def binomial_symmetry(n: int, k: int) -> int
  requires n >= 0 and 0 <= k and k <= n
  ensures result == n - k
  decreases 0
=
  return n - k

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
