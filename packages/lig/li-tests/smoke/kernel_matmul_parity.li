import lig
def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  if lig_kernel_run(lig_kernel_matmul_f32(), lig_backend_select_auto()) != 0: return 1
  if lig_validity_gate_pass(0.999) != 1: return 2
  return 0
