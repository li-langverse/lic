[package]
name = "li-libernetes-scheduler"
version = "0.0.1"
edition = "2026"
license = "Apache-2.0 OR MIT"
description = "libernetes pod scheduling (containers + VMs)"

[package.metadata.li]
github_repo = "libernetes"
import_name = "libernetes_scheduler"
min_lic = "0.1.0"
maintainer = "li-langverse"
pkg_id = "PKG-libernetes-scheduler"
