# Swarm gap orchestration — performance dimension

**Goal:** `swarm_coverage`  
**Dimension:** `performance`  
**Worker:** `e7e99635`  
**Date:** 2026-06-07  
**north_star_fit:** ecosystem, ai — proof → easy → fast (PH-5b numerics, PH-7e codegen, PH-8p CI throughput)

---

## Abstract

This pass audits swarm health through a **performance lens**: benchmark posture, gap-registry pressure on numerics/httpd/sim, and control-plane blockers that prevent unattended gap re-ingest. The Li swarm is **degraded** (`unattended_safe: false`, grade C 72.6) despite zero terminal run errors in the current worker — structural gaps (64 open registry rows, 6 stopped plan runners, missing PyYAML) dominate.

---

## Benchmark posture (2026-06-07)

Source: `/workspace/benchmarks/data/latest/ecosystem-audit.json` (benchmarks block generated 2026-06-01).

| Class | Count | IDs |
|-------|-------|-----|
| Green | 145 | — |
| Yellow | 2 | `num_eig_symmetric`, `num_root_newton` |
| Near-threshold (1.18–1.20× cpp) | 5 | `num_opt_bfgs`, `num_integ_semi_implicit`, `num_integ_euler`, `num_integ_rk4`, `num_cg` |
| Red (live audit) | 0 | — |
| Red (registry competitor_feature) | 8+ | matmul, gmres, line_search, euler, verlet, cloth, orbit, schrodinger |

**Pillar order:** No perf work may bypass proof. Yellow eigen/root solvers route to `numerics_researcher` under `numerics_sota` with Lean witness discipline; SIMD matmul remains deferred per master-plan Phase 7e partial.

---

## Gap orchestration state

| Signal | Value | Evidence |
|--------|-------|----------|
| Open gaps | 64 | `swarm-gap-actions.json` |
| By kind | 30 competitor, 31 plan_debt, 3 missing_package | registry.yaml |
| Last apply | 2026-05-31 | stale vs snapshot |
| Ingest status | Syntax fixed; PyYAML blocked | `swarm-gap-ingest.py` |

Performance-critical open plan_debt includes `gap-phase2-perf-wrk-soak` (httpd wrk soak) and sim todos `sim-p1-num-dot-axpy`, `sim-p1-md-neighbor-cell`, `sim-p2-qm-dft-scf`.

---

## Swarm execution health

| Metric | Value |
|--------|-------|
| runs_sampled | 1 |
| terminal_runs | 0 |
| error_rate | 0% |
| running_count | 1 (`swarm_observer`) |
| Goal drift | Briefing recommends `pr_merger`, `ci_maintainer`, `security_auditor` — not in recent_runs on this worker |

Triage stability (24h): fail_rate dropped to 0% by 2026-06-07T14:00Z after morning GH rate-limit backoff (`org-swarm-stability-audit.jsonl`).

---

## Recommendations

1. **Infra:** Bake `python3-yaml` in org-research worker; merge ingest syntax fix to `lic` main.
2. **Dispatch:** `pr_merger` → `ci_maintainer` → `bench_improver` + `numerics_researcher` → `gap_explorer`.
3. **Research goals:** Enable cadence on `numerics_sota`, `md_sim_algorithms` for yellow/red bench closure.
4. **Observer:** Persist `state.json` / `latest-report.json` each supervisor tick (not only meta-agent bootstrap).

---

## Publish path

Target: `research-findings/whitepapers/2026-06/swarm_coverage/performance/` (repo not mounted in this worker).

Staging copy: this file.
