def mul_distrib_instance(a: float, b: float, c: float) -> float
  requires true
  ensures result == a * b + a * c
  decreases 0
=
  return a * (b + c)

def main() -> int
  requires true
  ensures result == 0
  decreases 0
=
  return 0
