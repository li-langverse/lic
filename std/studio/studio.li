# std/studio — World Studio shell facade (PH-GD).
# Prefer: import studio  |  workspace packages/li-studio.

def studio_std_tag() -> int
  requires true
  ensures result == 1
  decreases 0
=
  return 1
